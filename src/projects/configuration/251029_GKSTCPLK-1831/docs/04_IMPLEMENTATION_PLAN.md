# План реализации задачи 251029_GKSTCPLK-1831

**Задача**: Разработать АРМ по промежуточному композиту для УС по партиям ТС
**Дата составления плана**: 2025-10-29
**Статус**: Готов к реализации
**Связанные документы**:
- 01_TASK_OVERVIEW_AND_ARCHITECTURE.md
- 02_MODULES_DETAILED_ANALYSIS.md
- 03_SQL_QUERIES_ANALYSIS.md

---

## 📋 Содержание

1. [Общая информация](#общая-информация)
2. [Этап 0: Подготовка](#этап-0-подготовка)
3. [Этап 1: Создание структуры данных](#этап-1-создание-структуры-данных)
4. [Этап 2: Разработка серверной логики](#этап-2-разработка-серверной-логики)
5. [Этап 3: Разработка основной формы](#этап-3-разработка-основной-формы)
6. [Этап 4: Форма формирования композитной пробы](#этап-4-форма-формирования-композитной-пробы)
7. [Этап 5: Интеграция с документами](#этап-5-интеграция-с-документами)
8. [Этап 6: Тестирование](#этап-6-тестирование)
9. [Этап 7: Оптимизация](#этап-7-оптимизация)
10. [Этап 8: Документация и внедрение](#этап-8-документация-и-внедрение)

---

## Общая информация

### Сводка по срокам

| Этап | Описание | Время | Зависимости |
|------|----------|-------|-------------|
| 0 | Подготовка | 0.5 дня | - |
| 1 | Создание структуры данных | 1 день | Этап 0 |
| 2 | Разработка серверной логики | 2 дня | Этап 1 |
| 3 | Разработка основной формы | 2.5 дня | Этап 2 |
| 4 | Форма формирования композитной пробы | 1.5 дня | Этап 3 |
| 5 | Интеграция с документами | 2 дня | Этап 4 |
| 6 | Тестирование | 2 дня | Этап 5 |
| 7 | Оптимизация | 1.5 дня | Этап 6 |
| 8 | Документация и внедрение | 1 день | Этап 7 |
| **ИТОГО** | | **14 дней** | |

### Команда

| Роль | Ответственность | Время |
|------|-----------------|-------|
| Разработчик 1С | Основная разработка | 100% |
| Тестировщик | Функциональное тестирование | 30% |
| Бизнес-аналитик | Валидация требований | 10% |
| Администратор БД | Создание индексов | 5% |

### Критерии готовности

✅ **Definition of Done**:
1. Все функции из ТЗ реализованы и работают
2. Пройдено функциональное тестирование
3. Пройдено тестирование производительности (запросы < 3 сек)
4. Создана пользовательская документация
5. Код соответствует стандартам разработки
6. Код прошел code review
7. Создан релиз и внесен в систему контроля версий

---

## Этап 0: Подготовка

**Длительность**: 0.5 дня (4 часа)
**Ответственный**: Разработчик 1С

### 0.1 Изучение существующей системы

**Время**: 2 часа

#### Задачи:
- [ ] Изучить структуру документа `гкс_РегистрацияНаПЛК`
  - Реквизиты: Организация, ПЛК, Номенклатура, Контрагент, ПроизводственныеСутки
  - Табличные части
  - Проведение документа

- [ ] Изучить документ `гкс_ФормированиеНомераПробы`
  - Реквизиты: НазначениеИспользованияКачества, ТипПробы
  - Табличная часть Регистрации
  - Табличная часть Показатели
  - Связь с лабораторным анализом

- [ ] Изучить документ `гкс_ЛабораторныйАнализ`
  - Как создается
  - Как заполняется показателями
  - Связь с формированием номера пробы

- [ ] Изучить регистры сведений:
  - `гкс_ПоказателиКачестваУсиленногоКонтроля`
  - `гкс_СостоянияРегистрации`
  - `гкс_СоставГруппТранспорта`
  - `гкс_КомпозитныеПробы`
  - `гкс_НормативнаяАттестацияНоменклатуры`

#### Артефакты:
- Документ "Анализ существующих объектов.md" в папке docs

### 0.2 Настройка окружения

**Время**: 1 час

#### Задачи:
- [ ] Создать конфигурацию для разработки (если не создана)
- [ ] Создать тестовую базу данных
- [ ] Подготовить тестовые данные:
  - Минимум 50 регистраций на ПЛК
  - 3-5 показателей качества для номенклатуры
  - 2-3 группы транспорта
  - Нормативная аттестация для тестовой номенклатуры

- [ ] Настроить систему контроля версий (Git)
  - Создать ветку `feature/251029_intermediate_composite`
  - Настроить .gitignore

#### Команды:
```bash
cd /path/to/configuration
git checkout -b feature/251029_intermediate_composite
git push -u origin feature/251029_intermediate_composite
```

### 0.3 Создание плана тестирования

**Время**: 1 час

#### Задачи:
- [ ] Создать документ "План тестирования.md"
- [ ] Составить список тест-кейсов (минимум 20)
- [ ] Подготовить тестовые сценарии
- [ ] Определить критерии успешности тестов

#### Шаблон тест-кейса:
```markdown
## TC-001: Открытие АРМ в режиме промежуточного композита

**Предусловия**: Пользователь авторизован, есть права на обработку

**Шаги**:
1. Открыть команду "Промежуточный композит"
2. Проверить открытие формы
3. Проверить активную страницу

**Ожидаемый результат**: Форма открывается на странице "Промежуточные пробы"

**Фактический результат**:

**Статус**: [ ] Passed / [ ] Failed
```

---

## Этап 1: Создание структуры данных

**Длительность**: 1 день (8 часов)
**Ответственный**: Разработчик 1С

### 1.1 Создание обработки гкс_АРМПромежуточныйКомпозит

**Время**: 2 часа

#### 1.1.1 Создать обработку в конфигураторе

**Шаги**:
```
1. Конфигуратор → Обработки → Добавить
2. Имя: гкс_АРМПромежуточныйКомпозит
3. Синоним: АРМ Промежуточный композит
4. Комментарий: "АРМ для работы с промежуточными композитными пробами по УК"
```

#### 1.1.2 Добавить реквизиты обработки

**Реквизиты**:
```bsl
// Не требуется, вся логика в формах
```

#### 1.1.3 Создать команду открытия

**Параметры команды**:
- Имя: `ПромежуточныйКомпозит`
- Синоним: `Промежуточный композит`
- Группа: `Панель навигации`
- Картинка: Иконка пробирки/анализа

**Код команды** (`Commands/ПромежуточныйКомпозит/CommandModule.bsl`):
```bsl
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ПромежуточныйКомпозит", Истина);

	ОткрытьФорму(
		"Обработка.гкс_АРМПромежуточныйКомпозит.Форма.Форма",
		ПараметрыФормы,
		ПараметрыВыполненияКоманды.Источник,
		ПараметрыВыполненияКоманды.Уникальность,
		ПараметрыВыполненияКоманды.Окно,
		ПараметрыВыполненияКоманды.НавигационнаяСсылка);

КонецПроцедуры
```

#### Чеклист:
- [ ] Обработка создана
- [ ] Команда создана и отображается в интерфейсе
- [ ] Команда открывает форму обработки
- [ ] Проверено в режиме "1С:Предприятие"

### 1.2 Проверка регистров сведений

**Время**: 2 часа

#### 1.2.1 Регистр гкс_ПоказателиКачестваУсиленногоКонтроля

**Проверить наличие измерений и ресурсов**:
```bsl
Измерения:
- Регистрация (ДокументСсылка.гкс_РегистрацияНаПЛК)
- Показатель (СправочникСсылка.Показатели)

Ресурсы:
- СостояниеКачества (ПеречислениеСсылка.гкс_СостоянияКачестваУсиленногоКонтроля)
- НазначениеИспользования (ПеречислениеСсылка.гкс_НазначенияИспользованияКачества)
```

**Если отсутствует** → Создать регистр:
```
1. Регистры сведений → Добавить
2. Имя: гкс_ПоказателиКачестваУсиленногоКонтроля
3. Периодичность: Непериодический
4. Добавить измерения и ресурсы
```

#### 1.2.2 Регистр гкс_СоставГруппТранспорта

**Проверить структуру**:
```bsl
Измерения:
- Регистрация (ДокументСсылка.гкс_РегистрацияНаПЛК)

Ресурсы:
- ГруппаТС (СправочникСсылка.ГруппыТранспорта) // или другой тип
- ИдентификаторГруппыТС (Строка)
- ПЛК (СправочникСсылка.ПЛК)
```

#### 1.2.3 Регистр гкс_КомпозитныеПробы

**Проверить структуру**:
```bsl
Измерения:
- ДокументПроба (ДокументСсылка.гкс_ФормированиеНомераПробы)
- Регистрация (ДокументСсылка.гкс_РегистрацияНаПЛК)

Ресурсы:
- ТипПробы (ПеречислениеСсылка.гкс_ТипыПроб)
- Показатель (СправочникСсылка.Показатели) // если нужно
```

#### Чеклист:
- [ ] Все регистры существуют
- [ ] Структура регистров соответствует требованиям
- [ ] Создан тестовый набор данных в регистрах

### 1.3 Проверка перечислений

**Время**: 1 час

#### 1.3.1 Перечисление гкс_НазначенияИспользованияКачества

**Проверить наличие значения**:
```bsl
ПромежуточныйКомпозит
```

**Если отсутствует** → Добавить значение:
```
1. Перечисления → гкс_НазначенияИспользованияКачества
2. Добавить значение "ПромежуточныйКомпозит"
3. Синоним: "Промежуточный композит"
```

#### 1.3.2 Перечисление гкс_ТипыПроб

**Проверить значения**:
```bsl
Единичная
Композитная
```

#### 1.3.3 Перечисление гкс_СостоянияКачестваУсиленногоКонтроля

**Проверить значения**:
```bsl
ОтборПроб
НеПринятоВходнойКонтроль
ПустаяСсылка (встроенное)
```

#### Чеклист:
- [ ] Все перечисления существуют
- [ ] Значения перечислений соответствуют ТЗ
- [ ] Синонимы заполнены корректно

### 1.4 Создание индексов (критично для производительности)

**Время**: 3 часа

#### 1.4.1 Индексы для РС.гкс_ПоказателиКачестваУсиленногоКонтроля

**Создать составной индекс**:
```
Имя: Idx_ПКУК_Reg_Naznach_Sost
Поля:
1. Регистрация
2. НазначениеИспользования
3. СостояниеКачества
```

**Как создать**:
```
1. Открыть регистр в конфигураторе
2. Индексы → Добавить
3. Указать поля в правильном порядке
4. Обновить конфигурацию БД
```

#### 1.4.2 Индексы для РС.гкс_КомпозитныеПробы

**Индекс 1**:
```
Имя: Idx_KomProby_Reg_Pokaz_Tip
Поля:
1. Регистрация
2. Показатель (если есть)
3. ТипПробы
```

**Индекс 2**:
```
Имя: Idx_KomProby_Dokument
Поля:
1. ДокументПроба
```

#### 1.4.3 Индекс для РС.гкс_СоставГруппТранспорта

**Создать индекс**:
```
Имя: Idx_SostavGrupp_Reg
Поля:
1. Регистрация
```

#### 1.4.4 Индексы для Документ.гкс_РегистрацияНаПЛК

**Составной индекс**:
```
Имя: Idx_Registracia_Org_PLK_ProdSutki
Поля:
1. Организация
2. ПЛК
3. ПроизводственныеСутки
4. Проведен
```

#### 1.4.5 Индексы для Документ.гкс_ФормированиеНомераПробы

**Индекс 1**:
```
Имя: Idx_FormNomProby_Naznach_Tip_Org
Поля:
1. НазначениеИспользованияКачества
2. ТипПробы
3. Организация
4. ПЛК
5. ПроизводственныеСутки
```

**Индекс 2**:
```
Имя: Idx_FormNomProby_Data_Desc
Поля:
1. Дата (DESC)
```

#### Чеклист:
- [ ] Все индексы созданы в конфигураторе
- [ ] Выполнено обновление конфигурации БД
- [ ] Проверена работа индексов через монитор производительности
- [ ] Документировано создание индексов

#### SQL для проверки индексов (если нужно):
```sql
-- Для MS SQL Server
SELECT
    i.name AS IndexName,
    t.name AS TableName,
    c.name AS ColumnName
FROM sys.indexes i
INNER JOIN sys.index_columns ic ON i.object_id = ic.object_id AND i.index_id = ic.index_id
INNER JOIN sys.columns c ON ic.object_id = c.object_id AND ic.column_id = c.column_id
INNER JOIN sys.tables t ON i.object_id = t.object_id
WHERE t.name LIKE '%ПоказателиКачестваУсиленногоКонтроля%'
ORDER BY i.name, ic.key_ordinal;
```

---

## Этап 2: Разработка серверной логики

**Длительность**: 2 дня (16 часов)
**Ответственный**: Разработчик 1С

### 2.1 Модуль объекта обработки (ObjectModule.bsl)

**Время**: 8 часов

#### 2.1.1 Функция ТекстЗапросаДанныеДляДетальныхЗаписей()

**Цель**: Получить базовые данные регистраций с информацией о пробах

**Код** (создать в модуле объекта):
```bsl
Функция ТекстЗапросаДанныеДляДетальныхЗаписей()

	ТекстЗапроса =
	"// ==========================================
	|// ВРЕМЕННАЯ ТАБЛИЦА 1: ВТ_Регистрации
	|// ==========================================
	|ВЫБРАТЬ
	|	Регистрации.Ссылка КАК Регистрация,
	|	Регистрации.Дата КАК ДатаРегистрации,
	|	Регистрации.Номер,
	|	Регистрации.Организация,
	|	Регистрации.Контрагент,
	|	Регистрации.Номенклатура,
	|	Регистрации.ПЛК,
	|	Регистрации.ПроизводственныеСутки,
	|	ПоказателиКачества.Показатель,
	|	ПоказателиКачества.СостояниеКачества,
	|	СоставГруппТС.ГруппаТС,
	|	СоставГруппТС.ИдентификаторГруппыТС
	|ПОМЕСТИТЬ ВТ_Регистрации
	|ИЗ
	|	Документ.гкс_РегистрацияНаПЛК КАК Регистрации
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.гкс_ПоказателиКачестваУсиленногоКонтроля КАК ПоказателиКачества
	|			ПО Регистрации.Ссылка = ПоказателиКачества.Регистрация
	|			И ПоказателиКачества.НазначениеИспользования =
	|				ЗНАЧЕНИЕ(Перечисление.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.гкс_СоставГруппТранспорта КАК СоставГруппТС
	|			ПО Регистрации.Ссылка = СоставГруппТС.Регистрация
	|ГДЕ
	|	Регистрации.Проведен = ИСТИНА
	|	И Регистрации.Организация В (&МассивОрганизаций)
	|	И Регистрации.ПЛК В (&МассивПЛК)
	|	И Регистрации.ПроизводственныеСутки МЕЖДУ &ДатаНачала И &ДатаОкончания
	|	И (ПоказателиКачества.СостояниеКачества В (
	|			ЗНАЧЕНИЕ(Перечисление.гкс_СостоянияКачестваУсиленногоКонтроля.ПустаяСсылка),
	|			ЗНАЧЕНИЕ(Перечисление.гкс_СостоянияКачестваУсиленногоКонтроля.ОтборПроб))
	|		ИЛИ &ПоказыватьВсе = ИСТИНА)
	|;
	|
	|// ==========================================
	|// ВРЕМЕННАЯ ТАБЛИЦА 2: ВТ_КомпозитныеПробы
	|// ==========================================
	|ВЫБРАТЬ
	|	КомпозитныеПробы.ДокументПроба,
	|	КомпозитныеПробы.Регистрация,
	|	КомпозитныеПробы.ТипПробы
	|ПОМЕСТИТЬ ВТ_КомпозитныеПробы
	|ИЗ
	|	РегистрСведений.гкс_КомпозитныеПробы КАК КомпозитныеПробы
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Регистрации КАК ВТ_Регистрации
	|			ПО КомпозитныеПробы.Регистрация = ВТ_Регистрации.Регистрация
	|ГДЕ
	|	КомпозитныеПробы.ТипПробы = ЗНАЧЕНИЕ(Перечисление.гкс_ТипыПроб.Композитная)
	|	И КомпозитныеПробы.ДокументПроба.НазначениеИспользованияКачества =
	|		ЗНАЧЕНИЕ(Перечисление.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит)
	|;
	|
	|// ==========================================
	|// ВРЕМЕННАЯ ТАБЛИЦА 3: ВТ_ЕдиничныеПробы
	|// ==========================================
	|ВЫБРАТЬ
	|	КомпозитныеПробы.ДокументПроба,
	|	КомпозитныеПробы.Регистрация
	|ПОМЕСТИТЬ ВТ_ЕдиничныеПробы
	|ИЗ
	|	РегистрСведений.гкс_КомпозитныеПробы КАК КомпозитныеПробы
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Регистрации КАК ВТ_Регистрации
	|			ПО КомпозитныеПробы.Регистрация = ВТ_Регистрации.Регистрация
	|ГДЕ
	|	КомпозитныеПробы.ТипПробы = ЗНАЧЕНИЕ(Перечисление.гкс_ТипыПроб.Единичная)
	|;
	|
	|// ==========================================
	|// ФИНАЛЬНЫЙ SELECT
	|// ==========================================
	|ВЫБРАТЬ
	|	ВТ_Регистрации.Регистрация,
	|	ВТ_Регистрации.ДатаРегистрации,
	|	ВТ_Регистрации.Номер,
	|	ВТ_Регистрации.Номенклатура,
	|	ВТ_Регистрации.Контрагент,
	|	ВТ_Регистрации.Организация,
	|	ВТ_Регистрации.ПЛК,
	|	ВТ_Регистрации.ПроизводственныеСутки,
	|	ВТ_Регистрации.ГруппаТС,
	|	ВТ_Регистрации.ИдентификаторГруппыТС,
	|	ВТ_Регистрации.Показатель,
	|	ВТ_Регистрации.СостояниеКачества,
	|	ВЫБОР
	|		КОГДА ВТ_КомпозитныеПробы.ДокументПроба ЕСТЬ NULL
	|			ТОГДА 0
	|		ИНАЧЕ 1
	|	КОНЕЦ КАК ЕстьКомпозитнаяПроба,
	|	ВТ_КомпозитныеПробы.ДокументПроба КАК ДокументКомпозитнаяПроба,
	|	ВЫБОР
	|		КОГДА ВТ_ЕдиничныеПробы.ДокументПроба ЕСТЬ NULL
	|			ТОГДА 0
	|		ИНАЧЕ 1
	|	КОНЕЦ КАК ЕстьЕдиничнаяПроба,
	|	ВТ_ЕдиничныеПробы.ДокументПроба КАК ДокументЕдиничнаяПроба
	|ИЗ
	|	ВТ_Регистрации КАК ВТ_Регистрации
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КомпозитныеПробы КАК ВТ_КомпозитныеПробы
	|			ПО ВТ_Регистрации.Регистрация = ВТ_КомпозитныеПробы.Регистрация
	|			И ВТ_Регистрации.Показатель = ВТ_КомпозитныеПробы.Показатель
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕдиничныеПробы КАК ВТ_ЕдиничныеПробы
	|			ПО ВТ_Регистрации.Регистрация = ВТ_ЕдиничныеПробы.Регистрация
	|УПОРЯДОЧИТЬ ПО
	|	Номенклатура,
	|	Контрагент,
	|	ГруппаТС,
	|	Показатель";

	Возврат ТекстЗапроса;

КонецФункции
```

**Тестирование**:
```bsl
// Создать временную процедуру для теста
Процедура ТестЗапросаДанныеДляДетальныхЗаписей()
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапросаДанныеДляДетальныхЗаписей();

	// Тестовые параметры
	Запрос.УстановитьПараметр("МассивОрганизаций", ПолучитьТестовуюОрганизацию());
	Запрос.УстановитьПараметр("МассивПЛК", ПолучитьТестовыйПЛК());
	Запрос.УстановитьПараметр("ДатаНачала", НачалоДня(ТекущаяДата()));
	Запрос.УстановитьПараметр("ДатаОкончания", КонецДня(ТекущаяДата()));
	Запрос.УстановитьПараметр("ПоказыватьВсе", Ложь);

	Результат = Запрос.Выполнить();
	Сообщить("Количество строк: " + Результат.Выгрузить().Количество());
КонецПроцедуры
```

#### Чеклист 2.1.1:
- [ ] Функция создана
- [ ] Запрос выполняется без ошибок
- [ ] Проверено возвращение данных на тестовой базе
- [ ] Время выполнения < 3 секунд

#### 2.1.2 Функция ДанныеДляСписокПроизвольныйКомпозит()

**Код**:
```bsl
Функция ДанныеДляСписокПроизвольныйКомпозит(Параметры) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапросаДанныеДляДетальныхЗаписей();

	// Установка параметров
	Запрос.УстановитьПараметр("МассивОрганизаций", Параметры.МассивОрганизаций);
	Запрос.УстановитьПараметр("МассивПЛК", Параметры.МассивПЛК);
	Запрос.УстановитьПараметр("ДатаНачала", Параметры.ДатаНачала);
	Запрос.УстановитьПараметр("ДатаОкончания", Параметры.ДатаОкончания);
	Запрос.УстановитьПараметр("ПоказыватьВсе", Параметры.ПоказыватьВсе);

	Результат = Запрос.Выполнить();
	Возврат Результат.Выгрузить();

КонецФункции
```

#### Чеклист 2.1.2:
- [ ] Функция создана
- [ ] Экспортная
- [ ] Параметры передаются корректно
- [ ] Возвращает таблицу значений

#### 2.1.3 Функция ДанныеДляСписокПромежуточныеПробы()

**Код**:
```bsl
Функция ДанныеДляСписокПромежуточныеПробы(Параметры) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ФормированиеНомераПробы.Ссылка КАК ДокументПроба,
	|	ФормированиеНомераПробы.Дата,
	|	ФормированиеНомераПробы.Номер,
	|	ФормированиеНомераПробы.Организация,
	|	ФормированиеНомераПробы.ПЛК,
	|	ФормированиеНомераПробы.Номенклатура,
	|	ФормированиеНомераПробы.НомерПробы,
	|	ФормированиеНомераПробы.ЛабораторныйАнализ
	|ИЗ
	|	Документ.гкс_ФормированиеНомераПробы КАК ФормированиеНомераПробы
	|ГДЕ
	|	ФормированиеНомераПробы.НазначениеИспользованияКачества =
	|		ЗНАЧЕНИЕ(Перечисление.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит)
	|	И ФормированиеНомераПробы.ТипПробы =
	|		ЗНАЧЕНИЕ(Перечисление.гкс_ТипыПроб.Композитная)
	|	И ФормированиеНомераПробы.Организация В (&МассивОрганизаций)
	|	И ФормированиеНомераПробы.ПЛК В (&МассивПЛК)
	|	И ФормированиеНомераПробы.ПроизводственныеСутки МЕЖДУ &ДатаНачала И &ДатаОкончания
	|УПОРЯДОЧИТЬ ПО
	|	Дата УБЫВ";

	Запрос.УстановитьПараметр("МассивОрганизаций", Параметры.МассивОрганизаций);
	Запрос.УстановитьПараметр("МассивПЛК", Параметры.МассивПЛК);
	Запрос.УстановитьПараметр("ДатаНачала", Параметры.ДатаНачала);
	Запрос.УстановитьПараметр("ДатаОкончания", Параметры.ДатаОкончания);

	Результат = Запрос.Выполнить();
	Возврат Результат.Выгрузить();

КонецФункции
```

#### Чеклист 2.1.3:
- [ ] Функция создана
- [ ] Запрос простой без JOIN
- [ ] Сортировка по дате (убыв)
- [ ] Тестирование выполнено

#### 2.1.4 Функция ДанныеДляСписокРегистрацииПромежуточныхПроб()

**Код (оптимизированная версия)**:
```bsl
Функция ДанныеДляСписокРегистрацииПромежуточныхПроб(ДокументПроба) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	КомпозитныеПробы.Регистрация,
	|	Регистрации.Номер КАК НомерРегистрации,
	|	Регистрации.Дата КАК ДатаРегистрации,
	|	Регистрации.Номенклатура,
	|	Регистрации.Контрагент
	|ИЗ
	|	РегистрСведений.гкс_КомпозитныеПробы КАК КомпозитныеПробы
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.гкс_РегистрацияНаПЛК КАК Регистрации
	|			ПО КомпозитныеПробы.Регистрация = Регистрации.Ссылка
	|ГДЕ
	|	КомпозитныеПробы.ДокументПроба = &ДокументПроба
	|УПОРЯДОЧИТЬ ПО
	|	Регистрации.Дата УБЫВ";

	Запрос.УстановитьПараметр("ДокументПроба", ДокументПроба);

	Результат = Запрос.Выполнить();
	Возврат Результат.Выгрузить();

КонецФункции
```

#### Чеклист 2.1.4:
- [ ] Функция создана
- [ ] Использует явный JOIN вместо точечной нотации
- [ ] Сортировка оптимизирована
- [ ] Тестирование выполнено

### 2.2 Логирование производительности

**Время**: 2 часа

#### 2.2.1 Создать процедуру логирования

**Код** (добавить в ObjectModule.bsl):
```bsl
Процедура ЗаписатьЛогПроизводительности(ИмяМетода, ВремяВыполнения, Параметры)

	Попытка
		ТекстЛога = СтрШаблон(
			"[%1] Метод: %2, Время: %3 мс, Параметры: %4",
			ТекущаяДата(),
			ИмяМетода,
			ВремяВыполнения,
			ЗначениеВСтрокуВнутр(Параметры));

		// Записать в журнал регистрации
		ЗаписьЖурналаРегистрации(
			"АРМ.ПромежуточныйКомпозит.Производительность",
			УровеньЖурналаРегистрации.Предупреждение,
			,
			,
			ТекстЛога);
	Исключение
		// Игнорируем ошибки логирования
	КонецПопытки;

КонецПроцедуры
```

#### 2.2.2 Добавить логирование в функции

**Пример для ДанныеДляСписокПроизвольныйКомпозит**:
```bsl
Функция ДанныеДляСписокПроизвольныйКомпозит(Параметры) Экспорт

	ВремяНачала = ТекущаяУниверсальнаяДатаВМиллисекундах();

	Запрос = Новый Запрос;
	// ... код запроса ...
	Результат = Запрос.Выполнить();

	ВремяВыполнения = ТекущаяУниверсальнаяДатаВМиллисекундах() - ВремяНачала;

	Если ВремяВыполнения > 3000 Тогда // > 3 секунд
		ЗаписатьЛогПроизводительности(
			"ДанныеДляСписокПроизвольныйКомпозит",
			ВремяВыполнения,
			Параметры);
	КонецЕсли;

	Возврат Результат.Выгрузить();

КонецФункции
```

#### Чеклист 2.2:
- [ ] Процедура логирования создана
- [ ] Добавлено логирование во все функции запросов
- [ ] Порог производительности настроен (3 сек)
- [ ] Тестирование логирования выполнено

### 2.3 Юнит-тесты для серверной логики

**Время**: 2 часа

#### 2.3.1 Создать тестовую обработку

**Создать**: `Обработки.гкс_ТестыАРМПромежуточныйКомпозит`

#### 2.3.2 Тест-кейсы

**Код тестов**:
```bsl
Процедура ТестПолученияДанныхПроизвольныйКомпозит() Экспорт

	// Подготовка
	ПараметрыЗапроса = ПодготовитьТестовыеПараметры();
	АРМ = Обработки.гкс_АРМПромежуточныйКомпозит.Создать();

	// Выполнение
	Результат = АРМ.ДанныеДляСписокПроизвольныйКомпозит(ПараметрыЗапроса);

	// Проверка
	Утверждения.ПроверитьИстину(
		Результат <> Неопределено,
		"Результат не должен быть неопределен");

	Утверждения.ПроверитьИстину(
		ТипЗнч(Результат) = Тип("ТаблицаЗначений"),
		"Результат должен быть таблицей значений");

	Утверждения.ПроверитьИстину(
		Результат.Колонки.Найти("Регистрация") <> Неопределено,
		"Должна быть колонка Регистрация");

	// Проверка производительности
	ВремяНачала = ТекущаяУниверсальнаяДатаВМиллисекундах();
	Результат = АРМ.ДанныеДляСписокПроизвольныйКомпозит(ПараметрыЗапроса);
	ВремяВыполнения = ТекущаяУниверсальнаяДатаВМиллисекундах() - ВремяНачала;

	Утверждения.ПроверитьИстину(
		ВремяВыполнения < 3000,
		СтрШаблон("Время выполнения (%1 мс) должно быть < 3000 мс", ВремяВыполнения));

КонецПроцедуры

Функция ПодготовитьТестовыеПараметры()
	Параметры = Новый Структура;
	Параметры.Вставить("МассивОрганизаций", ПолучитьТестовуюОрганизацию());
	Параметры.Вставить("МассивПЛК", ПолучитьТестовыйПЛК());
	Параметры.Вставить("ДатаНачала", НачалоДня(ТекущаяДата()));
	Параметры.Вставить("ДатаОкончания", КонецДня(ТекущаяДата()));
	Параметры.Вставить("ПоказыватьВсе", Ложь);
	Возврат Параметры;
КонецФункции
```

#### Чеклист 2.3:
- [ ] Тестовая обработка создана
- [ ] Написано минимум 3 теста
- [ ] Все тесты проходят
- [ ] Покрытие кода тестами > 70%

### 2.4 Документирование серверной логики

**Время**: 2 часа

#### Задачи:
- [ ] Добавить комментарии к функциям (JSDoc стиль)
- [ ] Описать параметры
- [ ] Описать возвращаемые значения
- [ ] Добавить примеры использования

**Пример документирования**:
```bsl
// Получает данные для списка произвольного композита
//
// Параметры:
//   Параметры - Структура - параметры запроса:
//     * МассивОрганизаций - Массив из СправочникСсылка.Организации
//     * МассивПЛК - Массив из СправочникСсылка.ПЛК
//     * ДатаНачала - Дата - начало периода производственных суток
//     * ДатаОкончания - Дата - конец периода
//     * ПоказыватьВсе - Булево - показывать все регистрации (игнорировать состояние качества)
//
// Возвращаемое значение:
//   ТаблицаЗначений - данные регистраций с колонками:
//     * Регистрация - ДокументСсылка.гкс_РегистрацияНаПЛК
//     * Номенклатура - СправочникСсылка.Номенклатура
//     * ГруппаТС - СправочникСсылка.ГруппыТС
//     * Показатель - СправочникСсылка.Показатели
//     * ... (и другие)
//
// Пример:
//   Параметры = Новый Структура;
//   Параметры.Вставить("МассивОрганизаций", МассивОрганизаций);
//   ...
//   Данные = АРМ.ДанныеДляСписокПроизвольныйКомпозит(Параметры);
//
Функция ДанныеДляСписокПроизвольныйКомпозит(Параметры) Экспорт
```

#### Чеклист 2.4:
- [ ] Все экспортные функции документированы
- [ ] Описаны все параметры
- [ ] Добавлены примеры использования
- [ ] Документация соответствует стандартам

---

## Этап 3: Разработка основной формы

**Длительность**: 2.5 дня (20 часов)
**Ответственный**: Разработчик 1С

### 3.1 Создание формы обработки

**Время**: 4 часа

#### 3.1.1 Создать основную форму

**Шаги**:
```
1. Обработка → Формы → Добавить
2. Имя: Форма
3. Тип: Управляемая форма
```

#### 3.1.2 Создать реквизиты формы

**Реквизиты для отборов**:
```bsl
// Отборы
МассивОрганизаций (Массив)
МассивПЛК (Массив)
ПроизводственныеСутки (Дата)
ПоказыватьВсе (Булево)
РежимПромежуточныйКомпозит (Булево)
```

**Табличные части формы**:
```bsl
// Таблица для страницы "Произвольный композит"
ТаблицаПроизвольныйКомпозит (ТаблицаЗначений)
Колонки:
- Отметка (Булево)
- Регистрация (ДокументСсылка.гкс_РегистрацияНаПЛК)
- Номенклатура
- Контрагент
- Организация
- ПЛК
- ГруппаТС
- Показатель
- СостояниеКачества
- ЕстьКомпозитнаяПроба (Число)
- ДокументКомпозитнаяПроба
- ЕстьЕдиничнаяПроба (Число)
- ДокументЕдиничнаяПроба

// Таблица для страницы "Промежуточные пробы"
ТаблицаПромежуточныеПробы (ТаблицаЗначений)
Колонки:
- ДокументПроба (ДокументСсылка.гкс_ФормированиеНомераПробы)
- Дата
- Номер
- Организация
- ПЛК
- Номенклатура
- НомерПробы
- ЛабораторныйАнализ

// Таблица регистраций в пробе (подчиненная)
ТаблицаРегистрацииВПробе (ТаблицаЗначений)
Колонки:
- Регистрация
- НомерРегистрации
- ДатаРегистрации
- Номенклатура
- Контрагент
```

#### 3.1.3 Создать элементы интерфейса

**Структура формы**:
```
Форма
├── ГруппаОтборы
│   ├── ПолеОрганизация (выбор из списка)
│   ├── ПолеПЛК (выбор из списка)
│   ├── ПолеПроизводственныеСутки (дата)
│   ├── КнопкаМинусДень
│   ├── КнопкаПлюсДень
│   └── ФлажокПоказыватьВсе
├── Страницы (элемент управления "Страницы")
│   ├── СтраницаПроизвольныйКомпозит
│   │   ├── ТаблицаПроизвольныйКомпозит (таблица)
│   │   └── КнопкаСформироватьПробы
│   └── СтраницаПромежуточныеПробы
│       ├── ТаблицаПромежуточныеПробы (таблица)
│       └── ТаблицаРегистрацииВПробе (таблица, подчиненная)
└── КнопкаОбновить
```

#### Чеклист 3.1:
- [ ] Форма создана
- [ ] Все реквизиты добавлены
- [ ] Структура интерфейса создана
- [ ] Страницы настроены

### 3.2 Процедура ПриСозданииНаСервере()

**Время**: 2 часа

**Код**:
```bsl
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	// Установка начальных значений
	Если НЕ ЗначениеЗаполнено(ПроизводственныеСутки) Тогда
		ПроизводственныеСутки = ТекущаяДата();
	КонецЕсли;

	// Определение режима работы из параметров
	Если Параметры.Свойство("ПромежуточныйКомпозит")
		И Параметры.ПромежуточныйКомпозит = Истина Тогда

		РежимПромежуточныйКомпозит = Истина;
		Элементы.Страницы.ТекущаяСтраница = Элементы.СтраницаПромежуточныеПробы;
	Иначе
		РежимПромежуточныйКомпозит = Ложь;
		Элементы.Страницы.ТекущаяСтраница = Элементы.СтраницаПроизвольныйКомпозит;
	КонецЕсли;

	// Заполнение отборов по умолчанию
	ЗаполнитьМассивОрганизацийПоУмолчанию();
	ЗаполнитьМассивПЛКПоУмолчанию();

	// Первичная загрузка данных
	ОбновлениеОтображенияДанных();

КонецПроцедуры

&НаСервере
Процедура ЗаполнитьМассивОрганизацийПоУмолчанию()

	// Получить организацию текущего пользователя или основную
	ОсновнаяОрганизация = ПараметрыСеанса.ОсновнаяОрганизация; // или другая логика

	Если ЗначениеЗаполнено(ОсновнаяОрганизация) Тогда
		МассивОрганизаций = Новый Массив;
		МассивОрганизаций.Добавить(ОсновнаяОрганизация);
	Иначе
		// Получить все доступные организации
		МассивОрганизаций = ПолучитьВсеОрганизации();
	КонецЕсли;

КонецПроцедуры

&НаСервере
Процедура ЗаполнитьМассивПЛКПоУмолчанию()

	// Получить все ПЛК или текущий ПЛК пользователя
	МассивПЛК = ПолучитьВсеПЛК();

КонецПроцедуры
```

#### Чеклист 3.2:
- [ ] Процедура создана
- [ ] Инициализация параметров работает
- [ ] Отборы заполняются по умолчанию
- [ ] Режим работы определяется правильно

### 3.3 Процедура ОбновлениеОтображенияДанных()

**Время**: 3 часа

**Код**:
```bsl
&НаСервере
Процедура ОбновлениеОтображенияДанных()

	// Формирование параметров запроса
	ПараметрыЗапроса = Новый Структура;
	ПараметрыЗапроса.Вставить("МассивОрганизаций", МассивОрганизаций);
	ПараметрыЗапроса.Вставить("МассивПЛК", МассивПЛК);
	ПараметрыЗапроса.Вставить("ДатаНачала", НачалоДня(ПроизводственныеСутки));
	ПараметрыЗапроса.Вставить("ДатаОкончания", КонецДня(ПроизводственныеСутки));
	ПараметрыЗапроса.Вставить("ПоказыватьВсе", ПоказыватьВсе);

	Если РежимПромежуточныйКомпозит Тогда
		// Обновление страницы промежуточных проб
		ТаблицаПромежуточныеПробы.Загрузить(
			РеквизитФормыВЗначение("Объект")
				.ДанныеДляСписокПромежуточныеПробы(ПараметрыЗапроса)
		);
	Иначе
		// Обновление страницы произвольного композита
		ТаблицаПроизвольныйКомпозит.Загрузить(
			РеквизитФормыВЗначение("Объект")
				.ДанныеДляСписокПроизвольныйКомпозит(ПараметрыЗапроса)
		);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура КнопкаОбновитьНажатие(Элемент)
	ОбновлениеОтображенияДанных();
КонецПроцедуры

&НаКлиенте
Процедура ПоказыватьВсеПриИзменении(Элемент)
	ОбновлениеОтображенияДанных();
КонецПроцедуры
```

#### Чеклист 3.3:
- [ ] Процедура обновления создана
- [ ] Параметры формируются корректно
- [ ] Данные загружаются в таблицы
- [ ] Кнопка обновления работает

### 3.4 Интерактивное выделение строк

**Время**: 4 часа

#### 3.4.1 Обработчик изменения флажка

**Код**:
```bsl
&НаКлиенте
Процедура ТаблицаПроизвольныйКомпозитПриИзмененииФлажка(Элемент)

	ТекущиеДанные = Элементы.ТаблицаПроизвольныйКомпозит.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	// Получаем ключевые значения текущей строки
	ТекущаяГруппаТС = ТекущиеДанные.ГруппаТС;
	ТекущийПоказатель = ТекущиеДанные.Показатель;
	ТекущаяОтметка = ТекущиеДанные.Отметка;

	// Проходим по всем строкам таблицы
	Для Каждого СтрокаТаблицы Из ТаблицаПроизвольныйКомпозит Цикл

		// Если совпадают ГруппаТС и Показатель
		Если СтрокаТаблицы.ГруппаТС = ТекущаяГруппаТС
			И СтрокаТаблицы.Показатель = ТекущийПоказатель Тогда

			// Устанавливаем такую же отметку
			СтрокаТаблицы.Отметка = ТекущаяОтметка;

		КонецЕсли;

	КонецЦикла;

КонецПроцедуры
```

#### Чеклист 3.4.1:
- [ ] Обработчик создан
- [ ] Массовое выделение работает
- [ ] Массовое снятие выделения работает
- [ ] Тестирование на 50+ строках

### 3.5 Валидация выбранных строк

**Время**: 3 часа

#### 3.5.1 Функция проверки наличия выделенных строк

**Код**:
```bsl
&НаКлиенте
Функция ПроверитьНаличиеВыделенныхСтрок()

	КоличествоОтмеченных = 0;
	Для Каждого Строка Из ТаблицаПроизвольныйКомпозит Цикл
		Если Строка.Отметка = Истина Тогда
			КоличествоОтмеченных = КоличествоОтмеченных + 1;
		КонецЕсли;
	КонецЦикла;

	Если КоличествоОтмеченных = 0 Тогда
		ПоказатьПредупреждение(,
			НСтр("ru = 'Не выбрано ни одной строки для формирования композитной пробы!'"));
		Возврат Ложь;
	КонецЕсли;

	Возврат Истина;

КонецФункции
```

#### 3.5.2 Функция проверки совпадения данных

**Код**:
```bsl
&НаКлиенте
Функция ПроверитьСовпадениеДанныхСПервойСтрокой(ТекущиеДанные, ПерваяОтмеченнаяСтрока)

	// Проверка номенклатуры (строгая)
	Если ТекущиеДанные.Номенклатура <> ПерваяОтмеченнаяСтрока.Номенклатура Тогда
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'Выбранная строка имеет отличающуюся номенклатуру от первой отмеченной строки:%1- Номенклатура: %2 <> %3'"),
			Символы.ПС,
			Строка(ТекущиеДанные.Номенклатура),
			Строка(ПерваяОтмеченнаяСтрока.Номенклатура));
		ПоказатьПредупреждение(, ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;

	// Проверка контрагента (с запросом подтверждения)
	Если ТекущиеДанные.Контрагент <> ПерваяОтмеченнаяСтрока.Контрагент Тогда
		ТекстВопроса = СтрШаблон(
			НСтр("ru = 'Выбранная строка имеет отличающегося контрагента:%1%2 <> %3%1%1Продолжить?'"),
			Символы.ПС,
			Строка(ТекущиеДанные.Контрагент),
			Строка(ПерваяОтмеченнаяСтрока.Контрагент));

		Ответ = Вопрос(ТекстВопроса, РежимДиалогаВопрос.ДаНет);
		Если Ответ = КодВозвратаДиалога.Нет Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;

	// Проверка организации (с запросом подтверждения)
	Если ТекущиеДанные.Организация <> ПерваяОтмеченнаяСтрока.Организация Тогда
		ТекстВопроса = СтрШаблон(
			НСтр("ru = 'Выбранная строка имеет отличающуюся организацию:%1%2 <> %3%1%1Продолжить?'"),
			Символы.ПС,
			Строка(ТекущиеДанные.Организация),
			Строка(ПерваяОтмеченнаяСтрока.Организация));

		Ответ = Вопрос(ТекстВопроса, РежимДиалогаВопрос.ДаНет);
		Если Ответ = КодВозвратаДиалога.Нет Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;

	Возврат Истина;

КонецФункции
```

#### Чеклист 3.5:
- [ ] Функции валидации созданы
- [ ] Проверка номенклатуры работает (строгая)
- [ ] Проверка контрагента работает (с подтверждением)
- [ ] Проверка организации работает (с подтверждением)
- [ ] Тестирование всех сценариев

### 3.6 Навигация по производственным суткам

**Время**: 2 часа

**Код**:
```bsl
&НаКлиенте
Процедура КнопкаДобавитьДеньНажатие(Элемент)
	ПроизводственныеСутки = ПроизводственныеСутки + 86400; // +1 день
	ОбновлениеОтображенияДанных();
КонецПроцедуры

&НаКлиенте
Процедура КнопкаВычестьДеньНажатие(Элемент)
	ПроизводственныеСутки = ПроизводственныеСутки - 86400; // -1 день
	ОбновлениеОтображенияДанных();
КонецПроцедуры

&НаКлиенте
Процедура ПроизводственныеСуткиПриИзменении(Элемент)
	ОбновлениеОтображенияДанных();
КонецПроцедуры
```

#### Чеклист 3.6:
- [ ] Кнопки навигации созданы
- [ ] +1 день работает
- [ ] -1 день работает
- [ ] Прямое изменение даты работает
- [ ] Автообновление данных работает

### 3.7 Обработка страницы "Промежуточные пробы"

**Время**: 2 часа

**Код**:
```bsl
&НаКлиенте
Процедура ТаблицаПромежуточныеПробыПриАктивизацииСтроки(Элемент)

	ТекущиеДанные = Элементы.ТаблицаПромежуточныеПробы.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		ТаблицаРегистрацииВПробе.Очистить();
		Возврат;
	КонецЕсли;

	// Загрузить регистрации для выбранной пробы
	ЗагрузитьРегистрацииДляПробы(ТекущиеДанные.ДокументПроба);

КонецПроцедуры

&НаСервере
Процедура ЗагрузитьРегистрацииДляПробы(ДокументПроба)

	ТаблицаРегистрацииВПробе.Загрузить(
		РеквизитФормыВЗначение("Объект")
			.ДанныеДляСписокРегистрацииПромежуточныхПроб(ДокументПроба)
	);

КонецПроцедуры

&НаКлиенте
Процедура ТаблицаПромежуточныеПробыВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)

	СтандартнаяОбработка = Ложь;

	ТекущиеДанные = Элементы.ТаблицаПромежуточныеПробы.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	// Открыть документ формирования номера пробы
	ПоказатьЗначение(, ТекущиеДанные.ДокументПроба);

КонецПроцедуры
```

#### Чеклист 3.7:
- [ ] Обработчик активизации строки создан
- [ ] Загрузка регистраций работает
- [ ] Двойной клик открывает документ
- [ ] Тестирование выполнено

---

## Этап 4: Форма формирования композитной пробы

**Длительность**: 1.5 дня (12 часов)
**Ответственный**: Разработчик 1С

### 4.1 Создание формы

**Время**: 2 часа

#### 4.1.1 Создать форму

**Шаги**:
```
1. Обработка → Формы → Добавить
2. Имя: ФормаФормированиеКомпозитнойФормы
3. Тип: Управляемая форма
```

#### 4.1.2 Реквизиты формы

```bsl
// Реквизиты шапки
Организация (СправочникСсылка.Организации)
ПЛК (СправочникСсылка.ПЛК)
Номенклатура (СправочникСсылка.Номенклатура)
Контрагент (СправочникСсылка.Контрагенты)

// Табличные части
Регистрации (ТаблицаЗначений)
  - Регистрация (ДокументСсылка.гкс_РегистрацияНаПЛК)

Показатели (ТаблицаЗначений)
  - Показатель (СправочникСсылка.Показатели)
```

#### 4.1.3 Элементы интерфейса

```
Форма
├── ГруппаШапка
│   ├── ПолеОрганизация
│   ├── ПолеПЛК
│   ├── ПолеНоменклатура
│   └── ПолеКонтрагент
├── ГруппаРегистрации
│   └── ТаблицаРегистрации (только просмотр)
├── ГруппаПоказатели
│   ├── ТаблицаПоказатели
│   ├── КнопкаЗагрузитьИзНормы
│   ├── КнопкаДобавитьПоказатель
│   └── КнопкаУдалитьПоказатель
└── КоманднаяПанель
    ├── КнопкаСформироватьДокумент
    └── КнопкаОтмена
```

#### Чеклист 4.1:
- [ ] Форма создана
- [ ] Реквизиты добавлены
- [ ] Интерфейс создан
- [ ] Элементы настроены

### 4.2 ПриСозданииНаСервере()

**Время**: 2 часа

**Код**:
```bsl
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	// Получение параметров из вызывающей формы
	Если Параметры.Свойство("Организация") Тогда
		Объект.Организация = Параметры.Организация;
	КонецЕсли;

	Если Параметры.Свойство("ПЛК") Тогда
		Объект.ПЛК = Параметры.ПЛК;
	КонецЕсли;

	Если Параметры.Свойство("Номенклатура") Тогда
		Объект.Номенклатура = Параметры.Номенклатура;
	КонецЕсли;

	Если Параметры.Свойство("Контрагент") Тогда
		Объект.Контрагент = Параметры.Контрагент;
	КонецЕсли;

	// Заполнение регистраций
	Если Параметры.Свойство("МассивРегистраций") Тогда
		Для Каждого Регистрация Из Параметры.МассивРегистраций Цикл
			НоваяСтрока = Объект.Регистрации.Добавить();
			НоваяСтрока.Регистрация = Регистрация;
		КонецЦикла;
	КонецЕсли;

	// Заполнение показателей (если переданы)
	Если Параметры.Свойство("МассивПоказателей") Тогда
		Для Каждого Показатель Из Параметры.МассивПоказателей Цикл
			НоваяСтрока = Объект.Показатели.Добавить();
			НоваяСтрока.Показатель = Показатель;
		КонецЦикла;
	Иначе
		// Автоматическая загрузка из нормативной аттестации
		ЗагрузитьПоказателиИзНормативнойАттестации();
	КонецЕсли;

	// Настройка доступности элементов
	Элементы.ТаблицаРегистрации.ТолькоПросмотр = Истина;

КонецПроцедуры
```

#### Чеклист 4.2:
- [ ] Процедура создана
- [ ] Параметры обрабатываются корректно
- [ ] Регистрации заполняются
- [ ] Показатели загружаются автоматически

### 4.3 Загрузка показателей из нормы

**Время**: 3 часа

**Код**:
```bsl
&НаСервере
Процедура ЗагрузитьПоказателиИзНормативнойАттестации()

	// Очистка текущих показателей
	Объект.Показатели.Очистить();

	// Проверка заполнения номенклатуры
	Если НЕ ЗначениеЗаполнено(Объект.Номенклатура) Тогда
		Сообщить(НСтр("ru = 'Не указана номенклатура для загрузки показателей'"));
		Возврат;
	КонецЕсли;

	// Получение показателей из нормативной аттестации
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	НормативнаяАттестация.Показатель КАК Показатель,
	|	Показатели.Наименование КАК НаименованиеПоказателя
	|ИЗ
	|	РегистрСведений.гкс_НормативнаяАттестацияНоменклатуры КАК НормативнаяАттестация
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Показатели КАК Показатели
	|			ПО НормативнаяАттестация.Показатель = Показатели.Ссылка
	|ГДЕ
	|	НормативнаяАттестация.Номенклатура = &Номенклатура
	|	И НормативнаяАттестация.НазначениеИспользования =
	|		ЗНАЧЕНИЕ(Перечисление.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит)
	|УПОРЯДОЧИТЬ ПО
	|	НаименованиеПоказателя";

	Запрос.УстановитьПараметр("Номенклатура", Объект.Номенклатура);

	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать();

	// Заполнение таблицы показателей
	Пока Выборка.Следующий() Цикл
		НоваяСтрока = Объект.Показатели.Добавить();
		НоваяСтрока.Показатель = Выборка.Показатель;
	КонецЦикла;

	// Сообщение о результате
	ТекстСообщения = СтрШаблон(
		НСтр("ru = 'Загружено показателей из нормативной аттестации: %1'"),
		Объект.Показатели.Количество());
	Сообщить(ТекстСообщения);

КонецПроцедуры

&НаКлиенте
Процедура КнопкаЗагрузитьИзНормыНажатие(Элемент)
	ЗагрузитьПоказателиИзНормативнойАттестации();
КонецПроцедуры
```

#### Чеклист 4.3:
- [ ] Процедура загрузки создана
- [ ] Запрос оптимизирован (с JOIN)
- [ ] Сортировка по наименованию
- [ ] Кнопка загрузки работает
- [ ] Тестирование выполнено

### 4.4 Добавление/удаление показателей

**Время**: 2 часа

**Код**:
```bsl
&НаКлиенте
Процедура КнопкаДобавитьПоказательНажатие(Элемент)

	// Открыть форму выбора показателя
	ФормаВыбора = ПолучитьФорму("Справочник.Показатели.ФормаВыбора");
	ФормаВыбора.Открыть();

	// Обработчик выбора
	ОповещениеПослеВыбора = Новый ОписаниеОповещения(
		"ПослеВыбораПоказателя",
		ЭтотОбъект);
	ФормаВыбора.ПриВыборе = ОповещениеПослеВыбора;

КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораПоказателя(ВыбранноеЗначение, ДополнительныеПараметры) Экспорт

	Если ВыбранноеЗначение = Неопределено Тогда
		Возврат;
	КонецЕсли;

	// Проверка наличия показателя в таблице
	Для Каждого Строка Из Объект.Показатели Цикл
		Если Строка.Показатель = ВыбранноеЗначение Тогда
			ПоказатьПредупреждение(,
				НСтр("ru = 'Данный показатель уже добавлен в список'"));
			Возврат;
		КонецЕсли;
	КонецЦикла;

	// Проверка наличия показателя в нормативной аттестации
	Если НЕ ПроверитьНаличиеПоказателяВНорме(ВыбранноеЗначение) Тогда
		ТекстВопроса = НСтр("ru = 'Выбранный показатель отсутствует в нормативной аттестации для данной номенклатуры. Добавить все равно?'");
		Ответ = Вопрос(ТекстВопроса, РежимДиалогаВопрос.ДаНет);
		Если Ответ = КодВозвратаДиалога.Нет Тогда
			Возврат;
		КонецЕсли;
	КонецЕсли;

	// Добавление показателя
	НоваяСтрока = Объект.Показатели.Добавить();
	НоваяСтрока.Показатель = ВыбранноеЗначение;

КонецПроцедуры

&НаСервере
Функция ПроверитьНаличиеПоказателяВНорме(Показатель)

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	ИСТИНА КАК Результат
	|ИЗ
	|	РегистрСведений.гкс_НормативнаяАттестацияНоменклатуры КАК НормативнаяАттестация
	|ГДЕ
	|	НормативнаяАттестация.Номенклатура = &Номенклатура
	|	И НормативнаяАттестация.Показатель = &Показатель
	|	И НормативнаяАттестация.НазначениеИспользования =
	|		ЗНАЧЕНИЕ(Перечисление.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит)";

	Запрос.УстановитьПараметр("Номенклатура", Объект.Номенклатура);
	Запрос.УстановитьПараметр("Показатель", Показатель);

	Результат = Запрос.Выполнить();

	Возврат НЕ Результат.Пустой();

КонецФункции

&НаКлиенте
Процедура КнопкаУдалитьПоказательНажатие(Элемент)

	ТекущиеДанные = Элементы.ТаблицаПоказатели.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		ПоказатьПредупреждение(,
			НСтр("ru = 'Не выбран показатель для удаления'"));
		Возврат;
	КонецЕсли;

	// Подтверждение удаления
	ТекстВопроса = СтрШаблон(
		НСтр("ru = 'Удалить показатель ""%1"" из списка?'"),
		Строка(ТекущиеДанные.Показатель));

	Ответ = Вопрос(ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	Если Ответ = КодВозвратаДиалога.Да Тогда
		Объект.Показатели.Удалить(ТекущиеДанные);
	КонецЕсли;

КонецПроцедуры
```

#### Чеклист 4.4:
- [ ] Добавление показателя работает
- [ ] Валидация дубликатов работает
- [ ] Проверка в нормативной аттестации работает
- [ ] Удаление показателя работает
- [ ] Тестирование выполнено

### 4.5 Формирование документа

**Время**: 3 часа

**Код**:
```bsl
&НаКлиенте
Процедура КнопкаСформироватьДокументНажатие(Элемент)

	// Валидация данных
	Если НЕ ПроверитьЗаполнениеДанных() Тогда
		Возврат;
	КонецЕсли;

	// Формирование документа на сервере
	СформироватьДокументНаСервере();

	// Закрытие формы
	Закрыть();

КонецПроцедуры

&НаКлиенте
Функция ПроверитьЗаполнениеДанных()

	// Проверка показателей
	Если Объект.Показатели.Количество() = 0 Тогда
		ПоказатьПредупреждение(,
			НСтр("ru = 'Не указаны показатели для анализа!'"));
		Возврат Ложь;
	КонецЕсли;

	// Проверка регистраций
	Если Объект.Регистрации.Количество() = 0 Тогда
		ПоказатьПредупреждение(,
			НСтр("ru = 'Не указаны регистрации для формирования пробы!'"));
		Возврат Ложь;
	КонецЕсли;

	// Проверка обязательных реквизитов
	Если НЕ ЗначениеЗаполнено(Объект.Организация) Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Не указана организация!'"));
		Возврат Ложь;
	КонецЕсли;

	Если НЕ ЗначениеЗаполнено(Объект.ПЛК) Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Не указан ПЛК!'"));
		Возврат Ложь;
	КонецЕсли;

	Если НЕ ЗначениеЗаполнено(Объект.Номенклатура) Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Не указана номенклатура!'"));
		Возврат Ложь;
	КонецЕсли;

	Возврат Истина;

КонецФункции

&НаСервере
Процедура СформироватьДокументНаСервере()

	// Формирование массива показателей
	МассивПоказателей = Новый Массив;
	Для Каждого СтрокаПоказатель Из Объект.Показатели Цикл
		Если ЗначениеЗаполнено(СтрокаПоказатель.Показатель) Тогда
			МассивПоказателей.Добавить(СтрокаПоказатель.Показатель);
		КонецЕсли;
	КонецЦикла;

	// Формирование массива регистраций
	МассивРегистраций = Новый Массив;
	Для Каждого СтрокаРегистрация Из Объект.Регистрации Цикл
		Если ЗначениеЗаполнено(СтрокаРегистрация.Регистрация) Тогда
			МассивРегистраций.Добавить(СтрокаРегистрация.Регистрация);
		КонецЕсли;
	КонецЦикла;

	// Параметры для создания документа
	ПараметрыСоздания = Новый Структура;
	ПараметрыСоздания.Вставить("Организация", Объект.Организация);
	ПараметрыСоздания.Вставить("ПЛК", Объект.ПЛК);
	ПараметрыСоздания.Вставить("Номенклатура", Объект.Номенклатура);
	ПараметрыСоздания.Вставить("Контрагент", Объект.Контрагент);
	ПараметрыСоздания.Вставить("МассивРегистраций", МассивРегистраций);
	ПараметрыСоздания.Вставить("ТипПробы",
		Перечисления.гкс_ТипыПроб.Композитная);

	// Параметры для лабораторного анализа
	ПараметрыСозданияАнализа = Новый Структура;
	ПараметрыСозданияАнализа.Вставить("МассивПоказателей", МассивПоказателей);
	ПараметрыСозданияАнализа.Вставить("НазначениеИспользованияКачества",
		Перечисления.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит);

	// Создание документа
	НовыйДокумент = Документы.гкс_ФормированиеНомераПробы.ДокументПромежуточногоКомпозита(
		ПараметрыСоздания,
		ПараметрыСозданияАнализа);

	// Открытие созданного документа
	Если ЗначениеЗаполнено(НовыйДокумент) Тогда
		ПоказатьЗначение(, НовыйДокумент);
	КонецЕсли;

КонецПроцедуры
```

#### Чеклист 4.5:
- [ ] Валидация данных работает
- [ ] Формирование массивов работает
- [ ] Вызов метода создания документа работает
- [ ] Открытие созданного документа работает
- [ ] Тестирование выполнено

---

**(Продолжение следует...)**

**Конец файла `04_IMPLEMENTATION_PLAN.md` (Часть 1 из 2)**

Этот план содержит первые 4 этапа из 8. Продолжить со следующими этапами:
- Этап 5: Интеграция с документами
- Этап 6: Тестирование
- Этап 7: Оптимизация
- Этап 8: Документация и внедрение

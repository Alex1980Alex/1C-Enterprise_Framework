# ИТОГОВЫЙ ДЕТАЛЬНЫЙ ОТЧЕТ ПО АНАЛИЗУ ОБРАБОТКИ

**Обработка**: `гкс_АРМПромежуточныйКомпозит`
**Проект**: 251029_GKSTCPLK-1831
**Дата анализа**: 2025-10-29
**Назначение**: Автоматизированное рабочее место для работы с промежуточными композитными пробами в рамках усиленного контроля качества по партиям транспортных средств

---

## 1. ОБЩАЯ ИНФОРМАЦИЯ

### Структура обработки

- 📁 **2 формы**:
  - `Форма` (основная форма с 3 страницами)
  - `ФормаФормированиеКомпозитнойФормы` (создание композита)
- 📄 **4 модуля** (1289 строк кода):
  - `ObjectModule.bsl` (312 строк)
  - `Forms/Форма/Ext/Form/Module.bsl` (600 строк)
  - `Forms/ФормаФормированиеКомпозитнойФормы/Ext/Form/Module.bsl` (362 строк)
  - `Commands/ПромежуточныйКомпозит/Ext/CommandModule.bsl` (15 строк)
- ⚙️ **1 команда**: `ПромежуточныйКомпозит`
- 🔍 **3 сложных запроса** с временными таблицами

### Назначение и функциональность

Обработка предназначена для:
1. Просмотра регистраций ПЛК, требующих создания промежуточных композитных проб
2. Формирования композитных проб из нескольких регистраций
3. Автоматического создания документов "ФормированиеНомераПробы" и "ЛабораторныйАнализ"
4. Управления состояниями качества усиленного контроля
5. Просмотра уже созданных промежуточных проб

---

## 2. ДЕТАЛЬНЫЙ АНАЛИЗ МОДУЛЕЙ

### 2.1 Модуль объекта (ObjectModule.bsl) - 312 строк

Модуль содержит серверную логику для получения данных.

#### Экспортные функции

##### Функция 1: `ДанныеДляСписокПроизвольныйКомпозит(Параметры)`
**Расположение**: строки 1-65
**Назначение**: Получение данных для списка регистраций ПЛК, доступных для создания композитных проб

**Параметры**:
- `СписокСостояний` - массив состояний качества УК для отбора
- `ТочкаМаршрута` - точка маршрута (справочник)
- `ВидПеревозки` - вид перевозки (справочник)
- `Организация` - организация (справочник)
- `Контрагент` - контрагент (справочник)
- `Номенклатура` - номенклатура (справочник)
- `ПроизводственныеСуткиНачало` - начало периода
- `ПроизводственныеСуткиКонец` - окончание периода
- `ПоказатьВсе` - флаг показа всех регистраций

**Запрос**: Использует `ТекстЗапросаДанныеДляДетальныхЗаписей()` (4 временные таблицы)

**Возвращаемое значение**: Таблица значений с колонками:
- `Регистрация` - ссылка на документ РегистрацияНаПЛК
- `Номенклатура`, `Организация`, `Контрагент`
- `СостояниеКачестваУК` - текущее состояние качества
- `ГруппаТС` - группа транспортных средств
- `Показатель` - показатель качества
- `ЕстьКомпозитнаяПроба` - флаг наличия композита
- Другие реквизиты регистрации

**Код**:
```bsl
Функция ДанныеДляСписокПроизвольныйКомпозит(Параметры) Экспорт

	СписокСостояний = Новый Массив;

	Если НЕ Параметры.ПоказатьВсе Тогда
		СписокСостояний.Добавить(Перечисления.гкс_СостоянияКачестваУсиленногоКонтроля.ПустаяСсылка());
		СписокСостояний.Добавить(Перечисления.гкс_СостоянияКачестваУсиленногоКонтроля.ОтборПроб);
	КонецЕсли;

	Запрос	= Новый Запрос;
	Запрос.Текст = ТекстЗапросаДанныеДляДетальныхЗаписей();

	Запрос.УстановитьПараметр("СписокСостояний", СписокСостояний);
	Запрос.УстановитьПараметр("ТочкаМаршрута", Параметры.ТочкаМаршрута);
	Запрос.УстановитьПараметр("ВидПеревозки", Параметры.ВидПеревозки);
	Запрос.УстановитьПараметр("Организация", Параметры.Организация);
	Запрос.УстановитьПараметр("Контрагент", Параметры.Контрагент);
	Запрос.УстановитьПараметр("Номенклатура", Параметры.Номенклатура);
	Запрос.УстановитьПараметр("ПроизводственныеСуткиНачало", Параметры.ПроизводственныеСуткиНачало);
	Запрос.УстановитьПараметр("ПроизводственныеСуткиКонец", Параметры.ПроизводственныеСуткиКонец);
	Запрос.УстановитьПараметр("ПоказатьВсе", Параметры.ПоказатьВсе);

	Возврат Запрос.Выполнить().Выгрузить();

КонецФункции
```

##### Функция 2: `ДанныеДляСписокПромежуточныеПробы(Параметры)`
**Расположение**: строки 67-112
**Назначение**: Получение списка существующих промежуточных композитных проб

**Параметры**: Аналогичны функции 1

**Запрос**: Использует `ТекстЗапросаДанныеДляПромежуточныхПроб()` (5 временных таблиц с UNION)

**Возвращаемое значение**: Таблица значений с промежуточными пробами в двух статусах:
- Пробы в статусе "ОтборПроб"
- Уже сформированные композитные пробы

**Код**:
```bsl
Функция ДанныеДляСписокПромежуточныеПробы(Параметры) Экспорт

	СписокСостояний = Новый Массив;
	СписокСостояний.Добавить(Перечисления.гкс_СостоянияКачестваУсиленногоКонтроля.ОтборПроб);

	Запрос	= Новый Запрос;
	Запрос.Текст = ТекстЗапросаДанныеДляПромежуточныхПроб();

	Запрос.УстановитьПараметр("СписокСостояний", СписокСостояний);
	Запрос.УстановитьПараметр("ТочкаМаршрута", Параметры.ТочкаМаршрута);
	Запрос.УстановитьПараметр("ВидПеревозки", Параметры.ВидПеревозки);
	Запрос.УстановитьПараметр("Организация", Параметры.Организация);
	Запрос.УстановитьПараметр("Контрагент", Параметры.Контрагент);
	Запрос.УстановитьПараметр("Номенклатура", Параметры.Номенклатура);
	Запрос.УстановитьПараметр("ПроизводственныеСуткиНачало", Параметры.ПроизводственныеСуткиНачало);
	Запрос.УстановитьПараметр("ПроизводственныеСуткиКонец", Параметры.ПроизводственныеСуткиКонец);

	Возврат Запрос.Выполнить().Выгрузить();

КонецФункции
```

##### Функция 3: `ДанныеДляСписокРегистраций(Параметры)`
**Расположение**: строки 114-156
**Назначение**: Простой запрос для получения списка регистраций с последним состоянием

**Код**:
```bsl
Функция ДанныеДляСписокРегистраций(Параметры) Экспорт

	Запрос	= Новый Запрос;
	Запрос.Текст = ТекстЗапросаДанныеДляСписокРегистраций();

	Запрос.УстановитьПараметр("ТочкаМаршрута", Параметры.ТочкаМаршрута);
	Запрос.УстановитьПараметр("ВидПеревозки", Параметры.ВидПеревозки);
	Запрос.УстановитьПараметр("Организация", Параметры.Организация);
	Запрос.УстановитьПараметр("Контрагент", Параметры.Контрагент);
	Запрос.УстановитьПараметр("Номенклатура", Параметры.Номенклатура);
	Запрос.УстановитьПараметр("ПроизводственныеСуткиНачало", Параметры.ПроизводственныеСуткиНачало);
	Запрос.УстановитьПараметр("ПроизводственныеСуткиКонец", Параметры.ПроизводственныеСуткиКонец);

	Возврат Запрос.Выполнить().Выгрузить();

КонецФункции
```

#### Внутренние функции запросов

##### Функция 4: `ТекстЗапросаДанныеДляДетальныхЗаписей()`
**Расположение**: строки 158-245
**Назначение**: Генерация текста запроса с 4 временными таблицами

**Структура запроса**:

1. **Временная таблица ВТ_Регистрации**:
```sql
ВЫБРАТЬ
    СостоянияРегистрации.Регистрация КАК Регистрация,
    СостоянияРегистрации.СостояниеКачестваУК КАК СостояниеКачестваУК
ПОМЕСТИТЬ ВТ_Регистрации
ИЗ
    РегистрСведений.гкс_СостоянияРегистрации.СрезПоследних КАК СостоянияРегистрации
ГДЕ
    (СостоянияРегистрации.СостояниеКачестваУК В (&СписокСостояний)
    ИЛИ &ПоказатьВсе)
```
**Назначение**: Получение последних состояний качества для регистраций

2. **Временная таблица ВТ_КомпозитныеПробы**:
```sql
ВЫБРАТЬ
    КомпозитныеПробы.Регистрация КАК Регистрация,
    КомпозитныеПробы.ФормированиеНомераПробы КАК ФормированиеНомераПробы,
    КомпозитныеПробы.Показатель КАК Показатель
ПОМЕСТИТЬ ВТ_КомпозитныеПробы
ИЗ
    РегистрСведений.гкс_КомпозитныеПробы КАК КомпозитныеПробы
ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Регистрации КАК ВТ_Регистрации
    ПО КомпозитныеПробы.Регистрация = ВТ_Регистрации.Регистрация
```
**Назначение**: Связь регистраций с композитными пробами

3. **Временная таблица ВТ_ЕдиничныеПробы**:
```sql
ВЫБРАТЬ
    ФормированиеНомераПробы.Ссылка КАК ФормированиеНомераПробы,
    ФормированиеНомераПробы.Регистрация КАК Регистрация
ПОМЕСТИТЬ ВТ_ЕдиничныеПробы
ИЗ
    Документ.гкс_ФормированиеНомераПробы КАК ФормированиеНомераПробы
ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Регистрации КАК ВТ_Регистрации
    ПО ФормированиеНомераПробы.Регистрация = ВТ_Регистрации.Регистрация
ГДЕ
    ФормированиеНомераПробы.ВидПробы = ЗНАЧЕНИЕ(Перечисление.гкс_ВидыПроб.ЕдиничнаяПроба)
    И НЕ ФормированиеНомераПробы.ПометкаУдаления
```
**Назначение**: Получение единичных проб для регистраций

4. **Основной SELECT**:
```sql
ВЫБРАТЬ
    РегистрацияНаПЛК.Ссылка КАК Регистрация,
    РегистрацияНаПЛК.Номенклатура КАК Номенклатура,
    РегистрацияНаПЛК.Характеристика КАК Характеристика,
    РегистрацияНаПЛК.Организация КАК Организация,
    РегистрацияНаПЛК.Контрагент КАК Контрагент,
    РегистрацияНаПЛК.ТочкаМаршрута КАК ТочкаМаршрута,
    РегистрацияНаПЛК.ВидПеревозки КАК ВидПеревозки,
    ВТ_Регистрации.СостояниеКачестваУК КАК СостояниеКачестваУК,
    ПоказателиКачестваУК.Показатель КАК Показатель,
    СоставГруппТранспорта.ГруппаТС КАК ГруппаТС,
    ВЫБОР
        КОГДА ВТ_КомпозитныеПробы.ФормированиеНомераПробы ЕСТЬ NULL
            ТОГДА ЛОЖЬ
        ИНАЧЕ ИСТИНА
    КОНЕЦ КАК ЕстьКомпозитнаяПроба,
    ВТ_ЕдиничныеПробы.ФормированиеНомераПробы КАК ДокументЕдиничнаяПроба
ИЗ
    Документ.гкс_РегистрацияНаПЛК КАК РегистрацияНаПЛК
    ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Регистрации КАК ВТ_Регистрации
        ПО РегистрацияНаПЛК.Ссылка = ВТ_Регистрации.Регистрация
    ЛЕВОЕ СОЕДИНЕНИЕ ВТ_КомпозитныеПробы КАК ВТ_КомпозитныеПробы
        ПО ВТ_Регистрации.Регистрация = ВТ_КомпозитныеПробы.Регистрация
    ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЕдиничныеПробы КАК ВТ_ЕдиничныеПробы
        ПО ВТ_Регистрации.Регистрация = ВТ_ЕдиничныеПробы.Регистрация
    ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.гкс_ПоказателиКачестваУсиленногоКонтроля КАК ПоказателиКачестваУК
        ПО (ПоказателиКачестваУК.НазначениеИспользования =
            ЗНАЧЕНИЕ(Перечисление.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит))
    ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.гкс_СоставГруппТранспорта КАК СоставГруппТранспорта
        ПО РегистрацияНаПЛК.Ссылка = СоставГруппТранспорта.Регистрация
ГДЕ
    РегистрацияНаПЛК.Проведен
    И НЕ РегистрацияНаПЛК.ПометкаУдаления
    И (РегистрацияНаПЛК.ТочкаМаршрута = &ТочкаМаршрута ИЛИ &ТочкаМаршрута = ЗНАЧЕНИЕ(Справочник.гкс_ТочкиМаршрутов.ПустаяСсылка))
    И (РегистрацияНаПЛК.ВидПеревозки = &ВидПеревозки ИЛИ &ВидПеревозки = ЗНАЧЕНИЕ(Справочник.гкс_ВидыПеревозки.ПустаяСсылка))
    -- И другие условия фильтрации
```

##### Функция 5: `ТекстЗапросаДанныеДляПромежуточныхПроб()`
**Расположение**: строки 247-295
**Назначение**: Запрос с UNION для получения промежуточных проб в разных статусах

**Структура**: 5 временных таблиц + UNION для объединения данных

##### Функция 6: `ТекстЗапросаДанныеДляСписокРегистраций()`
**Расположение**: строки 297-312
**Назначение**: Простой запрос со срезом последних состояний

---

### 2.2 Модуль основной формы (Forms/Форма/Ext/Form/Module.bsl) - 600 строк

Модуль реализует клиент-серверную логику пользовательского интерфейса.

#### Структура формы

Форма содержит **3 страницы**:

1. **СтраницаПроизвольныйКомпозит** - работа с регистрациями для создания композита
2. **СтраницаУправлениеРегистрациями** - просмотр и управление регистрациями
3. **СтраницаУсиленныйКонтроль** - просмотр существующих промежуточных проб

#### Ключевые серверные процедуры

##### Процедура: `ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)`
**Расположение**: строки 1-45
**Назначение**: Инициализация формы при открытии

**Логика**:
```bsl
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	// Проверка параметра открытия формы
	Если Параметры.Свойство("ПромежуточныйКомпозит") Тогда
		ПромежуточныйКомпозит = Параметры.ПромежуточныйКомпозит;
	КонецЕсли;

	// Установка видимости страниц
	Если ПромежуточныйКомпозит Тогда
		Элементы.СтраницаПроизвольныйКомпозит.Видимость = Истина;
		Элементы.СтраницаУсиленныйКонтроль.Видимость = Истина;
		Элементы.СтраницаУправлениеРегистрациями.Видимость = Ложь;
		Элементы.Страницы.ТекущаяСтраница = Элементы.СтраницаУсиленныйКонтроль;
	Иначе
		Элементы.СтраницаПроизвольныйКомпозит.Видимость = Ложь;
		Элементы.СтраницаУсиленныйКонтроль.Видимость = Ложь;
		Элементы.СтраницаУправлениеРегистрациями.Видимость = Истина;
	КонецЕсли;

	// Инициализация значений по умолчанию
	ПроизводственныеСуткиНачало = НачалоДня(ТекущаяДатаСеанса());
	ПроизводственныеСуткиКонец = КонецДня(ТекущаяДатаСеанса());

	// Загрузка начальных данных
	ПолучитьДанные();

КонецПроцедуры
```

##### Процедура: `ПолучитьДанные()`
**Расположение**: строки 47-89
**Назначение**: Обновление данных в списках по установленным фильтрам

**Логика**:
```bsl
&НаСервере
Процедура ПолучитьДанные()

	// Подготовка параметров
	Параметры = Новый Структура;
	Параметры.Вставить("ТочкаМаршрута", ТочкаМаршрута);
	Параметры.Вставить("ВидПеревозки", ВидПеревозки);
	Параметры.Вставить("Организация", Организация);
	Параметры.Вставить("Контрагент", Контрагент);
	Параметры.Вставить("Номенклатура", Номенклатура);
	Параметры.Вставить("ПроизводственныеСуткиНачало", ПроизводственныеСуткиНачало);
	Параметры.Вставить("ПроизводственныеСуткиКонец", ПроизводственныеСуткиКонец);
	Параметры.Вставить("ПоказатьВсе", ПоказатьВсе);

	// Получение данных для произвольного композита
	Если Элементы.СтраницаПроизвольныйКомпозит.Видимость Тогда
		Данные = Объект.ДанныеДляСписокПроизвольныйКомпозит(Параметры);
		СписокПроизвольныйКомпозит.Загрузить(Данные);
	КонецЕсли;

	// Получение данных для промежуточных проб
	Если Элементы.СтраницаУсиленныйКонтроль.Видимость Тогда
		Данные = Объект.ДанныеДляСписокПромежуточныеПробы(Параметры);
		СписокПромежуточныеПробы.Загрузить(Данные);
	КонецЕсли;

	// Получение данных для управления регистрациями
	Если Элементы.СтраницаУправлениеРегистрациями.Видимость Тогда
		Данные = Объект.ДанныеДляСписокРегистраций(Параметры);
		СписокРегистраций.Загрузить(Данные);
	КонецЕсли;

КонецПроцедуры
```

#### Ключевые клиентские процедуры

##### Процедура: `КомандаОбновить(Команда)` ⭐
**Расположение**: строки 91-98
**Назначение**: Обновление данных по команде пользователя

```bsl
&НаКлиенте
Процедура КомандаОбновить(Команда)
	ПолучитьДанные();
КонецПроцедуры
```

##### Процедура: `СписокПроизвольныйКомпозитПометкаПриИзменении(Элемент)` ⭐ **КРИТИЧЕСКАЯ**
**Расположение**: строки 120-185
**Назначение**: Автоматическое выделение/снятие выделения строк с одинаковой группой ТС и показателем

**Алгоритм**:
```bsl
&НаКлиенте
Процедура СписокПроизвольныйКомпозитПометкаПриИзменении(Элемент)

	ТекущиеДанные = Элементы.СписокПроизвольныйКомпозит.ТекущиеДанные;

	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Если ТекущиеДанные.Пометка Тогда
		// ВЫДЕЛЕНИЕ СТРОК

		// Найти первую отмеченную строку (не текущую)
		ПерваяОтмеченнаяСтрока = Неопределено;
		Для Каждого СтрокаСписка Из СписокПроизвольныйКомпозит Цикл
			Если СтрокаСписка.Пометка И СтрокаСписка <> ТекущиеДанные Тогда
				ПерваяОтмеченнаяСтрока = СтрокаСписка;
				Прервать;
			КонецЕсли;
		КонецЦикла;

		// Если есть другие отмеченные строки - проверить совпадение данных
		Если ПерваяОтмеченнаяСтрока <> Неопределено Тогда
			Если НЕ ПроверитьСовпадениеДанныхСПервойСтрокой(ТекущиеДанные, ПерваяОтмеченнаяСтрока) Тогда
				ТекущиеДанные.Пометка = Ложь;
				Возврат;
			КонецЕсли;
		КонецЕсли;

		// Автоматически выделить все строки с той же группой ТС и показателем
		Для Каждого СтрокаСписка Из СписокПроизвольныйКомпозит Цикл
			Если СтрокаСписка.ГруппаТС = ТекущиеДанные.ГруппаТС
				И СтрокаСписка.Показатель = ТекущиеДанные.Показатель Тогда

				// Проверка совпадения данных
				Если ПерваяОтмеченнаяСтрока <> Неопределено Тогда
					Если НЕ ПроверитьСовпадениеДанныхСПервойСтрокой(СтрокаСписка, ПерваяОтмеченнаяСтрока) Тогда
						Продолжить;
					КонецЕсли;
				КонецЕсли;

				СтрокаСписка.Пометка = Истина;
			КонецЕсли;
		КонецЦикла;

	Иначе
		// СНЯТИЕ ВЫДЕЛЕНИЯ

		// Снять выделение со всех строк с той же группой ТС и показателем
		Для Каждого СтрокаСписка Из СписокПроизвольныйКомпозит Цикл
			Если СтрокаСписка.ГруппаТС = ТекущиеДанные.ГруппаТС
				И СтрокаСписка.Показатель = ТекущиеДанные.Показатель Тогда
				СтрокаСписка.Пометка = Ложь;
			КонецЕсли;
		КонецЦикла;

	КонецЕсли;

КонецПроцедуры
```

##### Процедура: `КомандаСформироватьПробыПроизвольныйКомпозит(Команда)` ⭐ **КРИТИЧЕСКАЯ**
**Расположение**: строки 187-245
**Назначение**: Формирование композитной пробы из выделенных регистраций

**Алгоритм**:
```bsl
&НаКлиенте
Процедура КомандаСформироватьПробыПроизвольныйКомпозит(Команда)

	// Найти все отмеченные строки
	ОтборПоПометке = Новый Структура("Пометка", Истина);
	НайденныеСтроки = СписокПроизвольныйКомпозит.НайтиСтроки(ОтборПоПометке);

	Если НайденныеСтроки.Количество() = 0 Тогда
		ПоказатьПредупреждение(, "Не выбраны регистрации для формирования композитной пробы");
		Возврат;
	КонецЕсли;

	// Проверить состояния качества регистраций
	Если НЕ ПроверитьСостояниеКачестваРегистраций(НайденныеСтроки) Тогда
		Возврат;
	КонецЕсли;

	// Получить показатель (должен быть один для всех отмеченных строк)
	Показатель = НайденныеСтроки[0].Показатель;

	// Подготовить массив строк для передачи
	МассивСтрокДанных = Новый Массив;
	Для Каждого Строка Из НайденныеСтроки Цикл
		СтруктураСтроки = Новый Структура;
		СтруктураСтроки.Вставить("Регистрация", Строка.Регистрация);
		СтруктураСтроки.Вставить("Номенклатура", Строка.Номенклатура);
		СтруктураСтроки.Вставить("Характеристика", Строка.Характеристика);
		СтруктураСтроки.Вставить("ВидТранспорта", Строка.ВидТранспорта);
		СтруктураСтроки.Вставить("Показатель", Строка.Показатель);
		МассивСтрокДанных.Добавить(СтруктураСтроки);
	КонецЦикла;

	// Проверить, есть ли уже созданный документ композитной пробы
	ДокументКомпозитнаяПроба = ПолучитьДокументКомпозитнойПробы(НайденныеСтроки);

	Если ЗначениеЗаполнено(ДокументКомпозитнаяПроба) Тогда
		// Открыть существующий документ
		ИмяФормыДокумента = "Документ.гкс_ФормированиеНомераПробы.Форма.ФормаДокумента";
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("Ключ", ДокументКомпозитнаяПроба);
		ОткрытьФорму(ИмяФормыДокумента, ПараметрыФормы, ЭтотОбъект);
	Иначе
		// Создать новый композит
		ИмяФормыДокумента = "Обработка.гкс_АРМПромежуточныйКомпозит.Форма.ФормаФормированиеКомпозитнойФормы";
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("МассивСтрокДанных", МассивСтрокДанных);
		ПараметрыФормы.Вставить("Показатель", Показатель);
		ОткрытьФорму(ИмяФормыДокумента, ПараметрыФормы, ЭтотОбъект,,,,
			Новый ОписаниеОповещения("ПослеЗакрытияФормыСозданияКомпозита", ЭтотОбъект));
	КонецЕсли;

КонецПроцедуры
```

##### Функция: `ПроверитьСовпадениеДанныхСПервойСтрокой(ТекущиеДанные, ПерваяОтмеченнаяСтрока)` ⭐
**Расположение**: строки 290-335
**Назначение**: Валидация данных перед добавлением в композит

**Уровни проверки**:
1. **СТРОГАЯ ПРОВЕРКА** (обязательное совпадение):
   - Номенклатура
   - Показатель

2. **МЯГКАЯ ПРОВЕРКА** (с подтверждением пользователя):
   - Организация
   - Контрагент

**Код**:
```bsl
&НаКлиенте
Функция ПроверитьСовпадениеДанныхСПервойСтрокой(ТекущиеДанные, ПерваяОтмеченнаяСтрока)

	// СТРОГАЯ ПРОВЕРКА: Номенклатура
	Если ТекущиеДанные.Номенклатура <> ПерваяОтмеченнаяСтрока.Номенклатура Тогда
		ТекстСообщения = "Выбранная строка имеет отличающиеся данные по номенклатуре.
		|Для формирования композитной пробы необходимо выбрать регистрации
		|с одинаковой номенклатурой.";
		ПоказатьПредупреждение(, ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;

	// СТРОГАЯ ПРОВЕРКА: Показатель
	Если ТекущиеДанные.Показатель <> ПерваяОтмеченнаяСтрока.Показатель Тогда
		ТекстСообщения = "Выбранная строка имеет отличающийся показатель качества.
		|Для формирования композитной пробы необходимо выбрать регистрации
		|с одинаковым показателем.";
		ПоказатьПредупреждение(, ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;

	// МЯГКАЯ ПРОВЕРКА: Организация и Контрагент (с подтверждением)
	Если ТекущиеДанные.Организация <> ПерваяОтмеченнаяСтрока.Организация
		ИЛИ ТекущиеДанные.Контрагент <> ПерваяОтмеченнаяСтрока.Контрагент Тогда

		ТекстВопроса = "Выбранная строка имеет отличающиеся данные по организации или контрагенту.
		|Продолжить формирование композитной пробы?";

		Ответ = Неопределено;
		ПоказатьВопрос(Новый ОписаниеОповещения("ПроверитьСовпадениеДанныхЗавершение",
			ЭтотОбъект), ТекстВопроса, РежимДиалогаВопрос.ДаНет);

		// Если пользователь ответил "Нет" - отменить
		Если Ответ = КодВозвратаДиалога.Нет Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;

	Возврат Истина;

КонецФункции
```

##### Функция: `ПроверитьСостояниеКачестваРегистраций(НайденныеСтроки)`
**Расположение**: строки 337-380
**Назначение**: Проверка возможности создания лабораторных анализов

**Логика**:
- Блокирует создание, если состояние != "Пустое" или "ОтборПроб"
- Выводит предупреждение с указанием регистраций, для которых анализы уже созданы

```bsl
&НаКлиенте
Функция ПроверитьСостояниеКачестваРегистраций(НайденныеСтроки)

	МассивНеподходящихРегистраций = Новый Массив;

	Для Каждого Строка Из НайденныеСтроки Цикл
		Если Строка.СостояниеКачестваУК <> ПредопределенноеЗначение("Перечисление.гкс_СостоянияКачестваУсиленногоКонтроля.ПустаяСсылка")
			И Строка.СостояниеКачестваУК <> ПредопределенноеЗначение("Перечисление.гкс_СостоянияКачестваУсиленногоКонтроля.ОтборПроб") Тогда
			МассивНеподходящихРегистраций.Добавить(Строка.Регистрация);
		КонецЕсли;
	КонецЦикла;

	Если МассивНеподходящихРегистраций.Количество() > 0 Тогда
		ТекстСообщения = "Для следующих регистраций анализы уже сделаны на показатели
		|и повторно не могут быть сделаны:" + Символы.ПС;

		Для Каждого Регистрация Из МассивНеподходящихРегистраций Цикл
			ТекстСообщения = ТекстСообщения + "- " + Строка(Регистрация) + Символы.ПС;
		КонецЦикла;

		ПоказатьПредупреждение(, ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;

	Возврат Истина;

КонецФункции
```

---

### 2.3 Модуль формы создания композита (ФормаФормированиеКомпозитнойФормы/Module.bsl) - 362 строки

Модуль отвечает за создание документов "ФормированиеНомераПробы" и "ЛабораторныйАнализ".

#### Ключевые процедуры

##### Процедура: `ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)`
**Расположение**: строки 1-85
**Назначение**: Инициализация формы и заполнение данных

**Логика**:
```bsl
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	// Получение переданных данных
	Если Параметры.Свойство("МассивСтрокДанных") Тогда
		МассивСтрокДанных = Параметры.МассивСтрокДанных;
	КонецЕсли;

	Если Параметры.Свойство("Показатель") Тогда
		Показатель = Параметры.Показатель;
	КонецЕсли;

	// Автоматическое заполнение показателей качества
	Если ЗначениеЗаполнено(Показатель) Тогда
		ЗаполнитьПоказателиКачества();
	КонецЕсли;

	// Заполнение списка регистраций
	Если МассивСтрокДанных.Количество() > 0 Тогда
		Для Каждого СтруктураСтроки Из МассивСтрокДанных Цикл
			ДобавитьРегистрацию(СтруктураСтроки);
		КонецЦикла;

		// Заполнение реквизитов шапки из первой регистрации
		ПерваяСтрока = МассивСтрокДанных[0];
		Объект.Номенклатура = ПерваяСтрока.Номенклатура;
		Объект.Характеристика = ПерваяСтрока.Характеристика;
	КонецЕсли;

КонецПроцедуры
```

##### Процедура: `СформироватьДокумент()` ⭐ **КРИТИЧЕСКАЯ**
**Расположение**: строки 120-195
**Назначение**: Создание документов "ФормированиеНомераПробы" и "ЛабораторныйАнализ"

**Алгоритм**:
```bsl
&НаСервере
Процедура СформироватьДокумент()

	Попытка

		// 1. Подготовка параметров для создания документа пробы
		ПараметрыСоздания = Новый Структура;
		ПараметрыСоздания.Вставить("Номенклатура", Объект.Номенклатура);
		ПараметрыСоздания.Вставить("Характеристика", Объект.Характеристика);
		ПараметрыСоздания.Вставить("ВидПробы",
			Перечисления.гкс_ВидыПроб.КомпозитнаяПроба);

		// Массив регистраций
		МассивРегистраций = Новый Массив;
		Для Каждого СтрокаРегистрации Из Объект.Регистрации Цикл
			МассивРегистраций.Добавить(СтрокаРегистрации.Регистрация);
		КонецЦикла;
		ПараметрыСоздания.Вставить("МассивРегистраций", МассивРегистраций);

		// 2. Подготовка параметров для создания лабораторного анализа
		ПараметрыСозданияАнализа = Новый Структура;
		ПараметрыСозданияАнализа.Вставить("гкс_НазначениеИспользованияКачества",
			Перечисления.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит);

		// Массив показателей
		МассивПоказателей = Новый Массив;
		Для Каждого СтрокаПоказатель Из Объект.Показатели Цикл
			МассивПоказателей.Добавить(СтрокаПоказатель.Показатель);
		КонецЦикла;
		ПараметрыСозданияАнализа.Вставить("МассивПоказателей", МассивПоказателей);

		// 3. Вызов метода создания документов
		// Этот метод создаст:
		// - Документ "ФормированиеНомераПробы"
		// - Документ "ЛабораторныйАнализ"
		// - Запишет связь в РС гкс_КомпозитныеПробы
		// - Обновит состояние в РС гкс_СостоянияРегистрации
		СозданныеДокументы = Документы.гкс_ФормированиеНомераПробы.ДокументПромежуточногоКомпозита(
			ПараметрыСоздания,
			ПараметрыСозданияАнализа);

		// 4. Вывод сообщения об успехе
		ТекстСообщения = СтрШаблон("Успешно создан документ формирования номера пробы: %1",
			СозданныеДокументы.ДокументПробы);
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);

		// 5. Закрытие формы
		Закрыть();

	Исключение

		// Обработка ошибки
		ТекстОшибки = ОписаниеОшибки();
		ТекстСообщения = "Ошибка при создании документов композитной пробы:" + Символы.ПС + ТекстОшибки;
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);

	КонецПопытки;

КонецПроцедуры
```

##### Процедура: `ЗаполнитьПоказателиКачества()`
**Расположение**: строки 197-245
**Назначение**: Автоматическое заполнение списка показателей качества

**Логика**:
```bsl
&НаСервере
Процедура ЗаполнитьПоказателиКачества()

	// Получить показатели качества для промежуточного композита
	ТаблицаПоказателей = ПолучитьПоказателиКачестваДляКомпозита();

	// Очистить текущие показатели
	Объект.Показатели.Очистить();

	// Заполнить из полученной таблицы
	Для Каждого СтрокаПоказатель Из ТаблицаПоказателей Цикл
		НоваяСтрока = Объект.Показатели.Добавить();
		НоваяСтрока.Показатель = СтрокаПоказатель.Показатель;
		НоваяСтрока.ПоказательНаименование = СтрокаПоказатель.ПоказательНаименование;
	КонецЦикла;

КонецПроцедуры
```

##### Функция: `ПолучитьПоказателиКачестваДляКомпозита()`
**Расположение**: строки 247-295
**Назначение**: Получение показателей качества из РС

**Запрос**:
```bsl
&НаСервере
Функция ПолучитьПоказателиКачестваДляКомпозита()

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ПоказателиКачестваУК.Показатель КАК Показатель,
	|	ПоказателиКачестваУК.Показатель.Наименование КАК ПоказательНаименование
	|ИЗ
	|	РегистрСведений.гкс_ПоказателиКачестваУсиленногоКонтроля КАК ПоказателиКачестваУК
	|ГДЕ
	|	ПоказателиКачестваУК.НазначениеИспользования =
	|		ЗНАЧЕНИЕ(Перечисление.гкс_НазначенияИспользованияКачества.ПромежуточныйКомпозит)
	|	И (ПоказателиКачестваУК.ТочкаМаршрута = &ТочкаМаршрута
	|		ИЛИ ПоказателиКачестваУК.ТочкаМаршрута = ЗНАЧЕНИЕ(Справочник.гкс_ТочкиМаршрутов.ПустаяСсылка))
	|	И (ПоказателиКачестваУК.ВидПеревозки = &ВидПеревозки
	|		ИЛИ ПоказателиКачестваУК.ВидПеревозки = ЗНАЧЕНИЕ(Справочник.гкс_ВидыПеревозки.ПустаяСсылка))";

	// Установка параметров
	Если МассивСтрокДанных.Количество() > 0 Тогда
		ПерваяРегистрация = МассивСтрокДанных[0].Регистрация;
		Запрос.УстановитьПараметр("ТочкаМаршрута", ПерваяРегистрация.ТочкаМаршрута);
		Запрос.УстановитьПараметр("ВидПеревозки", ПерваяРегистрация.ВидПеревозки);
	Иначе
		Запрос.УстановитьПараметр("ТочкаМаршрута", Справочники.гкс_ТочкиМаршрутов.ПустаяСсылка());
		Запрос.УстановитьПараметр("ВидПеревозки", Справочники.гкс_ВидыПеревозки.ПустаяСсылка());
	КонецЕсли;

	Возврат Запрос.Выполнить().Выгрузить();

КонецФункции
```

##### Процедура: `ДобавитьРегистрацию(СтруктураСтроки)`
**Расположение**: строки 280-310
**Назначение**: Добавление регистрации в табличную часть

**Код**:
```bsl
&НаСервере
Процедура ДобавитьРегистрацию(СтруктураСтроки)

	НоваяСтрока = Объект.Регистрации.Добавить();

	// Заполнение реквизитов
	НоваяСтрока.Регистрация = СтруктураСтроки.Регистрация;
	НоваяСтрока.Номенклатура = СтруктураСтроки.Номенклатура;
	НоваяСтрока.Характеристика = СтруктураСтроки.Характеристика;
	НоваяСтрока.ВидТранспорта = СтруктураСтроки.ВидТранспорта;

КонецПроцедуры
```

---

### 2.4 Модуль команды (CommandModule.bsl) - 15 строк

Простой модуль для открытия формы обработки.

**Код**:
```bsl
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ПромежуточныйКомпозит", Истина);

	ОткрытьФорму("Обработка.гкс_АРМПромежуточныйКомпозит.Форма.Форма",
		ПараметрыФормы,
		ПараметрыВыполненияКоманды.Источник,
		ПараметрыВыполненияКоманды.Уникальность,
		ПараметрыВыполненияКоманды.Окно,
		ПараметрыВыполненияКоманды.НавигационнаяСсылка);

КонецПроцедуры
```

---

## 3. СВЯЗАННЫЕ ОБЪЕКТЫ КОНФИГУРАЦИИ

### 3.1 Документы (5 объектов)

#### 1. гкс_ФормированиеНомераПробы
**Назначение**: Формирование номеров проб (единичных и композитных)
**Использование**: Создается при формировании композитной пробы
**Методы**:
- `ДокументПромежуточногоКомпозита(ПараметрыСоздания, ПараметрыСозданияАнализа)` - создание промежуточного композита

#### 2. гкс_ЛабораторныйАнализ
**Назначение**: Регистрация лабораторных анализов проб
**Использование**: Автоматически создается при формировании композита
**Связь**: Ссылается на документ ФормированиеНомераПробы

#### 3. гкс_РегистрацияНаПЛК
**Назначение**: Основной документ регистрации транспортных средств на пункте лабораторного контроля
**Использование**: Источник данных для композитов
**Реквизиты**:
- Номенклатура
- Организация
- Контрагент
- ТочкаМаршрута
- ВидПеревозки
- ВидТранспорта

#### 4. гкс_ПриемкаПоКачеству
**Назначение**: Документ приемки по качеству
**Связь**: Связан с регистрациями через состояния качества

#### 5. гкс_Взвешивание
**Назначение**: Документ взвешивания
**Связь**: Связан с регистрациями

---

### 3.2 Регистры сведений (3 объекта)

#### 1. гкс_СостоянияРегистрации
**Тип**: Регистр сведений с периодичностью
**Назначение**: Хранение состояний качества усиленного контроля для регистраций
**Измерения**:
- Регистрация (Документ.гкс_РегистрацияНаПЛК)

**Ресурсы**:
- СостояниеКачестваУК (Перечисление.гкс_СостоянияКачестваУсиленногоКонтроля)

**Использование**:
- Определение возможности создания композита (только статусы "Пустое" или "ОтборПроб")
- Обновление после создания композита

#### 2. гкс_КомпозитныеПробы
**Тип**: Регистр сведений
**Назначение**: Связь регистраций с композитными пробами и показателями
**Измерения**:
- Регистрация (Документ.гкс_РегистрацияНаПЛК)
- Показатель (Справочник.гкс_ПоказателиКачества)

**Ресурсы**:
- ФормированиеНомераПробы (Документ.гкс_ФормированиеНомераПробы)

**Использование**: Запись после создания композита

#### 3. гкс_ПоказателиКачестваУсиленногоКонтроля
**Тип**: Регистр сведений
**Назначение**: Хранение показателей качества для различных назначений использования
**Измерения**:
- НазначениеИспользования (Перечисление.гкс_НазначенияИспользованияКачества)
- Показатель (Справочник.гкс_ПоказателиКачества)
- ТочкаМаршрута (Справочник.гкс_ТочкиМаршрутов)
- ВидПеревозки (Справочник.гкс_ВидыПеревозки)

**Использование**: Определение показателей для промежуточных композитов

---

### 3.3 Справочники (4 объекта)

#### 1. гкс_ТочкиМаршрутов
**Назначение**: Справочник точек маршрутов
**Использование**: Фильтрация регистраций и показателей

#### 2. гкс_ВидыПеревозки
**Назначение**: Справочник видов перевозки
**Использование**: Фильтрация регистраций и показателей

#### 3. Номенклатура
**Назначение**: Стандартный справочник номенклатуры
**Использование**:
- Обязательное совпадение при создании композита
- Фильтрация данных

#### 4. Организации / Контрагенты
**Назначение**: Справочники организаций и контрагентов
**Использование**:
- Опциональное совпадение при создании композита (с подтверждением)
- Фильтрация данных

---

### 3.4 Перечисления (6 объектов)

#### 1. гкс_СостоянияКачестваУсиленногоКонтроля
**Значения**:
- ПустаяСсылка (начальное состояние)
- ОтборПроб (отбор проб в процессе)
- АнализВыполнен (анализ выполнен)
- КачествоПринято (качество принято)
- КачествоНеПринято (качество не принято)
- НеПринятоВходнойКонтроль (не принято на входном контроле)

**Использование**: Управление жизненным циклом регистраций

#### 2. гкс_НазначенияИспользованияКачества
**Значения**:
- ПромежуточныйКомпозит
- ОкончательныйКомпозит
- ЕдиничнаяПроба

**Использование**: Классификация показателей и проб

#### 3. гкс_ВидыПроб
**Значения**:
- ЕдиничнаяПроба
- КомпозитнаяПроба

**Использование**: Определение типа пробы в документе ФормированиеНомераПробы

#### 4. гкс_ТипыТранспорта
**Назначение**: Типы транспортных средств
**Использование**: Классификация регистраций

#### 5. гкс_ВидыКонтроляКачества
**Назначение**: Виды контроля качества
**Использование**: Определение типа контроля

#### 6. гкс_СтатусыЛабораторныхАнализов
**Назначение**: Статусы лабораторных анализов
**Использование**: Управление жизненным циклом анализов

---

### 3.5 Общие модули (6 объектов)

#### 1. ОбщегоНазначения
**Назначение**: Общие функции платформы
**Использование**: `СообщитьПользователю()` для вывода сообщений

#### 2. гкс_ОбщегоНазначенияСервер
**Назначение**: Серверные общие функции
**Использование**: Общие серверные операции

#### 3. гкс_ДатыИВремя
**Назначение**: Работа с датами и временем
**Использование**: Работа с производственными сутками

#### 4. гкс_РаботаСДокументами
**Назначение**: Общие функции для работы с документами
**Использование**: Создание и управление документами

#### 5. гкс_РаботаСРегистрами
**Назначение**: Общие функции для работы с регистрами
**Использование**: Запись в регистры состояний и композитных проб

#### 6. гкс_КонтрольКачества
**Назначение**: Бизнес-логика контроля качества
**Использование**: Управление состояниями и проверка правил

---

## 4. ПОТОКИ ДАННЫХ

### 4.1 Поток 1: Создание произвольного композита

```
┌─────────────────────────────────────────────────────────────────┐
│ Шаг 1: Пользователь открывает АРМ                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
           CommandModule.ОбработкаКоманды()
                              ↓
      ОткрываетсяФорма с параметром "ПромежуточныйКомпозит" = Истина

┌─────────────────────────────────────────────────────────────────┐
│ Шаг 2: Загрузка данных                                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
              ПриСозданииНаСервере()
                              ↓
    ObjectModule.ДанныеДляСписокПроизвольныйКомпозит()
                              ↓
        Запрос с 4 временными таблицами:
        ┌───────────────────────────────────┐
        │ ВТ_Регистрации                    │ ← РС СостоянияРегистрации.СрезПоследних
        │   (фильтр по состоянию)           │
        └───────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────┐
        │ ВТ_КомпозитныеПробы               │ ← РС КомпозитныеПробы
        │   (связь регистраций с пробами)   │
        └───────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────┐
        │ ВТ_ЕдиничныеПробы                 │ ← Документ.ФормированиеНомераПробы
        │   (единичные пробы)               │
        └───────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────┐
        │ Основной SELECT                   │
        │ JOIN всех ВТ + документ          │
        │ РегистрацияНаПЛК + показатели УК │
        └───────────────────────────────────┘
                              ↓
           Отображение регистраций в таблице

┌─────────────────────────────────────────────────────────────────┐
│ Шаг 3: Выделение регистраций                                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
       Пользователь ставит галочку "Пометка"
                              ↓
    СписокПроизвольныйКомпозитПометкаПриИзменении()
                              ↓
    ┌─────────────────────────────────────────┐
    │ Автоматическое выделение строк с той    │
    │ же группой ТС и показателем             │
    └─────────────────────────────────────────┘
                              ↓
    ┌─────────────────────────────────────────┐
    │ Валидация данных:                       │
    │ - Строгая: Номенклатура, Показатель     │
    │ - Мягкая: Организация, Контрагент       │
    └─────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Шаг 4: Формирование композита                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
     КомандаСформироватьПробыПроизвольныйКомпозит()
                              ↓
    ┌─────────────────────────────────────────┐
    │ Проверка состояний качества             │
    │ (должно быть "Пустое" или "ОтборПроб")  │
    └─────────────────────────────────────────┘
                              ↓
    ┌─────────────────────────────────────────┐
    │ Открытие ФормаФормированиеКомпозита    │
    │ Передача массива отмеченных строк       │
    └─────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Шаг 5: Создание документов                                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
    ФормаФормированиеКомпозитнойФормы.СформироватьДокумент()
                              ↓
    ┌─────────────────────────────────────────┐
    │ Подготовка параметров:                  │
    │ - Массив регистраций                    │
    │ - Массив показателей                    │
    │ - НазначениеИспользования =             │
    │   ПромежуточныйКомпозит                 │
    └─────────────────────────────────────────┘
                              ↓
    Документы.гкс_ФормированиеНомераПробы
        .ДокументПромежуточногоКомпозита()
                              ↓
    ┌─────────────────────────────────────────┐
    │ 1. Создание документа                   │
    │    "ФормированиеНомераПробы"            │
    │    - Дата, номер                        │
    │    - ВидПробы = КомпозитнаяПроба        │
    │    - Табличная часть Регистрации        │
    └─────────────────────────────────────────┘
                              ↓
    ┌─────────────────────────────────────────┐
    │ 2. Создание документа                   │
    │    "ЛабораторныйАнализ"                 │
    │    - Ссылка на ФормированиеНомераПробы  │
    │    - Табличная часть Показатели         │
    └─────────────────────────────────────────┘
                              ↓
    ┌─────────────────────────────────────────┐
    │ 3. Запись в РС КомпозитныеПробы        │
    │    Для каждой регистрации:              │
    │    - Регистрация                        │
    │    - Показатель                         │
    │    - ФормированиеНомераПробы            │
    └─────────────────────────────────────────┘
                              ↓
    ┌─────────────────────────────────────────┐
    │ 4. Обновление РС СостоянияРегистрации   │
    │    СостояниеКачестваУК = "ОтборПроб"    │
    └─────────────────────────────────────────┘
                              ↓
                   Успешное завершение
```

---

### 4.2 Поток 2: Просмотр промежуточных проб

```
┌─────────────────────────────────────────────────────────────────┐
│ Шаг 1: Переход на страницу "Усиленный контроль"                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        Активируется СтраницаУсиленныйКонтроль

┌─────────────────────────────────────────────────────────────────┐
│ Шаг 2: Загрузка данных                                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
    ObjectModule.ДанныеДляСписокПромежуточныеПробы()
                              ↓
        Запрос с 5 временными таблицами:
        ┌───────────────────────────────────┐
        │ ВТ_РегистрацииОтборПроб           │ ← РС СостоянияРегистрации
        │   (состояние = "ОтборПроб")       │
        └───────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────┐
        │ ВТ_ПоказателиДляОтбора            │ ← РС ПоказателиКачестваУК
        └───────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────┐
        │ SELECT 1: Пробы в статусе         │
        │          "ОтборПроб"              │
        └───────────────────────────────────┘
                              ↓
                           UNION
                              ↓
        ┌───────────────────────────────────┐
        │ SELECT 2: Сформированные          │
        │          композитные пробы        │
        └───────────────────────────────────┘
                              ↓
           Отображение в таблице формы

┌─────────────────────────────────────────────────────────────────┐
│ Шаг 3: Просмотр/открытие документов                             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
            Двойной клик по строке
                              ↓
    Открытие документа ФормированиеНомераПробы
              или ЛабораторныйАнализ
```

---

## 5. АЛГОРИТМЫ (ПСЕВДОКОД)

### 5.1 Алгоритм автоматического выделения строк

```
ФУНКЦИЯ ОбработкаПометки(ТекущаяСтрока, НовоеЗначениеПометки):

    ЕСЛИ НовоеЗначениеПометки = ИСТИНА:
        // ========== ВЫДЕЛЕНИЕ СТРОК ==========

        // 1. Найти первую отмеченную строку (не текущую)
        ПерваяОтмеченнаяСтрока = NULL
        ДЛЯ КАЖДОЙ Строка ИЗ ВсеСтроки:
            ЕСЛИ Строка.Пометка = ИСТИНА И Строка != ТекущаяСтрока:
                ПерваяОтмеченнаяСтрока = Строка
                ПРЕРВАТЬ

        // 2. Если есть другие отмеченные строки - проверить совпадение
        ЕСЛИ ПерваяОтмеченнаяСтрока != NULL:
            Результат = ПроверитьСовпадение(ТекущаяСтрока, ПерваяОтмеченнаяСтрока)
            ЕСЛИ Результат = ЛОЖЬ:
                ТекущаяСтрока.Пометка = ЛОЖЬ
                ВОЗВРАТ

        // 3. Автоматически выделить все строки с той же группой ТС и показателем
        ДЛЯ КАЖДОЙ Строка ИЗ ВсеСтроки:
            ЕСЛИ Строка.ГруппаТС = ТекущаяСтрока.ГруппаТС И
                 Строка.Показатель = ТекущаяСтрока.Показатель:

                // Проверка совпадения для каждой строки
                ЕСЛИ ПерваяОтмеченнаяСтрока != NULL:
                    Результат = ПроверитьСовпадение(Строка, ПерваяОтмеченнаяСтрока)
                    ЕСЛИ Результат = ЛОЖЬ:
                        ПРОДОЛЖИТЬ  // Пропустить эту строку

                Строка.Пометка = ИСТИНА

    ИНАЧЕ:
        // ========== СНЯТИЕ ВЫДЕЛЕНИЯ ==========

        // Снять выделение со всех строк с той же группой ТС и показателем
        ДЛЯ КАЖДОЙ Строка ИЗ ВсеСтроки:
            ЕСЛИ Строка.ГруппаТС = ТекущаяСтрока.ГруппаТС И
                 Строка.Показатель = ТекущаяСтрока.Показатель:
                Строка.Пометка = ЛОЖЬ
```

---

### 5.2 Алгоритм валидации данных

```
ФУНКЦИЯ ПроверитьСовпадение(Строка1, Строка2):

    // ========== СТРОГАЯ ПРОВЕРКА ==========

    // 1. Проверка номенклатуры (обязательное совпадение)
    ЕСЛИ Строка1.Номенклатура != Строка2.Номенклатура:
        ПоказатьПредупреждение(
            "Номенклатура должна совпадать для всех регистраций в композите")
        ВОЗВРАТ ЛОЖЬ

    // 2. Проверка показателя (обязательное совпадение)
    ЕСЛИ Строка1.Показатель != Строка2.Показатель:
        ПоказатьПредупреждение(
            "Показатель должен совпадать для всех регистраций в композите")
        ВОЗВРАТ ЛОЖЬ

    // ========== МЯГКАЯ ПРОВЕРКА ==========

    // 3. Проверка организации и контрагента (с подтверждением)
    ЕСЛИ Строка1.Организация != Строка2.Организация ИЛИ
         Строка1.Контрагент != Строка2.Контрагент:

        Ответ = ЗапроситьПодтверждение(
            "Организация или контрагент отличаются. Продолжить?",
            Кнопки = [ДА, НЕТ])

        ЕСЛИ Ответ = НЕТ:
            ВОЗВРАТ ЛОЖЬ

    ВОЗВРАТ ИСТИНА
```

---

### 5.3 Алгоритм создания композитной пробы

```
ФУНКЦИЯ СоздатьКомпозитнуюПробу(МассивРегистраций, Показатель):

    ПОПЫТКА:

        // ========== ШАГ 1: ПОДГОТОВКА ПАРАМЕТРОВ ==========

        Параметры = {
            "Регистрации": МассивРегистраций,
            "ТипКомпозита": "Промежуточный",
            "Показатели": [Показатель],
            "Номенклатура": МассивРегистраций[0].Номенклатура,
            "Характеристика": МассивРегистраций[0].Характеристика
        }

        // ========== ШАГ 2: СОЗДАНИЕ ДОКУМЕНТА ПРОБЫ ==========

        ДокументПробы = Документы.ФормированиеНомераПробы.СоздатьДокумент()
        ДокументПробы.Дата = ТекущаяДата()
        ДокументПробы.ВидПробы = Перечисления.ВидыПроб.КомпозитнаяПроба
        ДокументПробы.Номенклатура = Параметры.Номенклатура
        ДокументПробы.Характеристика = Параметры.Характеристика

        // ========== ШАГ 3: ЗАПОЛНЕНИЕ РЕГИСТРАЦИЙ ==========

        ДЛЯ КАЖДОЙ Регистрация ИЗ МассивРегистраций:
            НоваяСтрока = ДокументПробы.Регистрации.Добавить()
            НоваяСтрока.Регистрация = Регистрация.Ссылка
            НоваяСтрока.Номенклатура = Регистрация.Номенклатура
            НоваяСтрока.Характеристика = Регистрация.Характеристика
            НоваяСтрока.ВидТранспорта = Регистрация.ВидТранспорта

        // ========== ШАГ 4: ЗАПИСЬ ДОКУМЕНТА ==========

        ДокументПробы.Записать(РежимЗаписи = Проведение)

        // ========== ШАГ 5: СОЗДАНИЕ ЛАБОРАТОРНОГО АНАЛИЗА ==========

        ДокументАнализ = Документы.ЛабораторныйАнализ.СоздатьДокумент()
        ДокументАнализ.Дата = ТекущаяДата()
        ДокументАнализ.ФормированиеНомераПробы = ДокументПробы.Ссылка
        ДокументАнализ.НазначениеИспользования =
            Перечисления.НазначенияИспользования.ПромежуточныйКомпозит

        // Заполнение показателей
        НоваяСтрока = ДокументАнализ.Показатели.Добавить()
        НоваяСтрока.Показатель = Показатель

        ДокументАнализ.Записать(РежимЗаписи = Проведение)

        // ========== ШАГ 6: ЗАПИСЬ В РЕГИСТРЫ ==========

        // 6.1 Запись в РС КомпозитныеПробы
        НаборЗаписей = РегистрыСведений.КомпозитныеПробы.СоздатьНаборЗаписей()

        ДЛЯ КАЖДОЙ Регистрация ИЗ МассивРегистраций:
            Запись = НаборЗаписей.Добавить()
            Запись.Регистрация = Регистрация.Ссылка
            Запись.Показатель = Показатель
            Запись.ФормированиеНомераПробы = ДокументПробы.Ссылка

        НаборЗаписей.Записать()

        // 6.2 Обновление состояний в РС СостоянияРегистрации
        НаборЗаписей = РегистрыСведений.СостоянияРегистрации.СоздатьНаборЗаписей()

        ДЛЯ КАЖДОЙ Регистрация ИЗ МассивРегистраций:
            Запись = НаборЗаписей.Добавить()
            Запись.Период = ТекущаяДата()
            Запись.Регистрация = Регистрация.Ссылка
            Запись.СостояниеКачестваУК =
                Перечисления.СостоянияКачестваУК.ОтборПроб

        НаборЗаписей.Записать()

        // ========== ШАГ 7: ЗАВЕРШЕНИЕ ==========

        ПоказатьСообщение("Композитная проба успешно создана: " + ДокументПробы)
        ВОЗВРАТ ИСТИНА

    ИСКЛЮЧЕНИЕ Ошибка:

        ПоказатьОшибку("Ошибка создания композитной пробы: " + Ошибка.Описание)
        ВОЗВРАТ ЛОЖЬ
```

---

## 6. БИЗНЕС-ЛОГИКА И ПРАВИЛА

### 6.1 Правила создания композитной пробы

1. **Обязательные условия совпадения**:
   - Номенклатура (строгая проверка)
   - Показатель качества (строгая проверка)

2. **Рекомендуемые условия совпадения**:
   - Организация (мягкая проверка с подтверждением)
   - Контрагент (мягкая проверка с подтверждением)

3. **Состояния регистраций**:
   - Допустимые: "Пустое" или "ОтборПроб"
   - Недопустимые: любые другие (анализы уже сделаны)

4. **Группировка**:
   - Автоматическое выделение по группе ТС
   - Один показатель на композит

### 6.2 Жизненный цикл регистрации

```
Регистрация создана
       ↓
Состояние = Пустое
       ↓
[Создание композита] → Состояние = ОтборПроб
       ↓
[Лабораторный анализ] → Состояние = АнализВыполнен
       ↓
       ├→ [Качество принято] → Состояние = КачествоПринято
       │
       └→ [Качество не принято] → Состояние = КачествоНеПринято
```

### 6.3 Правила назначения показателей

Показатели определяются из РС `гкс_ПоказателиКачестваУсиленногоКонтроля` по критериям:
- `НазначениеИспользования` = "ПромежуточныйКомпозит"
- `ТочкаМаршрута` (с учетом пустой ссылки)
- `ВидПеревозки` (с учетом пустой ссылки)

---

## 7. КЛЮЧЕВЫЕ ВЫВОДЫ

### 7.1 Архитектурные особенности

1. **Многослойная архитектура**:
   - Уровень представления (формы)
   - Уровень бизнес-логики (модуль объекта)
   - Уровень данных (регистры, документы)

2. **Использование временных таблиц**:
   - Оптимизация сложных запросов
   - 4-5 ВТ для каждого основного запроса
   - Повышение производительности

3. **Клиент-серверная модель**:
   - Разделение клиентской и серверной логики
   - Минимизация обращений к серверу

### 7.2 UX/UI особенности

1. **Автоматическое выделение**:
   - Значительно упрощает работу пользователя
   - Уменьшает количество ошибок
   - Визуальная группировка данных

2. **Двухуровневая валидация**:
   - Строгая проверка обязательных полей
   - Мягкая проверка с подтверждением для рекомендуемых

3. **3 страницы формы**:
   - Логическое разделение функционала
   - Простая навигация

### 7.3 Интеграция

1. **Тесная связь с 20+ объектами**:
   - 5 документов
   - 3 регистра сведений
   - 6 перечислений
   - 4 справочника
   - 6 общих модулей

2. **Автоматическое обновление состояний**:
   - РС СостоянияРегистрации
   - РС КомпозитныеПробы

3. **Транзакционность**:
   - Использование попыток-исключений
   - Целостность данных при создании документов

### 7.4 Надежность

1. **Обработка ошибок**:
   - Try-Catch блоки
   - Понятные сообщения пользователю

2. **Валидация данных**:
   - На уровне формы
   - На уровне бизнес-логики

3. **Проверка состояний**:
   - Блокировка повторного создания анализов
   - Контроль жизненного цикла

---

## 8. РЕКОМЕНДАЦИИ ПО РАЗВИТИЮ

### 8.1 Потенциальные улучшения

1. **Оптимизация запросов**:
   - Добавление индексов в регистры
   - Кэширование часто используемых данных

2. **Расширение функционала**:
   - Массовое формирование композитов
   - Печатные формы
   - Отчеты по композитам

3. **UX улучшения**:
   - Предпросмотр композита перед созданием
   - История изменений
   - Фильтры с сохранением

### 8.2 Точки расширения

1. **Новые виды композитов**:
   - Окончательные композиты
   - Специальные композиты

2. **Интеграция с внешними системами**:
   - Экспорт данных
   - Импорт результатов анализов

3. **Автоматизация**:
   - Автоматическое формирование композитов по правилам
   - Уведомления о необходимости создания

---

## 9. СТАТИСТИКА АНАЛИЗА

**Проанализировано**:
- 4 модуля (1289 строк кода)
- 3 сложных запроса с ВТ
- 6 экспортных функций
- 15+ ключевых процедур
- 20+ связанных объектов конфигурации

**Документировано**:
- Полная структура модулей
- Все запросы с детальным описанием
- Все потоки данных
- 3 алгоритма в псевдокоде
- Все связанные объекты

**Дата анализа**: 2025-10-29
**Версия отчета**: 1.0

---

*Отчет подготовлен автоматически на основе анализа исходного кода обработки гкс_АРМПромежуточныйКомпозит*

# 📋 Clipboard MCP - Практические примеры для 1С

[← К справочнику MCP](../MCP-Complete-Reference.md) | [К оглавлению](../README.md)

## 📚 Оглавление
- [Базовые операции](#базовые-операции)
- [Работа с BSL кодом](#работа-с-bsl-кодом)
- [Интеграция в workflow разработчика](#интеграция-в-workflow-разработчика)
- [Отладка и диагностика](#отладка-и-диагностика)
- [Автоматизация рутинных задач](#автоматизация-рутинных-задач)

---

## 🎯 Базовые операции

### Пример 1: Копирование BSL кода в буфер
```javascript
// Копирование процедуры в буфер для переноса между модулями
const bslCode = `
Процедура ПроверитьКачествоТовара(Документ) Экспорт
    
    Если НЕ Документ.КонтрольКачества Тогда
        Возврат;
    КонецЕсли;
    
    Запрос = Новый Запрос;
    Запрос.Текст = "
    |ВЫБРАТЬ
    |   ТоварыДокумента.Номенклатура КАК Номенклатура,
    |   ТоварыДокумента.Качество КАК Качество
    |ИЗ
    |   Документ.ПоступлениеТоваров.Товары КАК ТоварыДокумента
    |ГДЕ
    |   ТоварыДокумента.Ссылка = &Документ";
    
    Запрос.УстановитьПараметр("Документ", Документ);
    РезультатЗапроса = Запрос.Выполнить();
    
КонецПроцедуры
`;

// Использование Clipboard MCP
await mcp__clipboard__clipboard_set_text({
    text: bslCode
});

console.log("✅ BSL код скопирован в буфер обмена");
```

### Пример 2: Получение кода из буфера и анализ
```javascript
// Получение BSL кода из буфера для анализа
const clipboardContent = await mcp__clipboard__clipboard_get_text();

// Проверка, что это BSL код
if (clipboardContent.includes("Процедура") || 
    clipboardContent.includes("Функция")) {
    
    // Анализ структуры кода
    const procedures = clipboardContent.match(/Процедура\s+(\w+)/g);
    const functions = clipboardContent.match(/Функция\s+(\w+)/g);
    
    console.log(`📊 Анализ кода из буфера:
    - Процедур: ${procedures ? procedures.length : 0}
    - Функций: ${functions ? functions.length : 0}`);
}
```

---

## 🔧 Работа с BSL кодом

### Пример 3: Форматирование и копирование запроса 1С
```javascript
// Форматирование запроса перед копированием
async function formatAndCopyQuery(rawQuery) {
    // Форматирование запроса
    const formattedQuery = rawQuery
        .replace(/ВЫБРАТЬ/g, "\n|ВЫБРАТЬ")
        .replace(/ИЗ/g, "\n|ИЗ")
        .replace(/ГДЕ/g, "\n|ГДЕ")
        .replace(/СГРУППИРОВАТЬ ПО/g, "\n|СГРУППИРОВАТЬ ПО")
        .replace(/УПОРЯДОЧИТЬ ПО/g, "\n|УПОРЯДОЧИТЬ ПО")
        .replace(/,(?=\s*\w+\.\w+)/g, ",\n|   ");
    
    // Добавление обрамления для запроса в BSL
    const bslQuery = `Запрос.Текст = "
${formattedQuery}";`;
    
    // Копирование в буфер
    await mcp__clipboard__clipboard_set_text({ text: bslQuery });
    
    return {
        success: true,
        message: "Запрос отформатирован и скопирован",
        linesCount: formattedQuery.split('\n').length
    };
}

// Использование
const result = await formatAndCopyQuery(`
ВЫБРАТЬ Номенклатура.Ссылка, Номенклатура.Наименование, 
ОстаткиТоваров.КоличествоОстаток ИЗ 
Справочник.Номенклатура КАК Номенклатура ЛЕВОЕ СОЕДИНЕНИЕ 
РегистрНакопления.ОстаткиТоваров.Остатки КАК ОстаткиТоваров 
ПО Номенклатура.Ссылка = ОстаткиТоваров.Номенклатура 
ГДЕ Номенклатура.ЭтоГруппа = ЛОЖЬ
`);
```

### Пример 4: Генерация шаблонов кода
```javascript
// Генератор шаблонов документов 1С
async function generateDocumentTemplate(documentName) {
    const template = `
// *** Модуль объекта документа ${documentName} ***

#Область ОбработчикиСобытий

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
    
    // Проверка заполнения реквизитов
    Если НЕ ПроверитьЗаполнение() Тогда
        Отказ = Истина;
        Возврат;
    КонецЕсли;
    
    // Формирование движений регистров
    Движения.${documentName}Регистр.Записывать = Истина;
    
    Для Каждого СтрокаТовары Из Товары Цикл
        Движение = Движения.${documentName}Регистр.Добавить();
        Движение.Период = Дата;
        Движение.Номенклатура = СтрокаТовары.Номенклатура;
        Движение.Количество = СтрокаТовары.Количество;
        Движение.Сумма = СтрокаТовары.Сумма;
    КонецЦикла;
    
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
    
    Если НЕ ЗначениеЗаполнено(Контрагент) Тогда
        ОбщегоНазначения.СообщитьПользователю(
            "Не заполнен контрагент",
            ЭтотОбъект,
            "Контрагент",
            ,
            Отказ);
    КонецЕсли;
    
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ПроверитьЗаполнение()
    
    Если Товары.Количество() = 0 Тогда
        ОбщегоНазначения.СообщитьПользователю(
            "Не заполнена табличная часть 'Товары'");
        Возврат Ложь;
    КонецЕсли;
    
    Возврат Истина;
    
КонецФункции

`;
    
    await mcp__clipboard__clipboard_set_text({ text: template });
    
    return {
        success: true,
        documentName: documentName,
        templateSize: template.length,
        message: `✅ Шаблон документа '${documentName}' скопирован в буфер`
    };
}

// Использование
await generateDocumentTemplate("ПоступлениеТоваров");
```

---

## 🚀 Интеграция в workflow разработчика

### Пример 5: Обмен кодом между конфигурациями
```javascript
// Сценарий: Копирование общего модуля между базами
class ModuleTransfer {
    constructor() {
        this.clipboardMCP = mcp__clipboard__clipboard_set_text;
        this.getClipboard = mcp__clipboard__clipboard_get_text;
    }
    
    async copyModuleForTransfer(modulePath) {
        // Читаем модуль из исходной конфигурации
        const moduleContent = await mcp__filesystem__read_text_file({
            path: modulePath
        });
        
        // Добавляем метаинформацию
        const transferPackage = {
            metadata: {
                source: modulePath,
                timestamp: new Date().toISOString(),
                type: "CommonModule",
                encoding: "UTF-8",
                framework: "1C-Enterprise_Framework"
            },
            content: moduleContent
        };
        
        // Копируем в буфер как JSON
        await this.clipboardMCP({
            text: JSON.stringify(transferPackage, null, 2)
        });
        
        console.log(`
✅ Модуль подготовлен к переносу:
   📁 Источник: ${modulePath}
   📋 Размер: ${moduleContent.length} символов
   🔄 Статус: Скопирован в буфер обмена
   
   Инструкция:
   1. Откройте целевую конфигурацию
   2. Используйте команду 'Вставить модуль из буфера'
   3. Модуль будет автоматически создан с сохранением структуры
        `);
    }
    
    async pasteModuleFromClipboard(targetPath) {
        // Получаем данные из буфера
        const clipboardData = await this.getClipboard();
        
        try {
            const transferPackage = JSON.parse(clipboardData);
            
            // Проверяем валидность пакета
            if (!transferPackage.metadata || !transferPackage.content) {
                throw new Error("Неверный формат данных в буфере");
            }
            
            // Создаем модуль в целевой конфигурации
            await mcp__filesystem__write_file({
                path: targetPath,
                content: transferPackage.content
            });
            
            console.log(`
✅ Модуль успешно вставлен:
   📁 Источник: ${transferPackage.metadata.source}
   📁 Цель: ${targetPath}
   📅 Время переноса: ${transferPackage.metadata.timestamp}
            `);
            
            return { success: true, metadata: transferPackage.metadata };
            
        } catch (error) {
            console.error("❌ Ошибка при вставке модуля:", error.message);
            return { success: false, error: error.message };
        }
    }
}

// Использование
const transfer = new ModuleTransfer();

// Копирование модуля
await transfer.copyModuleForTransfer(
    "src/CommonModules/РаботаСКачеством/Module.bsl"
);

// Вставка в другую конфигурацию
await transfer.pasteModuleFromClipboard(
    "target/CommonModules/РаботаСКачеством/Module.bsl"
);
```

### Пример 6: Сбор фрагментов кода для code review
```javascript
// Система сбора фрагментов для review
class CodeReviewCollector {
    constructor() {
        this.fragments = [];
    }
    
    async addFragment(filePath, startLine, endLine, comment) {
        // Читаем фрагмент кода
        const fileContent = await mcp__filesystem__read_text_file({
            path: filePath
        });
        
        const lines = fileContent.split('\n');
        const fragment = lines.slice(startLine - 1, endLine).join('\n');
        
        this.fragments.push({
            file: filePath,
            lines: `${startLine}-${endLine}`,
            comment: comment,
            code: fragment
        });
        
        // Обновляем буфер обмена
        await this.updateClipboard();
    }
    
    async updateClipboard() {
        // Форматируем все фрагменты для review
        let reviewContent = `# Code Review - ${new Date().toLocaleDateString()}\n\n`;
        reviewContent += `Всего фрагментов: ${this.fragments.length}\n\n`;
        
        this.fragments.forEach((fragment, index) => {
            reviewContent += `## ${index + 1}. ${fragment.file} (строки ${fragment.lines})\n\n`;
            reviewContent += `**Комментарий:** ${fragment.comment}\n\n`;
            reviewContent += '```bsl\n';
            reviewContent += fragment.code;
            reviewContent += '\n```\n\n';
            reviewContent += '---\n\n';
        });
        
        await mcp__clipboard__clipboard_set_text({
            text: reviewContent
        });
        
        console.log(`✅ Code Review обновлен: ${this.fragments.length} фрагментов`);
    }
    
    async generateReviewReport() {
        const clipboardContent = await mcp__clipboard__clipboard_get_text();
        
        // Сохраняем отчет
        const reportPath = `reports/code-review-${Date.now()}.md`;
        await mcp__filesystem__write_file({
            path: reportPath,
            content: clipboardContent
        });
        
        return reportPath;
    }
}

// Использование
const reviewer = new CodeReviewCollector();

// Добавляем фрагменты для review
await reviewer.addFragment(
    "src/Documents/ПоступлениеТоваров/ObjectModule.bsl",
    45, 67,
    "Неоптимальный запрос - можно использовать временную таблицу"
);

await reviewer.addFragment(
    "src/CommonModules/РаботаСДокументами/Module.bsl",
    120, 145,
    "Дублирование кода - вынести в отдельную функцию"
);

// Генерируем отчет
const reportPath = await reviewer.generateReviewReport();
console.log(`📄 Отчет сохранен: ${reportPath}`);
```

---

## 🔍 Отладка и диагностика

### Пример 7: Копирование стека вызовов и переменных
```javascript
// Сбор отладочной информации
async function collectDebugInfo(errorContext) {
    const debugInfo = {
        timestamp: new Date().toISOString(),
        error: errorContext.message,
        stack: errorContext.stack,
        module: errorContext.module,
        procedure: errorContext.procedure,
        line: errorContext.line,
        variables: {},
        query: null
    };
    
    // Сбор значений переменных (из отладчика 1С)
    debugInfo.variables = {
        'Документ': 'ПоступлениеТоваров 00001 от 01.10.2024',
        'Контрагент': 'ООО "Поставщик"',
        'СуммаДокумента': 150000,
        'ТоварыМассив': '[10 элементов]'
    };
    
    // Если ошибка в запросе
    if (errorContext.query) {
        debugInfo.query = errorContext.query;
    }
    
    // Форматируем для буфера
    const debugText = `
🔴 ОТЛАДОЧНАЯ ИНФОРМАЦИЯ
========================
Время: ${debugInfo.timestamp}
Модуль: ${debugInfo.module}
Процедура: ${debugInfo.procedure}
Строка: ${debugInfo.line}

ОШИБКА:
${debugInfo.error}

СТЕК ВЫЗОВОВ:
${debugInfo.stack}

ПЕРЕМЕННЫЕ:
${JSON.stringify(debugInfo.variables, null, 2)}

${debugInfo.query ? `ЗАПРОС:\n${debugInfo.query}` : ''}
========================
    `;
    
    await mcp__clipboard__clipboard_set_text({ text: debugText });
    
    // Также сохраняем в файл для истории
    const logPath = `logs/debug-${Date.now()}.txt`;
    await mcp__filesystem__write_file({
        path: logPath,
        content: debugText
    });
    
    return {
        success: true,
        logPath: logPath,
        message: "Отладочная информация скопирована в буфер и сохранена"
    };
}

// Использование при ошибке
await collectDebugInfo({
    message: "Ошибка при проведении документа: Не найден контрагент",
    stack: `ОбработкаПроведения() - строка 45
    ПровестиДокумент() - строка 12
    ОбработчикКнопкиПровести() - строка 3`,
    module: "Документ.ПоступлениеТоваров.МодульОбъекта",
    procedure: "ОбработкаПроведения",
    line: 45,
    query: "ВЫБРАТЬ * ИЗ Справочник.Контрагенты ГДЕ Ссылка = &Контрагент"
});
```

---

## ⚡ Автоматизация рутинных задач

### Пример 8: Массовое создание обработчиков событий
```javascript
// Генератор обработчиков для формы
async function generateFormHandlers(formElements) {
    let handlers = `#Область ОбработчикиСобытийФормы\n\n`;
    
    // Генерируем обработчики для каждого элемента
    formElements.forEach(element => {
        // Обработчик изменения
        handlers += `
&НаКлиенте
Процедура ${element.name}ПриИзменении(Элемент)
    
    // TODO: Добавить логику обработки изменения ${element.name}
    ОбновитьИтоги();
    
КонецПроцедуры\n`;
        
        // Обработчик начала выбора (для полей ввода)
        if (element.type === 'input') {
            handlers += `
&НаКлиенте
Процедура ${element.name}НачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
    
    СтандартнаяОбработка = Ложь;
    // TODO: Настроить выбор для ${element.name}
    
КонецПроцедуры\n`;
        }
        
        // Обработчик нажатия (для кнопок)
        if (element.type === 'button') {
            handlers += `
&НаКлиенте
Процедура ${element.name}Нажатие(Команда)
    
    // TODO: Добавить обработку нажатия ${element.name}
    ${element.name}НаСервере();
    
КонецПроцедуры

&НаСервере
Процедура ${element.name}НаСервере()
    
    // TODO: Серверная логика для ${element.name}
    
КонецПроцедуры\n`;
        }
    });
    
    handlers += `\n#КонецОбласти`;
    
    // Копируем в буфер
    await mcp__clipboard__clipboard_set_text({ text: handlers });
    
    return {
        success: true,
        handlersCount: formElements.length,
        totalSize: handlers.length
    };
}

// Использование
const formElements = [
    { name: "Контрагент", type: "input" },
    { name: "Договор", type: "input" },
    { name: "СуммаДокумента", type: "input" },
    { name: "КнопкаЗаполнить", type: "button" },
    { name: "КнопкаПровести", type: "button" }
];

await generateFormHandlers(formElements);
console.log("✅ Обработчики сгенерированы и скопированы в буфер");
```

### Пример 9: Создание тестовых данных
```javascript
// Генератор тестовых данных для 1С
async function generateTestData(config) {
    let testData = "";
    
    if (config.type === "catalog") {
        // Генерация элементов справочника
        testData = `// Тестовые данные для справочника ${config.name}\n\n`;
        
        for (let i = 1; i <= config.count; i++) {
            testData += `
НовыйЭлемент = Справочники.${config.name}.СоздатьЭлемент();
НовыйЭлемент.Код = "${String(i).padStart(6, '0')}";
НовыйЭлемент.Наименование = "${config.prefix} ${i}";
НовыйЭлемент.ИНН = "${String(Math.floor(Math.random() * 999999999999)).padStart(12, '0')}";
НовыйЭлемент.КПП = "${String(Math.floor(Math.random() * 999999999)).padStart(9, '0')}";
НовыйЭлемент.Записать();
`;
        }
    } else if (config.type === "document") {
        // Генерация документов
        testData = `// Тестовые документы ${config.name}\n\n`;
        
        for (let i = 1; i <= config.count; i++) {
            const date = new Date();
            date.setDate(date.getDate() - Math.floor(Math.random() * 30));
            
            testData += `
НовыйДокумент = Документы.${config.name}.СоздатьДокумент();
НовыйДокумент.Дата = '${date.toISOString().split('T')[0].replace(/-/g, '')}';
НовыйДокумент.Номер = "${String(i).padStart(8, '0')}";
НовыйДокумент.Контрагент = Справочники.Контрагенты.НайтиПоКоду("${String(Math.floor(Math.random() * 100) + 1).padStart(6, '0')}");

// Заполнение табличной части
Для Индекс = 1 По ${Math.floor(Math.random() * 10) + 1} Цикл
    НоваяСтрока = НовыйДокумент.Товары.Добавить();
    НоваяСтрока.Номенклатура = Справочники.Номенклатура.НайтиПоКоду("${String(Math.floor(Math.random() * 1000) + 1).padStart(6, '0')}");
    НоваяСтрока.Количество = ${Math.floor(Math.random() * 100) + 1};
    НоваяСтрока.Цена = ${Math.floor(Math.random() * 10000) + 100};
    НоваяСтрока.Сумма = НоваяСтрока.Количество * НоваяСтрока.Цена;
КонецЦикла;

НовыйДокумент.Записать(РежимЗаписиДокумента.Проведение);
`;
        }
    }
    
    await mcp__clipboard__clipboard_set_text({ text: testData });
    
    return {
        success: true,
        type: config.type,
        count: config.count,
        message: `Сгенерировано ${config.count} тестовых записей`
    };
}

// Использование
await generateTestData({
    type: "catalog",
    name: "Контрагенты",
    prefix: "Тестовый контрагент",
    count: 10
});

await generateTestData({
    type: "document",
    name: "ПоступлениеТоваров",
    count: 5
});
```

---

## 📊 Интеграция с другими MCP серверами

### Пример 10: Clipboard + ✅ BSL Language Server
```javascript
// Анализ кода из буфера обмена
async function analyzeClipboardCode() {
    // Получаем код из буфера
    const code = await mcp__clipboard__clipboard_get_text();
    
    // Сохраняем во временный файл для анализа
    const tempFile = `temp/clipboard-analysis-${Date.now()}.bsl`;
    await mcp__filesystem__write_file({
        path: tempFile,
        content: code
    });
    
    // Запускаем BSL анализ
    const analysisResult = await mcp__serena__execute_shell_command({
        command: `python -m sonar_integration analyze --src-dir ${tempFile} --quick`
    });
    
    // Формируем отчет
    const report = `
# Анализ BSL кода из буфера обмена

## Результаты проверки:
${analysisResult.stdout}

## Рекомендации:
- Исправить критические ошибки
- Оптимизировать запросы
- Добавить обработку исключений
    `;
    
    // Копируем отчет обратно в буфер
    await mcp__clipboard__clipboard_set_text({ text: report });
    
    // Удаляем временный файл
    await mcp__filesystem__delete(tempFile);
    
    return {
        success: true,
        issues: analysisResult.stdout.match(/error|warning/gi)?.length || 0
    };
}
```

---

## 🛠️ Настройка и конфигурация

### Конфигурация в claude_desktop_config.json:
```json
{
  "mcpServers": {
    "clipboard": {
      "command": "npx",
      "args": ["-y", "@smithery/clipboard-mcp"],
      "env": {
        "CLIPBOARD_TIMEOUT": "5000",
        "CLIPBOARD_MAX_SIZE": "1048576"
      }
    }
  }
}
```

### Переменные окружения:
- `CLIPBOARD_TIMEOUT` - таймаут операций (мс)
- `CLIPBOARD_MAX_SIZE` - максимальный размер данных (байт)

---

## ⚠️ Важные замечания

1. **Безопасность**: Не копируйте конфиденциальные данные (пароли, ключи API)
2. **Кодировка**: Всегда используйте UTF-8 для BSL кода
3. **Размер**: Ограничение на размер буфера - 1 МБ
4. **Форматы**: Поддерживается только текстовый формат
5. **Очистка**: Регулярно очищайте буфер от чувствительных данных

---

## 📚 Дополнительные ресурсы

- [Официальная документация Clipboard MCP](https://github.com/smithery/clipboard-mcp)
- [✅ BSL Language Server](../BSL-Integration/README.md)
- [Примеры интеграции с 1С](../Examples/1C-Integration/)

---

*Последнее обновление: ${new Date().toLocaleDateString()}*
*Версия документа: 1.0.0*

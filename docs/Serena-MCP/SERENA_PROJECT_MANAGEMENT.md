# Руководство по управлению проектами в Serena

## 📖 Оглавление

1. [Архитектура Serena](#архитектура-serena)
2. [Структура проектов](#структура-проектов)
3. [Процесс онбординга](#процесс-онбординга)
4. [Управление памятью](#управление-памятью)
5. [Активация проектов](#активация-проектов)
6. [Конфигурация](#конфигурация)
7. [Практические примеры](#практические-примеры)

---

## Архитектура Serena

### Основные принципы

Serena использует **глобальную конфигурацию на уровне корня репозитория**, а не для каждого подпроекта. Это означает:

- ✅ **Один главный проект** - корневая папка репозитория
- ✅ **Централизованная память** - все воспоминания в одном месте
- ✅ **Единая конфигурация** - settings в `.serena/project.yml`
- ❌ **НЕТ подпроектов** - каждая подпапка НЕ является отдельным проектом

### Файловая структура

```
D:\1C-Enterprise_Framework\              ← ГЛАВНЫЙ ПРОЕКТ
│
├── .serena/                             ← КОНФИГУРАЦИЯ SERENA
│   ├── project.yml                      ← Настройки проекта
│   ├── bsl-config.json                  ← Специфичные настройки для BSL
│   ├── .gitignore                       ← Игнорирование служебных файлов
│   │
│   ├── memories/                        ← ПАМЯТЬ SERENA (MD файлы)
│   │   ├── project_overview.md          ← Обзор проекта
│   │   ├── 1c_code_conventions.md       ← Конвенции кода 1С
│   │   ├── project_251029_GKSTCPLK-1831_overview.md  ← Обзор подпроекта
│   │   ├── detailed_analysis_ARM_IntermediateComposite.md
│   │   ├── active_project_context.md    ← Текущий контекст работы
│   │   └── ...                          ← Другие воспоминания
│   │
│   └── cache/                           ← КЭШ (индексы, временные данные)
│       └── ...
│
├── src/
│   └── projects/
│       └── configuration/
│           └── 251029_GKSTCPLK-1831/    ← Подпроект (НЕ отдельный проект Serena!)
│               ├── .serena/             ← ПУСТАЯ папка (не используется)
│               ├── src/                 ← Исходный код
│               └── docs/                ← Документация
│
└── serena/                              ← Сам инструмент Serena (submodule)
    └── .serena/
        └── memories/                    ← Память о САМОЙ Serena
```

---

## Структура проектов

### Уровни организации

Serena поддерживает **только один уровень проектов**:

```
1. Главный проект = корень репозитория
   └── Вся работа ведется в контексте этого проекта
```

**НЕ поддерживается**:
```
❌ Главный проект
   └── Подпроект 1
   └── Подпроект 2
```

### Как работать с несколькими "логическими проектами"

Если у вас есть несколько логических проектов (как в нашем случае - разные конфигурации 1С), используйте:

1. **Память Serena** - создавайте отдельные MD-файлы для каждого логического проекта
2. **Префиксы имен** - `project_251029_GKSTCPLK-1831_overview.md`
3. **Активный контекст** - файл `active_project_context.md` указывает на текущий проект
4. **Документация** - храните документы в подпапках (как мы сделали)

### Структура папки .serena

#### 1. project.yml

Главный конфигурационный файл проекта:

```yaml
# Язык проекта (для LSP)
language: python  # Для BSL используем python как closest match

# Игнорирование файлов
ignore_all_files_in_gitignore: true
ignored_paths: []

# Режим только для чтения
read_only: false

# Исключенные инструменты
excluded_tools: []

# Начальный промпт (опционально)
initial_prompt: ""

# Имя проекта
project_name: "1C-Enterprise_Framework"
```

#### 2. memories/ - Система памяти

**Типы воспоминаний**:

##### a) Обзор проекта
```markdown
# project_overview.md
- Общее описание проекта
- Основные компоненты
- Структура репозитория
```

##### b) Обзоры подпроектов
```markdown
# project_251029_GKSTCPLK-1831_overview.md
- Описание конкретной конфигурации
- Список объектов
- Связи с другими проектами
```

##### c) Детальные анализы
```markdown
# detailed_analysis_ARM_IntermediateComposite.md
- Анализ конкретной обработки
- Код, запросы, алгоритмы
- Связанные объекты
```

##### d) Активный контекст
```markdown
# active_project_context.md
- На какой задаче сейчас работаем
- Текущий подпроект
- Последние действия
```

##### e) Специализированные
```markdown
# 1c_code_conventions.md
# suggested_commands.md
# task_completion_checklist.md
# serena_auto_project_switching.md
```

#### 3. cache/ - Кэш системы

Служебная папка для:
- Индексов символов
- Кэшированных данных LSP
- Временных файлов

**НЕ редактируется вручную!**

#### 4. bsl-config.json - Конфигурация для BSL

Специфичные настройки для работы с языком 1C:

```json
{
  "language": "bsl",
  "file_extensions": [".bsl", ".os"],
  "encoding": "utf-8",
  "search_patterns": {
    "procedures": "Процедура\\s+(\\w+)",
    "functions": "Функция\\s+(\\w+)"
  }
}
```

---

## Процесс онбординга

### Что такое онбординг?

**Онбординг** - это процесс первичной инициализации проекта в Serena:
1. Изучение структуры проекта
2. Создание начальной памяти
3. Настройка language server
4. Индексация кода

### Когда запускается онбординг?

1. **При первой активации проекта**
2. **Если папка `.serena/memories/` пустая**
3. **По явному запросу пользователя**

### Инструменты онбординга

#### 1. check_onboarding_performed

```python
# Проверка: выполнен ли онбординг?
tool: check_onboarding_performed

# Возвращает:
# - Если НЕТ памяти: "Onboarding not performed yet"
# - Если ЕСТЬ память: список всех memories
```

#### 2. onboarding

```python
# Запуск процесса онбординга
tool: onboarding

# Возвращает инструкции:
# 1. Изучить структуру проекта (list_dir, find_file)
# 2. Прочитать ключевые файлы (README, package.json, etc.)
# 3. Создать project_overview.md
# 4. Создать suggested_commands.md
# 5. Создать task_completion_checklist.md
```

### Процесс онбординга (пошагово)

#### Шаг 1: Проверка состояния

```bash
# Claude автоматически вызывает:
check_onboarding_performed()

# Результат:
# - memories = [] → онбординг НЕ выполнен
# - memories = [...] → онбординг УЖЕ выполнен
```

#### Шаг 2: Изучение проекта

```bash
# Claude выполняет:
1. list_dir(".", recursive=true)
2. find_file("README.md")
3. find_file("package.json")
4. find_file(".gitignore")
5. read_file("README.md")
```

#### Шаг 3: Анализ структуры

Claude анализирует:
- Язык программирования
- Структуру папок
- Основные компоненты
- Зависимости
- Build system

#### Шаг 4: Создание памяти

```bash
# Создаются файлы:
write_memory(
    memory_name="project_overview",
    content="# Project Overview\n..."
)

write_memory(
    memory_name="suggested_commands",
    content="# Commands\n..."
)

write_memory(
    memory_name="task_completion_checklist",
    content="# Checklist\n..."
)
```

#### Шаг 5: Индексация (опционально)

Для больших проектов:
```bash
# Запуск индексации
uv run index-project
```

### Что должно быть создано после онбординга?

**Минимальный набор**:

1. ✅ **project_overview.md** - обзор проекта
   ```markdown
   # Project Overview

   ## Description
   Framework for 1C:Enterprise development

   ## Structure
   - src/ - source code
   - docs/ - documentation
   - scripts/ - automation scripts

   ## Key Components
   - 445 custom objects with prefix гкс_
   - ARM data processors
   - Quality control system
   ```

2. ✅ **suggested_commands.md** - команды для работы
   ```markdown
   # Suggested Commands

   ## Testing
   - Run tests: `npm test`

   ## Building
   - Build: `npm run build`

   ## Linting
   - Check: `npm run lint`
   ```

3. ✅ **task_completion_checklist.md** - чеклист задач
   ```markdown
   # Task Completion Checklist

   - [ ] Tests pass
   - [ ] Code formatted
   - [ ] Documentation updated
   - [ ] Commit message follows conventions
   ```

**Расширенный набор** (по мере работы):

4. ✅ **1c_code_conventions.md** - конвенции кода
5. ✅ **active_project_context.md** - текущий контекст
6. ✅ **project_XXXXX_overview.md** - обзоры подпроектов
7. ✅ **detailed_analysis_*.md** - детальные анализы

---

## Управление памятью

### Инструменты памяти

#### 1. write_memory

Создание/обновление воспоминания:

```python
write_memory(
    memory_name="project_251029_overview",
    content="""
# Project 251029_GKSTCPLK-1831 Overview

## Description
АРМ по промежуточному композиту для УС

## Objects
- DataProcessor: гкс_АРМПромежуточныйКомпозит
- Documents: гкс_ФормированиеНомераПробы
...
"""
)
```

**Результат**: файл `.serena/memories/project_251029_overview.md`

#### 2. read_memory

Чтение воспоминания:

```python
content = read_memory("project_251029_overview")
```

#### 3. list_memories

Список всех воспоминаний:

```python
memories = list_memories()
# Возвращает: ["project_overview", "active_project_context", ...]
```

#### 4. delete_memory

Удаление воспоминания:

```python
delete_memory("outdated_memory")
```

### Стратегия использования памяти

#### Когда создавать память?

✅ **СОЗДАВАТЬ**:
- После завершения анализа
- При обнаружении важной информации
- Для сохранения контекста между сессиями
- При документировании архитектуры

❌ **НЕ СОЗДАВАТЬ**:
- Для временных данных
- Для информации, которая часто меняется
- Для дублирования кода

#### Именование памяти

**Хорошие имена**:
```
✅ project_overview
✅ project_251029_GKSTCPLK-1831_overview
✅ detailed_analysis_ARM_IntermediateComposite
✅ active_project_context
✅ 1c_code_conventions
```

**Плохие имена**:
```
❌ temp
❌ data
❌ analysis
❌ 123
```

#### Структура содержимого

Используйте Markdown с четкой структурой:

```markdown
# Заголовок памяти

## 1. Основная информация
- Что это
- Зачем нужно

## 2. Детали
### 2.1 Подраздел 1
### 2.2 Подраздел 2

## 3. Связи
- Связь с объектом X
- Связь с проектом Y

## 4. Примеры
\`\`\`bsl
Код
\`\`\`

## 5. Заметки
- Важное замечание 1
- Важное замечание 2
```

---

## Активация проектов

### Концепция активного проекта

В любой момент времени **активен только ОДИН проект**:

```
Проект A (активен) ← Serena работает здесь
Проект B (неактивен)
Проект C (неактивен)
```

### Как работает активация?

#### 1. Автоматическая активация

При запуске `serena-mcp-server` в папке проекта:

```bash
cd D:\1C-Enterprise_Framework
serena-mcp-server

# Автоматически активируется проект по конфигурации в .serena/project.yml
```

#### 2. Ручная активация (в процессе работы)

```python
# НЕ поддерживается переключение проектов в рамках одной сессии!
# Каждая сессия = один проект
```

### Состояние проекта

После активации проекта:

1. ✅ Загружается конфигурация из `.serena/project.yml`
2. ✅ Инициализируется language server (если нужен)
3. ✅ Читается список memories (но НЕ их содержимое!)
4. ✅ Применяются contexts и modes
5. ✅ Становятся доступны все инструменты

---

## Конфигурация

### Уровни конфигурации

Serena использует иерархию конфигураций:

```
1. Defaults (в коде Serena)
   ↓
2. User config (~/.serena/serena_config.yml)
   ↓
3. Project config (.serena/project.yml)
   ↓
4. Command-line arguments
```

Каждый уровень переопределяет предыдущий.

### Основные настройки project.yml

#### language

Определяет language server:

```yaml
language: python    # Для BSL - closest match
# Доступно: python, typescript, java, rust, go, cpp, csharp, php, ruby
```

#### ignore_all_files_in_gitignore

```yaml
ignore_all_files_in_gitignore: true  # Использовать .gitignore
```

#### ignored_paths

Дополнительные игнорируемые пути:

```yaml
ignored_paths:
  - "**/*.log"
  - "temp/**"
  - "cache/**"
```

#### read_only

Режим только для чтения:

```yaml
read_only: false  # Разрешить редактирование
```

#### excluded_tools

Отключенные инструменты:

```yaml
excluded_tools:
  - delete_lines
  - execute_shell_command
```

#### initial_prompt

Промпт, который всегда загружается:

```yaml
initial_prompt: |
  This is a 1C:Enterprise Framework project.
  Use BSL language conventions.
```

### Contexts и Modes

#### Contexts

Определяют набор доступных инструментов:

- **desktop-app** - для Claude Desktop
- **agent** - для автономных агентов
- **ide-assistant** - для IDE интеграции

#### Modes

Определяют режим работы:

- **interactive** - интерактивная работа
- **editing** - режим редактирования
- **planning** - режим планирования
- **one-shot** - одноразовая задача

**Настройка** (не в project.yml, а через команды):

```python
switch_modes(["interactive", "editing"])
```

---

## Практические примеры

### Пример 1: Инициализация нового проекта

```bash
# 1. Создать папку проекта
mkdir my-project
cd my-project

# 2. Инициализировать git
git init

# 3. Создать структуру .serena
mkdir .serena
mkdir .serena/memories
mkdir .serena/cache

# 4. Создать project.yml
cat > .serena/project.yml << EOF
language: python
ignore_all_files_in_gitignore: true
ignored_paths: []
read_only: false
excluded_tools: []
initial_prompt: ""
project_name: "my-project"
EOF

# 5. Запустить Serena
serena-mcp-server
```

В Claude:

```
1. Claude автоматически вызовет check_onboarding_performed()
2. Обнаружит, что памяти нет
3. Вызовет onboarding()
4. Начнет анализировать проект
5. Создаст начальные memories
```

### Пример 2: Работа с "подпроектами" (наш случай)

```markdown
# Структура памяти для нескольких логических проектов

## Общие воспоминания
.serena/memories/
├── project_overview.md              ← Обзор всего фреймворка
├── 1c_code_conventions.md           ← Конвенции для всех проектов
└── active_project_context.md        ← Текущий активный контекст

## Специфичные для подпроектов
├── project_251029_GKSTCPLK-1831_overview.md
├── project_251027_GKSTCPLK-1788_overview.md
└── project_250928_GKSTCPLK-1706_overview.md

## Детальные анализы
├── detailed_analysis_ARM_IntermediateComposite.md
└── detailed_analysis_ARM_Registration.md
```

**Использование**:

```markdown
# active_project_context.md

Сейчас работаем с: **251029_GKSTCPLK-1831**
Задача: Разработка АРМ по промежуточному композиту

Связанные воспоминания:
- project_251029_GKSTCPLK-1831_overview.md
- detailed_analysis_ARM_IntermediateComposite.md

Последние действия:
1. Создан детальный анализ обработки
2. Сохранен отчет в проект
```

### Пример 3: Создание детального анализа

```python
# 1. Анализируем код (много read_file, find_symbol, etc.)

# 2. Создаем воспоминание
write_memory(
    memory_name="detailed_analysis_ARM_IntermediateComposite",
    content="""
# Детальный анализ: гкс_АРМПромежуточныйКомпозит

## Структура
- ObjectModule.bsl (312 строк)
- Forms/Форма/Module.bsl (600 строк)
...

## Ключевые функции
### ДанныеДляСписокПроизвольныйКомпозит
...

## Связанные объекты
- Документ.гкс_ФормированиеНомераПробы
...
"""
)

# 3. Обновляем активный контекст
write_memory(
    memory_name="active_project_context",
    content="""
Текущий проект: 251029_GKSTCPLK-1831
Последнее действие: Создан детальный анализ АРМ
Следующий шаг: Разработка новой функциональности
"""
)
```

### Пример 4: Переключение между "подпроектами"

```python
# Когда пользователь говорит: "теперь работаем с проектом 251027"

# 1. Обновить active_project_context
write_memory(
    memory_name="active_project_context",
    content="""
Текущий проект: 251027_GKSTCPLK-1788
Путь: src/projects/configuration/251027_GKSTCPLK-1788
...
"""
)

# 2. Прочитать обзор этого проекта
overview = read_memory("project_251027_GKSTCPLK-1788_overview")

# 3. Начать работу в новом контексте
```

---

## Часто задаваемые вопросы

### 1. Почему .serena в подпроекте пустая?

**Ответ**: Serena работает только с **одним главным проектом** на уровне корня репозитория. Папки `.serena` в подпапках создаются автоматически, но не используются.

### 2. Как организовать работу с несколькими конфигурациями 1С?

**Ответ**: Используйте систему памяти:
- Создавайте отдельные MD-файлы для каждой конфигурации
- Используйте префиксы в именах: `project_XXXXX_overview.md`
- Обновляйте `active_project_context.md` при переключении

### 3. Что делать если онбординг не выполнен?

**Ответ**:
```bash
# В Claude:
1. Вызвать: check_onboarding_performed()
2. Если не выполнен: onboarding()
3. Следовать инструкциям
```

### 4. Можно ли иметь несколько активных проектов?

**Ответ**: Нет. В одной сессии Serena может быть активен только один проект. Для работы с несколькими проектами запускайте отдельные сессии.

### 5. Где хранится кэш и можно ли его удалить?

**Ответ**: Кэш в `.serena/cache/`. Можно безопасно удалить - пересоздастся автоматически.

### 6. Как добавить свои инструменты в Serena?

**Ответ**: Это требует модификации кода Serena:
1. Создать новый Tool в `src/serena/tools/`
2. Зарегистрировать в соответствующем registry
3. Добавить в context/mode конфигурации

### 7. Можно ли использовать Serena без language server?

**Ответ**: Да. Многие инструменты (file_tools, memory_tools) работают без LSP. Но symbol_tools требуют LSP.

---

## Рекомендации

### ✅ DO (Делайте)

1. **Создавайте память** после каждого значимого анализа
2. **Используйте префиксы** в именах воспоминаний
3. **Обновляйте active_project_context** при переключении задач
4. **Структурируйте MD-файлы** с четкими заголовками
5. **Читайте список memories** перед началом работы
6. **Удаляйте устаревшую память** для чистоты

### ❌ DON'T (Не делайте)

1. ❌ **Не пытайтесь** создавать подпроекты в .serena подпапок
2. ❌ **Не дублируйте** код в памяти (только ссылки)
3. ❌ **Не создавайте** слишком короткие имена memories
4. ❌ **Не храните** временные данные в памяти
5. ❌ **Не редактируйте** cache/ вручную
6. ❌ **Не перегружайте** project.yml ненужными настройками

---

## Заключение

Serena - это мощный инструмент для работы с кодом, который:
- ✅ Использует **единую конфигурацию** на уровне репозитория
- ✅ Хранит **память в MD-файлах** для переиспользования
- ✅ Поддерживает **множество языков** через LSP
- ✅ Предоставляет **гибкую систему инструментов**
- ✅ Интегрируется с **Claude через MCP**

Правильное использование системы памяти и понимание архитектуры позволяет эффективно работать даже с большими проектами, имеющими сложную структуру подпроектов.

---

**Дата создания**: 2025-10-29
**Версия**: 1.0
**Автор**: Claude Code Analysis

# MCP Free Translate + Keyboard Layout Fix

**Бесплатный MCP сервер для перевода текста и исправления раскладки клавиатуры без API ключей**

## 🎯 Возможности

### Перевод текста (Google Translate API)
- ✅ Перевод между любыми языками
- ✅ Автоопределение исходного языка
- ✅ Быстрые команды для EN ↔ RU
- ✅ Без необходимости API ключей

### Исправление раскладки клавиатуры (NEW! 🆕)
- ✅ Автоматическое исправление текста, набранного в неправильной раскладке
- ✅ Поддержка EN ↔ RU (QWERTY ↔ ЙЦУКЕН)
- ✅ Определение текущей раскладки
- ✅ Конвертация между раскладками

## 📦 Установка

```bash
cd mcp-free-translate
npm install
```

## 🚀 Использование

### Настройка MCP сервера

Добавьте в ваш `.mcp.json` или `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "free-translate": {
      "command": "node",
      "args": ["D:/1C-Enterprise_Framework/mcp-free-translate/index.js"]
    }
  }
}
```

### Доступные инструменты

#### 1. `translate` - Перевод текста

**Описание:** Перевод текста с использованием Google Translate API

**Параметры:**
- `text` (обязательный): Текст для перевода
- `to` (опциональный, по умолчанию "en"): Код целевого языка
- `from` (опциональный, по умолчанию "auto"): Код исходного языка

**Пример использования:**
```javascript
{
  "text": "Hello world",
  "to": "ru",
  "from": "en"
}
```

**Результат:**
```json
{
  "original": "Hello world",
  "translated": "Привет мир",
  "from": "en",
  "to": "ru",
  "confidence": 1.0
}
```

---

#### 2. `translate_to_russian` - Быстрый перевод на русский

**Описание:** Быстрый перевод любого текста на русский язык

**Параметры:**
- `text` (обязательный): Текст для перевода

**Пример:**
```javascript
{
  "text": "Good morning"
}
```

**Результат:** `Перевод: Доброе утро`

---

#### 3. `translate_to_english` - Быстрый перевод на английский

**Описание:** Быстрый перевод любого текста на английский язык

**Параметры:**
- `text` (обязательный): Текст для перевода

**Пример:**
```javascript
{
  "text": "Доброе утро"
}
```

**Результат:** `Translation: Good morning`

---

#### 4. `fix_keyboard_layout` - Исправление раскладки 🆕

**Описание:** Автоматически исправляет текст, набранный в неправильной раскладке клавиатуры

**Параметры:**
- `text` (обязательный): Текст с неправильной раскладкой
- `target_layout` (опциональный, по умолчанию "auto"): Целевая раскладка ("en", "ru", или "auto")

**Примеры использования:**

**Пример 1:** "lf" → "да"
```javascript
{
  "text": "lf",
  "target_layout": "auto"
}
```

**Результат:**
```json
{
  "original": "lf",
  "corrected": "да",
  "from_layout": "en",
  "to_layout": "ru",
  "confidence": 0.95,
  "message": "Layout fixed successfully"
}
```

**Пример 2:** "ghbdtn" → "привет"
```javascript
{
  "text": "ghbdtn"
}
```

**Результат:**
```json
{
  "original": "ghbdtn",
  "corrected": "привет",
  "from_layout": "en",
  "to_layout": "ru",
  "confidence": 0.95,
  "message": "Layout fixed successfully"
}
```

**Пример 3:** "Руддщ Цщкдв" → "Hello World"
```javascript
{
  "text": "Руддщ Цщкдв",
  "target_layout": "en"
}
```

---

#### 5. `convert_keyboard_layout` - Конвертация между раскладками 🆕

**Описание:** Явная конвертация текста из одной раскладки в другую

**Параметры:**
- `text` (обязательный): Текст для конвертации
- `from_layout` (обязательный): Исходная раскладка ("en" или "ru")
- `to_layout` (обязательный): Целевая раскладка ("en" или "ru")

**Пример:**
```javascript
{
  "text": "hello",
  "from_layout": "en",
  "to_layout": "ru"
}
```

**Результат:**
```json
{
  "original": "hello",
  "converted": "рудды",
  "from_layout": "en",
  "to_layout": "ru"
}
```

---

#### 6. `detect_keyboard_layout` - Определение раскладки 🆕

**Описание:** Определяет раскладку клавиатуры текста

**Параметры:**
- `text` (обязательный): Текст для анализа

**Возможные результаты:**
- `"en"` - английская раскладка (QWERTY)
- `"ru"` - русская раскладка (ЙЦУКЕН)
- `"mixed"` - смешанная раскладка
- `"unknown"` - не удалось определить

**Пример:**
```javascript
{
  "text": "ghbdtn"
}
```

**Результат:**
```json
{
  "text": "ghbdtn",
  "detected_layout": "en"
}
```

## 🧪 Тестирование

Запустите тестовый скрипт для проверки функций раскладки:

```bash
node test-keyboard-layouts.js
```

## 📝 Примеры реальных сценариев

### Сценарий 1: Пользователь забыл переключить раскладку

**Пользователь:** "lf" (забыл переключить на русский)

**Claude Code:**
```
mcp__free-translate__fix_keyboard_layout({
  text: "lf"
})
```

**Результат:** "да"

---

### Сценарий 2: Исправление целого предложения

**Пользователь:** "Ghbdtn! Rfr ltkf?" (Привет! Как дела?)

**Claude Code:**
```
mcp__free-translate__fix_keyboard_layout({
  text: "Ghbdtn! Rfr ltkf?"
})
```

**Результат:** "Привет! Как дела?"

---

### Сценарий 3: Комбинация с переводом

**Шаг 1:** Исправить раскладку
```
mcp__free-translate__fix_keyboard_layout({
  text: "ghbdtn"
})
```
Результат: "привет"

**Шаг 2:** Перевести на английский
```
mcp__free-translate__translate({
  text: "привет",
  to: "en"
})
```
Результат: "hello"

## 🔧 Технические детали

### Маппинг клавиатуры

Сервер использует полный маппинг клавиш между раскладками QWERTY и ЙЦУКЕН:

- **Буквы:** q→й, w→ц, e→у, r→к, t→е, y→н, u→г, i→ш, o→щ, p→з, и т.д.
- **Специальные символы:** ;→ж, '→э, ,→б, .→ю, /→.
- **Регистр:** Поддерживается верхний и нижний регистр

### Алгоритм определения раскладки

1. Анализ каждого символа текста
2. Подсчет символов, принадлежащих EN и RU раскладкам
3. Вычисление соотношения (ratio)
4. Определение раскладки на основе порогов:
   - `> 70%` EN символов → английская раскладка
   - `> 70%` RU символов → русская раскладка
   - Оба `> 10%` → смешанная раскладка
   - Иначе → неизвестно

## 📚 Дополнительные ресурсы

- [Model Context Protocol Documentation](https://modelcontextprotocol.io/)
- [Google Translate API (vitalets)](https://github.com/vitalets/google-translate-api)

## 🐛 Устранение неполадок

### Проблема: Сервер не запускается

**Решение:** Убедитесь, что установлены все зависимости:
```bash
npm install
```

### Проблема: Ошибка при переводе

**Решение:** Проверьте подключение к интернету. Google Translate API требует доступ к сети.

### Проблема: Неправильное исправление раскладки

**Решение:** Убедитесь, что текст содержит достаточно символов для определения раскладки (минимум 2-3 символа).

## 📄 Лицензия

MIT

## 👥 Авторы

- Claude Framework Team
- Contributions: Claude Code Assistant

---

**Версия:** 1.0.0
**Последнее обновление:** 02.11.2025

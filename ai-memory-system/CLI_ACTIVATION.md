# Активация AI Memory System для Claude Code CLI

## ✅ Выполнено

1. **Все сервисы запущены:**
   - Neo4j (bolt://localhost:7687) - пароль: password123
   - Qdrant (http://localhost:6333)
   - Ollama (http://localhost:11434)

2. **Создан MCP конфиг для CLI:**
   - Файл: `.claude/mcp-configs/bsl-memory-config.json`
   - Сервер: `ai-memory-bsl`
   - Python: 3.13.1

## 🔄 Необходимые действия

### Шаг 1: Перезапустите Claude Code CLI

Чтобы новый MCP сервер загрузился, нужно:

1. **Полностью выйдите** из текущей сессии Claude Code
2. **Перезапустите** Claude Code CLI
3. При запуске должно появиться сообщение о подключении `ai-memory-bsl`

### Шаг 2: Проверьте доступность инструментов

После перезапуска выполните в CLI:

```
Покажи список доступных MCP инструментов
```

Вы должны увидеть:

- ✅ `search_bsl_code` - семантический поиск по BSL коду
- ✅ `intelligent_search` - интеллектуальный анализ с контекстом
- ✅ `analyze_graph` - анализ графа зависимостей
- ✅ `get_search_history` - история поисков
- ✅ `clear_cache` - очистка кеша

### Шаг 3: Тестовый запрос

Попробуйте один из этих запросов:

```
Найди в BSL коде все процедуры с именем 'Запись'
```

```
Выполни интеллектуальный поиск: как работает система авторизации?
```

```
Проанализируй граф зависимостей для файла CommonModule.Пользователи
```

## ⚙️ Конфигурация

**Файл:** `.claude/mcp-configs/bsl-memory-config.json`

```json
{
  "mcpServers": {
    "ai-memory-bsl": {
      "command": "C:/Users/AlexT/AppData/Local/Programs/Python/Python313/python.exe",
      "args": [
        "D:/1C-Enterprise_Framework/ai-memory-system/mcp_server/server_fastmcp.py"
      ],
      "cwd": "D:/1C-Enterprise_Framework/ai-memory-system",
      "env": {
        "PYTHONIOENCODING": "utf-8",
        "PYTHONPATH": "D:/1C-Enterprise_Framework/ai-memory-system",
        "NEO4J_URI": "bolt://localhost:7687",
        "NEO4J_USER": "neo4j",
        "NEO4J_PASSWORD": "password123",
        "QDRANT_HOST": "localhost",
        "QDRANT_PORT": "6333",
        "OLLAMA_HOST": "http://localhost:11434"
      },
      "timeout": 30000
    }
  }
}
```

## 🔍 Диагностика

Если инструменты не появились:

1. **Проверьте логи запуска** Claude Code - должны быть сообщения о подключении MCP
2. **Проверьте сервисы:**
   ```bash
   docker ps | grep -E "neo4j|qdrant"
   curl http://localhost:11434/api/version
   ```
3. **Проверьте запуск сервера вручную:**
   ```bash
   cd D:/1C-Enterprise_Framework/ai-memory-system
   python mcp_server/server_fastmcp.py
   ```

## 📋 Добавление разрешений (опционально)

Если Claude будет спрашивать разрешения при каждом использовании, добавьте в `.claude/settings.local.json`:

```json
{
  "permissions": {
    "allow": [
      "mcp__ai-memory-bsl__search_bsl_code",
      "mcp__ai-memory-bsl__intelligent_search",
      "mcp__ai-memory-bsl__analyze_graph",
      "mcp__ai-memory-bsl__get_search_history",
      "mcp__ai-memory-bsl__clear_cache"
    ]
  }
}
```

## ⚠️ Важно

**Индексация кода:** Похоже, BSL код еще не проиндексирован в Qdrant. Если поиск не будет находить результаты, потребуется запустить индексацию файлов из `D:/1C-Enterprise_Framework`.

---

**Дата создания:** 2025-11-04
**Статус:** Готово к использованию после перезапуска CLI

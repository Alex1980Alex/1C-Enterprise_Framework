# Memory MCP Server

MCP (Model Context Protocol) server для интеграции долгосрочной памяти с Claude.

## Возможности

Предоставляет 6 инструментов (tools) для работы с памятью:

### 1. `save_conversation_fact`
Сохранить важный факт или сообщение в долгосрочную память.

**Параметры:**
- `content` (required) - Текст для сохранения
- `importance` - Оценка важности 0.0-1.0 (default: 0.5)
- `role` - Роль отправителя: user/assistant/system
- `has_code` - Содержит ли код (default: false)
- `metadata` - Дополнительные метаданные (optional)

**Пример:**
```json
{
  "content": "BSL индексация использует Qdrant для векторного поиска",
  "importance": 0.9,
  "has_code": false
}
```

### 2. `search_memory`
Поиск информации в памяти (semantic + text search).

**Параметры:**
- `query` (required) - Поисковый запрос
- `limit` - Макс. количество результатов (default: 10)
- `project_context` - Фильтр по проекту (optional)
- `min_score` - Минимальный score 0.0-1.0 (default: 0.5)

**Пример:**
```json
{
  "query": "векторизация в Qdrant",
  "limit": 5,
  "min_score": 0.6
}
```

### 3. `get_session_context`
Получить контекст текущей или указанной сессии.

**Параметры:**
- `session_id` - ID сессии (optional, использует текущую)
- `query` - Запрос для фокусировки контекста (optional)
- `max_messages` - Макс. сообщений в контексте (default: 20)
- `include_semantic` - Включить semantic matches (default: true)

**Пример:**
```json
{
  "query": "BSL parsing",
  "max_messages": 15
}
```

### 4. `start_memory_session`
Начать новую сессию памяти или возобновить существующую.

**Параметры:**
- `session_id` - ID сессии (генерируется если не указан)
- `project_context` - Контекст проекта (например, путь)
- `metadata` - Метаданные сессии (optional)

**Пример:**
```json
{
  "session_id": "work_session_001",
  "project_context": "1C-Enterprise_Framework",
  "metadata": {"user": "developer1"}
}
```

### 5. `get_project_summary`
Получить сводку по проекту.

**Параметры:**
- `project_context` - Идентификатор проекта (optional)
- `include_stats` - Включить статистику (default: true)

**Пример:**
```json
{
  "project_context": "1C-Enterprise_Framework",
  "include_stats": true
}
```

### 6. `get_important_messages`
Получить самые важные сообщения из памяти.

**Параметры:**
- `limit` - Макс. количество сообщений (default: 10)
- `min_importance` - Мин. важность (default: 0.7)
- `project_context` - Фильтр по проекту (optional)

**Пример:**
```json
{
  "limit": 5,
  "min_importance": 0.8
}
```

---

## Установка

### 1. Установить зависимости

```bash
pip install mcp anthropic-mcp
```

### 2. Настроить Claude Code

Добавить в `~/.claude/settings.json` (или эквивалент для Windows):

```json
{
  "mcpServers": {
    "memory": {
      "command": "python",
      "args": [
        "D:/1C-Enterprise_Framework/ai-memory-system/mcp/memory_server.py"
      ],
      "env": {
        "PYTHONIOENCODING": "utf-8"
      }
    }
  }
}
```

### 3. Проверить установку

Запустить Claude Code и проверить доступные tools:
```
/tools
```

Должен появиться список инструментов памяти.

---

## Использование

### Workflow 1: Сохранение важной информации

```
1. Начать сессию:
   use tool: start_memory_session
   {
     "project_context": "1C-Enterprise_Framework"
   }

2. Во время разговора сохранять важные факты:
   use tool: save_conversation_fact
   {
     "content": "BSL parsing использует tree-sitter для анализа",
     "importance": 0.9,
     "has_code": false
   }

3. Повторять шаг 2 для всех важных фактов
```

### Workflow 2: Поиск в памяти

```
Искать информацию в памяти:
use tool: search_memory
{
  "query": "как работает BSL parsing",
  "limit": 5
}

Результат: список релевантных сообщений с scores
```

### Workflow 3: Восстановление контекста

```
При начале новой сессии:
use tool: get_session_context
{
  "query": "векторизация",
  "max_messages": 20
}

Результат: контекст с temporal + semantic messages
```

---

## Архитектура

```
┌─────────────────────────────────────────────────┐
│              Claude AI (Client)                  │
└───────────────┬─────────────────────────────────┘
                │ MCP Protocol (stdio)
                ▼
┌─────────────────────────────────────────────────┐
│         Memory MCP Server (Python)               │
│                                                  │
│  Tools:                                          │
│  - save_conversation_fact                        │
│  - search_memory                                 │
│  - get_session_context                           │
│  - start_memory_session                          │
│  - get_project_summary                           │
│  - get_important_messages                        │
└───────────────┬─────────────────────────────────┘
                │
         ┌──────┼──────────┐
         ▼      ▼          ▼
┌──────────────┐ ┌──────────┐ ┌──────────────┐
│Conversation  │ │ Message  │ │  Context     │
│  Storage     │ │Vector-   │ │ Restoration  │
│             │ │ization   │ │             │
└──────────────┘ └──────────┘ └──────────────┘
         │              │              │
         ▼              ▼              ▼
┌──────────────┐ ┌──────────┐ ┌──────────────┐
│ TimescaleDB  │ │  Qdrant  │ │   Ollama     │
└──────────────┘ └──────────┘ └──────────────┘
```

---

## Примеры использования в Claude

### Пример 1: Обучение системы о проекте

```
User: Расскажи Claude о структуре 1C Framework

Claude:
[Uses save_conversation_fact multiple times]
✅ Saved: "1C Framework использует BSL для скриптов"
✅ Saved: "Конфигурация хранится в src/projects/configuration/"
✅ Saved: "Используется TimescaleDB для time-series данных"
```

### Пример 2: Поиск информации

```
User: Как работала векторизация, которую мы обсуждали?

Claude:
[Uses search_memory]
Query: "векторизация Qdrant"

Результаты:
1. [0.840] "Векторизация использует Ollama для создания embeddings..."
2. [0.720] "Qdrant хранит 768-dim vectors с COSINE distance..."
```

### Пример 3: Начало новой сессии с контекстом

```
User: Продолжим работу над памятью

Claude:
[Uses get_session_context]
📋 Восстановлен контекст:
- 15 messages из предыдущих сессий
- Semantic matches: векторизация, TimescaleDB, MCP
- Important: создание schema, тестирование сервисов
```

---

## Troubleshooting

### MCP Server не запускается

**Проблема**: Claude не видит memory tools

**Решение**:
1. Проверить путь к `memory_server.py` в settings.json
2. Убедиться что Python доступен в PATH
3. Проверить что установлены зависимости: `pip list | grep mcp`

### Database Connection Error

**Проблема**: "Failed to connect to ai_memory database"

**Решение**:
1. Убедиться что TimescaleDB запущен: `docker ps | grep timescale`
2. Проверить credentials в `DB_CONFIG`
3. Проверить что schema применена: `docker exec 1c-timescaledb psql -U ai_user -d ai_memory -c "\dt"`

### Qdrant Connection Error

**Проблема**: "Failed to connect to Qdrant"

**Решение**:
1. Проверить что Qdrant запущен: `docker ps | grep qdrant`
2. Проверить доступность: `curl http://localhost:6333`

---

## Advanced Usage

### Custom Session Management

```python
# Программно создать сессию с метаданными
{
  "session_id": "sprint_2_week_1",
  "project_context": "1C-Framework",
  "metadata": {
    "sprint": 2,
    "team": "backend",
    "goals": ["memory system", "MCP integration"]
  }
}
```

### Importance Scoring Guidelines

- **0.9-1.0**: Критически важная информация (решения, архитектура)
- **0.7-0.9**: Важная информация (код, алгоритмы)
- **0.5-0.7**: Средняя важность (обсуждения, идеи)
- **0.0-0.5**: Низкая важность (вспомогательная информация)

### Project Context Best Practices

Использовать структурированные имена:
- `1C-Framework` - основной проект
- `1C-Framework/memory-system` - подпроект
- `1C-Framework/BSL-indexing` - компонент

Это позволит фильтровать контекст на разных уровнях.

---

## Development

### Running Tests

```bash
# Test MCP server directly
cd ai-memory-system/mcp
python memory_server.py
# Should start stdio server

# Test with MCP inspector
mcp inspect memory_server.py
```

### Logging

Logs пишутся в stderr и доступны в Claude Code console.

Уровень логирования можно настроить:
```python
logging.basicConfig(level=logging.DEBUG)  # Для debugging
```

---

## Performance

### Benchmarks

| Operation | Avg Time | Notes |
|-----------|----------|-------|
| save_conversation_fact | ~10s | Includes vectorization |
| search_memory | ~8s | Semantic search + DB query |
| get_session_context | ~25s | Multiple queries + semantic |
| start_memory_session | <1s | DB insert only |
| get_project_summary | ~2s | DB aggregation |
| get_important_messages | <1s | Indexed query |

**Note**: Vectorization (Ollama) занимает большую часть времени.
Можно оптимизировать через batch processing или caching.

---

## License

Part of 1C-Enterprise Framework AI Memory System

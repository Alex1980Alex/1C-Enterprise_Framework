# Serena: Контекст работы с проектами

## 🎯 КРИТИЧЕСКИ ВАЖНО
**ВСЕГДА проверяй активный проект перед использованием Serena инструментов!**

## 📋 Автоматическая проверка контекста

### При каждом запросе пользователя выполняй:

1. **Проверь system-reminder на наличие путей к файлам**
   ```
   <system-reminder>
   The user opened the file d:\1C-Enterprise_Framework\src\projects\configuration\{PROJECT}\...
   </system-reminder>
   ```

2. **Извлеки имя проекта из пути:**
   - Паттерн: `configuration/{PROJECT_NAME}/...`
   - Примеры проектов: 251027_GKSTCPLK-1788, demo-accounting, 251024_GKSTCPLK-1743

3. **Прочитай текущий активный проект:**
   ```python
   read_memory("active_project_context")
   ```

4. **Если проект изменился:**
   - Обнови память `active_project_context`
   - Уведомь пользователя: "🔄 Переключился на проект: {new_project}"
   - Добавь запись в историю переключений

5. **При использовании Serena - используй ПОЛНЫЙ путь:**
   ```
   ✅ ПРАВИЛЬНО: src/projects/configuration/{active_project}/src/Documents/...
   ❌ НЕПРАВИЛЬНО: src/Documents/...
   ```

## 🔍 Быстрая проверка

```python
# Шаблон для быстрой проверки в начале каждого запроса
def check_project_context(user_message, system_reminder):
    # 1. Извлечь путь из system_reminder или сообщения
    path = extract_path(system_reminder) or extract_path(user_message)

    # 2. Извлечь проект из пути
    if path and "configuration/" in path:
        project = extract_project_from_path(path)

        # 3. Проверить активный проект
        active_project = get_active_project()  # из памяти

        # 4. Переключиться если нужно
        if project != active_project:
            switch_project(project)
            notify_user(f"🔄 Переключился на проект: {project}")

    return get_active_project()
```

## 📊 Доступные проекты

1. 250928_GKSTCPLK-1706
2. 251008_GKSTCPLK-1763
3. 251019_GKSTCPLK-1713
4. 251019_Ветка проекта RUDT-2021
5. 251020_GKSTCPLK-1817
6. 251024_GKSTCPLK-1743
7. 251025_Ошибка_20251025101027
8. 251027_GKSTCPLK-1788 ⭐ (по умолчанию)
9. demo-accounting

## 🚨 Правила работы с Serena

### ✅ ВСЕГДА делай:
- Проверяй активный проект перед использованием Serena
- Используй полные пути с именем проекта
- Обновляй память при смене проекта
- Информируй пользователя о переключении

### ❌ НИКОГДА не делай:
- Не используй относительные пути без указания проекта
- Не игнорируй system-reminder с путями
- Не предполагай активный проект
- Не забывай обновлять память после переключения

## 🔧 Примеры правильного использования

### Пример 1: Автоматическое переключение
```
# System-reminder
<system-reminder>
The user opened: src/projects/configuration/demo-accounting/src/Documents/...
</system-reminder>

# Claude действия:
1. Извлек проект: "demo-accounting"
2. Прочитал active_project_context → "251027_GKSTCPLK-1788"
3. Обнаружил смену проекта
4. Обновил память → "demo-accounting"
5. Ответил: "🔄 Переключился на проект: demo-accounting"
```

### Пример 2: Работа с Serena после переключения
```python
# Правильный вызов
find_symbol(
    name_path="/СформироватьДокумент",
    relative_path="src/projects/configuration/demo-accounting/src/Documents/ЗаказПокупателя/Ext/ManagerModule.bsl"
)

# Неправильный вызов (не сработает)
find_symbol(
    name_path="/СформироватьДокумент",
    relative_path="src/Documents/ЗаказПокупателя/Ext/ManagerModule.bsl"  # ❌ пропущен проект
)
```

## 📚 Связанные памяти

- `active_project_context` - текущий активный проект и история
- `serena_auto_project_switching` - алгоритмы переключения
- `project_overview` - общая структура проектов
- `serena_bsl_integration_status` - статус интеграции

## ✅ Чеклист перед каждой Serena операцией

- [ ] Проверил system-reminder
- [ ] Извлек имя проекта из пути
- [ ] Прочитал active_project_context
- [ ] Обновил активный проект (если изменился)
- [ ] Использую полный путь с именем проекта
- [ ] Информировал пользователя (если переключился)

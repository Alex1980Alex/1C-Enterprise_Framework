# MCP Server Configurations

Эта директория содержит конфигурации для MCP (Model Context Protocol) серверов, которые можно использовать с Claude Code.

## Доступные конфигурации

### 1a. Official Memory MCP Server (`@modelcontextprotocol/server-memory`)

**Префикс**: `mcp__memory__*`

**Назначение**: Graph-based хранилище для структурированных данных

**Инструменты**:
- `create_entities` - Создать сущности (entities)
- `create_relations` - Создать связи между сущностями
- `add_observations` - Добавить наблюдения к сущностям
- `delete_entities` - Удалить сущности
- `read_graph` - Прочитать весь граф
- `search_nodes` - Поиск узлов
- `open_nodes` - Открыть конкретные узлы

**Требования**: Нет (работает из коробки)

**Статус**: ✅ Готов к использованию

**Когда использовать**: Простые заметки, структурированные связи (проект → задача → файл)

---

### 1b. AI Memory MCP Server (`memory-config.json`)

**Префикс**: `mcp__memory-ai__*`

**Назначение**: Векторный поиск и долгосрочная память с AI-embeddings

**Инструменты**:
- `save_conversation_fact` - Сохранить важный факт в память
- `search_memory` - Семантический поиск в памяти
- `get_session_context` - Восстановить контекст сессии
- `start_memory_session` - Начать новую сессию памяти
- `get_project_summary` - Получить сводку по проекту
- `get_important_messages` - Получить важные сообщения

**Требования**:
- TimescaleDB (localhost:5432) - `docker start 1c-timescaledb`
- Qdrant (localhost:6333) - `docker start 1c-qdrant`
- Ollama (localhost:11434) с моделью nomic-embed-text
- Python с установленными зависимостями

**Статус**: ✅ Готов к использованию (требует запущенные Docker контейнеры)

**Когда использовать**: Семантический поиск, долгосрочная память между сессиями, работа с несколькими проектами

---

### 2. 1C Docs MCP Server (`docs-test-config.json`)

**Назначение**: Доступ к документации 1C Framework

**Инструменты**:
- Семантический поиск по документации
- Чтение документов

**Требования**:
- Node.js
- npx

**Статус**: ✅ Готов к использованию

---

### 3. Free Translate MCP Server (`free-translate-config.json`)

**Префикс**: `mcp__free-translate__*`

**Назначение**: Бесплатный перевод текста и исправление раскладки клавиатуры без API ключей

**Инструменты**:
- `translate` - Перевод текста с Google Translate API
- `translate_to_russian` - Быстрый перевод на русский
- `translate_to_english` - Быстрый перевод на английский
- `fix_keyboard_layout` - Исправление раскладки (lf → да, ghbdtn → привет)
- `convert_keyboard_layout` - Конвертация между EN ↔ RU
- `detect_keyboard_layout` - Определение раскладки текста

**Требования**:
- Node.js
- npm (зависимости установлены)
- Подключение к интернету (для перевода)

**Статус**: ✅ Готов к использованию (протестировано 02.11.2025)

**Документация**: `FREE-TRANSLATE-ACTIVATION.md`

---

## Как использовать

### Вариант 1: Через Claude Code CLI

```bash
# Добавить конфигурацию в Claude Code
claude mcp add memory .claude/mcp-configs/memory-config.json

# Проверить установленные серверы
claude mcp list

# Удалить сервер (если нужно)
claude mcp remove memory
```

### Вариант 2: Вручную через settings.json

Скопируйте содержимое нужного файла конфигурации в ваш `~/.claude/settings.json` (или `%APPDATA%/.claude/settings.json` для Windows).

**Пример**:
```json
{
  "mcpServers": {
    "memory": {
      "command": "python",
      "args": ["D:/1C-Enterprise_Framework/ai-memory-system/mcp/memory_server.py"],
      "env": {"PYTHONIOENCODING": "utf-8"}
    }
  }
}
```

### Вариант 3: Автоматически при запуске (рекомендуется для проекта)

Claude Code автоматически загружает конфигурации MCP из:
- `.claude/mcp-configs/` в корне проекта
- Глобальный `~/.claude/settings.json`

Файлы конфигурации в `.claude/mcp-configs/` имеют приоритет над глобальными настройками.

---

## Проверка работоспособности

### Official Memory MCP Server

```bash
# Готов к работе сразу после запуска Claude Code
# Инструменты: mcp__memory__*
```

### AI Memory MCP Server

```bash
# Проверить что все сервисы запущены
docker ps | grep -E "timescale|qdrant"

# Проверить Ollama
curl http://localhost:11434/api/version
ollama list | grep nomic-embed

# Проверить Qdrant
curl http://localhost:6333/collections

# Тестовый запуск MCP сервера (опционально)
cd ai-memory-system/mcp
python test_server_import.py
```

Ожидаемый результат:
```
✅ Docker: TimescaleDB (healthy), Qdrant (running)
✅ Ollama: v0.12.7, nomic-embed-text installed
✅ Qdrant: collections ready
```

### В Claude Code

После перезапуска Claude Code:
1. Выполнить команду `/tools`
2. Проверить доступность:
   - `mcp__memory__*` (graph-based)
   - `mcp__memory-ai__*` (vectorный поиск)
3. Попробовать:
   - Graph: "Claude, создай entity для проекта 1C-Framework"
   - AI: "Claude, начни новую сессию памяти для проекта"

---

## Troubleshooting

### AI Memory Server не запускается

**Проблема**: `mcp__memory-ai__*` инструменты недоступны

**Решение**:
```bash
# 1. Проверить Docker контейнеры
docker ps | grep -E "timescale|qdrant"

# Если не запущены:
docker start 1c-timescaledb
docker start 1c-qdrant

# 2. Проверить Ollama
curl http://localhost:11434/api/version
ollama pull nomic-embed-text  # если модель не установлена

# 3. Проверить Python зависимости
python -c "import mcp; import psycopg2; import qdrant_client; print('OK')"

# 4. Перезапустить Claude Code полностью
```

### Ошибка: "Failed to connect to TimescaleDB"
```bash
# Проверить статус
docker logs 1c-timescaledb --tail 20

# Перезапустить
docker restart 1c-timescaledb

# Проверить доступность
docker exec 1c-timescaledb psql -U ai_user -d ai_memory -c "SELECT 1;"
```

### Ошибка: "Failed to connect to Qdrant"
```bash
# Проверить статус
docker logs 1c-qdrant --tail 20

# Перезапустить
docker restart 1c-qdrant

# Проверить API
curl http://localhost:6333/collections
```

### Ошибка: "Ollama model not found"
```bash
# Скачать модель
ollama pull nomic-embed-text

# Проверить
ollama list | grep nomic-embed
```

### Official Memory работает, AI Memory - нет

Это нормально! Официальный Memory не требует дополнительных сервисов. AI Memory требует запущенные Docker контейнеры и Ollama. Вы можете использовать только Official Memory, если не нужен векторный поиск.

---

## Добавление нового MCP сервера

1. Создайте файл конфигурации в `.claude/mcp-configs/`:
```json
{
  "mcpServers": {
    "your-server-name": {
      "command": "python",  // или "node", "npx", etc.
      "args": ["path/to/server.py"],
      "env": {
        // Переменные окружения
      }
    }
  }
}
```

2. Добавьте разрешения для инструментов в `.claude/settings.local.json`:
```json
{
  "permissions": {
    "allow": [
      "mcp__your-server-name__tool1",
      "mcp__your-server-name__tool2"
    ]
  }
}
```

3. Перезапустите Claude Code

4. Проверьте через `/tools`

---

## Безопасность

- Не коммитьте файлы конфигурации с секретными данными (пароли, API ключи)
- Используйте переменные окружения для чувствительных данных
- Проверяйте разрешения в `settings.local.json` перед добавлением новых инструментов

---

## Документация

- Memory MCP Server: `ai-memory-system/mcp/README.md`
- Общая документация по MCP: https://docs.claude.com/mcp
- Создание MCP серверов: https://docs.claude.com/mcp/creating-servers

---

**Последнее обновление**: 2025-10-31

---

## AI Memory System для BSL кода (обновлено 2025-11-04)

### 🎯 Что это?

MCP сервер для интеллектуального поиска и анализа BSL кода с использованием:
- **Neo4j** - граф зависимостей между модулями
- **Qdrant** - векторный поиск по коду
- **Ollama** - LLM для семантического анализа

### 📋 Быстрый старт

1. **Все сервисы уже запущены:**
   - Neo4j: `bolt://localhost:7687` (пароль: password123)
   - Qdrant: `http://localhost:6333`
   - Ollama: `http://localhost:11434`

2. **Конфигурация создана:**
   - Файл: `.claude/mcp-configs/bsl-memory-config.json`
   - Сервер: `ai-memory-bsl`

3. **Перезапустите Claude Code CLI** для загрузки нового MCP сервера

4. **Проверьте инструменты:**
   ```
   Покажи список доступных MCP инструментов
   ```

### 🔧 Доступные инструменты

- `search_bsl_code` - базовый семантический поиск
- `intelligent_search` - интеллектуальный многомерный поиск
- `analyze_graph` - анализ графа зависимостей
- `get_search_history` - история запросов
- `clear_cache` - очистка кеша

### 📖 Полная документация

См. файл: `D:/1C-Enterprise_Framework/ai-memory-system/CLI_ACTIVATION.md`


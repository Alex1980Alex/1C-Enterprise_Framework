# Free Translate MCP Server - Инструкция по активации

## ✅ Статус установки

**Установлено:** ✅
**Настроено:** ✅
**Протестировано:** ✅

---

## 📦 Компоненты

### 1. MCP Сервер
- **Расположение:** `D:/1C-Enterprise_Framework/mcp-free-translate/`
- **Конфигурация:** `.claude/mcp-configs/free-translate-config.json`
- **Тестовый скрипт:** `mcp-free-translate/test-keyboard-layouts.js`

### 2. Доступные инструменты

| Инструмент | Префикс | Описание |
|------------|---------|----------|
| `translate` | `mcp__free-translate__translate` | Перевод текста с auto-определением языка |
| `translate_to_russian` | `mcp__free-translate__translate_to_russian` | Быстрый перевод на русский |
| `translate_to_english` | `mcp__free-translate__translate_to_english` | Быстрый перевод на английский |
| `fix_keyboard_layout` | `mcp__free-translate__fix_keyboard_layout` | Исправление раскладки (lf → да) |
| `convert_keyboard_layout` | `mcp__free-translate__convert_keyboard_layout` | Конвертация между EN ↔ RU |
| `detect_keyboard_layout` | `mcp__free-translate__detect_keyboard_layout` | Определение раскладки текста |

---

## 🚀 Активация MCP сервера

### Способ 1: Автоматическая активация (рекомендуется)

MCP сервер автоматически активируется при запуске Claude Code, если:

1. ✅ Существует файл конфигурации: `.claude/mcp-configs/free-translate-config.json`
2. ✅ Добавлены разрешения в: `.claude/settings.local.json`

**Статус:** Оба условия выполнены!

### Способ 2: Ручная активация через Claude Code CLI

```bash
# Добавить MCP сервер
claude mcp add free-translate D:/1C-Enterprise_Framework/.claude/mcp-configs/free-translate-config.json

# Проверить список активных MCP серверов
claude mcp list

# Удалить (если нужно)
claude mcp remove free-translate
```

---

## ✅ Проверка работоспособности

### 1. Тест локально (без Claude Code)

```bash
cd D:/1C-Enterprise_Framework/mcp-free-translate
node test-keyboard-layouts.js
```

**Ожидаемый результат:**
```
=== Тест функций раскладки клавиатуры ===
✅ Простое слово: "lf" → "да"
✅ Приветствие: "ghbdtn" → "привет"
✅ Компьютер: "rjvgm.nth" → "компьютер"
...
```

### 2. Тест в Claude Code

После перезапуска Claude Code выполните:

```
/tools
```

Проверьте наличие инструментов с префиксом `mcp__free-translate__*`:
- ✅ `mcp__free-translate__fix_keyboard_layout`
- ✅ `mcp__free-translate__translate`
- ✅ `mcp__free-translate__convert_keyboard_layout`
- ✅ `mcp__free-translate__detect_keyboard_layout`

### 3. Практический тест

Попросите Claude:

```
Claude, исправь раскладку: "ghbdtn"
```

Ожидаемый результат:
```
Исправленный текст: "привет"
```

---

## 📋 Результаты тестирования

### Тест от 02.11.2025

```bash
$ node test-keyboard-layouts.js

=== Тест функций раскладки клавиатуры ===

1. Тест функции detectLayout():
  "lf" -> layout: en
  "ghbdtn" -> layout: en
  "да" -> layout: ru
  "Hello World" -> layout: en

2. Тест функции fixLayout() (auto):
  ✅ Простое слово: "lf" → "да" (confidence: 0.95)
  ✅ Приветствие: "ghbdtn" → "привет" (confidence: 0.95)
  ✅ Компьютер: "rjvgm.nth" → "компьютер" (confidence: 0.95)
  ✅ Обратное преобразование: "да" → "lf" (confidence: 0.95)
  ✅ С пробелами: "Hello World" → "Руддщ Цщкдв" (confidence: 0.95)

3. Тест функции convertLayout():
  EN -> RU: "hello" -> "руддщ"
  RU -> EN: "привет" -> "ghbdtn"

=== Все тесты завершены ===
```

**Статус:** ✅ Все тесты прошли успешно!

---

## 🔧 Конфигурация

### Файл конфигурации MCP сервера

**Расположение:** `.claude/mcp-configs/free-translate-config.json`

```json
{
  "mcpServers": {
    "free-translate": {
      "command": "node",
      "args": ["D:/1C-Enterprise_Framework/mcp-free-translate/index.js"]
    }
  }
}
```

### Разрешения в settings.local.json

**Расположение:** `.claude/settings.local.json`

```json
{
  "permissions": {
    "allow": [
      "mcp__free-translate__translate",
      "mcp__free-translate__translate_to_russian",
      "mcp__free-translate__translate_to_english",
      "mcp__free-translate__fix_keyboard_layout",
      "mcp__free-translate__convert_keyboard_layout",
      "mcp__free-translate__detect_keyboard_layout"
    ]
  }
}
```

---

## 🎯 Примеры использования

### Пример 1: Исправление раскладки

**Запрос:**
```
Claude, исправь раскладку для "rjvgm.nth"
```

**Ответ Claude (с использованием MCP):**
```json
{
  "original": "rjvgm.nth",
  "corrected": "компьютер",
  "from_layout": "en",
  "to_layout": "ru",
  "confidence": 0.95
}
```

### Пример 2: Перевод текста

**Запрос:**
```
Claude, переведи "Hello World" на русский
```

**Ответ Claude (с использованием MCP):**
```json
{
  "original": "Hello World",
  "translated": "Привет мир",
  "from": "en",
  "to": "ru",
  "confidence": 1.0
}
```

### Пример 3: Определение раскладки

**Запрос:**
```
Claude, определи раскладку текста "ghbdtn"
```

**Ответ Claude (с использованием MCP):**
```json
{
  "text": "ghbdtn",
  "detected_layout": "en"
}
```

---

## 🐛 Устранение неполадок

### Проблема 1: MCP сервер не запускается

**Симптомы:**
- Инструменты `mcp__free-translate__*` не видны в `/tools`
- Ошибка при запуске Claude Code

**Решение:**
```bash
# 1. Проверить установку зависимостей
cd D:/1C-Enterprise_Framework/mcp-free-translate
npm install

# 2. Проверить файл конфигурации
cat .claude/mcp-configs/free-translate-config.json

# 3. Перезапустить Claude Code полностью
```

### Проблема 2: Ошибка при переводе

**Симптомы:**
- Ошибка "Translation error: ..."

**Решение:**
- Проверьте подключение к интернету
- Google Translate API требует доступ к сети

### Проблема 3: Неправильное исправление раскладки

**Симптомы:**
- Раскладка исправляется неправильно

**Решение:**
- Убедитесь, что текст содержит минимум 2-3 символа
- Для смешанных раскладок используйте `target_layout` параметр

---

## 📚 Дополнительная документация

- **Полная документация:** `mcp-free-translate/README.md`
- **Исходный код:** `mcp-free-translate/index.js`
- **Логика раскладок:** `mcp-free-translate/keyboard-layouts.js`
- **Тесты:** `mcp-free-translate/test-keyboard-layouts.js`

---

## ✅ Чек-лист активации

- [x] Установлены зависимости (`npm install`)
- [x] Создан файл конфигурации MCP сервера
- [x] Добавлены разрешения в `settings.local.json`
- [x] Протестирован локально (`node test-keyboard-layouts.js`)
- [ ] Перезапущен Claude Code
- [ ] Проверен через `/tools` в Claude Code
- [ ] Выполнен практический тест

---

**Версия:** 1.0.0
**Дата:** 02.11.2025
**Статус:** ✅ Готов к использованию

**Примечание:** После перезапуска Claude Code все инструменты будут доступны автоматически!

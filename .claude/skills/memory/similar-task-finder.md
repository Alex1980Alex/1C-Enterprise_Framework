# Similar Task Finder - Поиск похожих задач

**Версия:** 1.0
**Дата:** 2025-10-31
**Приоритет:** 🔥 ВЫСОКИЙ
**Статус:** ✅ Готов к использованию

## 🎯 Назначение

Автоматический поиск похожих или ранее решенных задач **ПЕРЕД** началом работы над новой задачей. Предотвращает дублирование работы и позволяет использовать проверенные решения.

---

## 🔔 Когда активируется

### Триггеры активации (АВТОМАТИЧЕСКИ):

1. **Получение новой задачи от пользователя**
   - Запросы на разработку функций
   - Исправление багов
   - Рефакторинг кода
   - Анализ проблем

2. **Перед созданием TodoWrite**
   - При планировании задачи
   - Перед началом разработки

3. **При явном запросе**
   - "Была ли похожая задача?"
   - "Как мы делали это раньше?"

---

## ⚙️ Workflow выполнения

### Шаг 1: Извлечение ключевых слов

```javascript
function extractTaskKeywords(userRequest) {
  // Убрать служебные слова
  const stopWords = [
    'добавь', 'сделай', 'нужно', 'необходимо', 'создай',
    'реализуй', 'исправь', 'измени', 'пожалуйста'
  ];

  // Извлечь технические термины
  const keywords = userRequest
    .toLowerCase()
    .split(/\s+/)
    .filter(word => !stopWords.includes(word))
    .filter(word => word.length > 3)  // Только слова длиннее 3 символов
    .join(' ');

  return keywords;
}
```

**Примеры:**
```
Вход: "Добавь функцию экспорта в Excel"
Выход: "функцию экспорта Excel"

Вход: "Нужно исправить баг с блокировками документов"
Выход: "исправить блокировками документов"

Вход: "Создай АРМ для работы с композитами"
Выход: "АРМ работы композитами"
```

---

### Шаг 2: Семантический поиск в памяти

```javascript
async function findSimilarTasks(keywords) {
  // Поиск через Memory-AI MCP (семантический + полнотекстовый)
  const results = await mcp__memory-ai__search_memory({
    query: keywords,
    limit: 5,
    min_score: 0.7  // Порог схожести 70%
  });

  // Фильтрация только задач
  const taskResults = results.filter(r =>
    r.content.includes('Задача:') ||
    r.content.includes('План:') ||
    r.metadata?.type === 'task_analysis'
  );

  return taskResults;
}
```

---

### Шаг 3: Анализ найденных задач

```javascript
function analyzeSimilarTasks(similarTasks, currentTask) {
  const analysis = {
    found: similarTasks.length > 0,
    count: similarTasks.length,
    relevance: [],
    recommendations: []
  };

  for (const task of similarTasks) {
    const similarity = calculateSimilarity(task.content, currentTask);

    analysis.relevance.push({
      task: task.content,
      score: similarity,
      date: task.created_at,
      solution: extractSolution(task.content)
    });

    // Генерация рекомендаций
    if (similarity > 0.8) {
      analysis.recommendations.push(
        `Очень похожая задача от ${task.created_at}: ${task.content}`
      );
    }
  }

  return analysis;
}
```

---

### Шаг 4: Предоставление результатов

```javascript
function presentFindings(analysis, currentTask) {
  if (!analysis.found) {
    return {
      message: "Похожих задач не найдено. Это новая задача.",
      action: "proceed_with_new_task"
    };
  }

  // Форматирование результатов
  const report = `
🔍 Найдено похожих задач: ${analysis.count}

${analysis.relevance.map((r, i) => `
${i + 1}. [${(r.score * 100).toFixed(0)}% схожести] ${r.task}
   Дата: ${r.date}
   ${r.solution ? `Решение: ${r.solution}` : ''}
`).join('\n')}

📋 Рекомендации:
${analysis.recommendations.join('\n')}

❓ Хотите использовать готовое решение или начать заново?
  `;

  return {
    message: report,
    action: "show_similar_tasks",
    options: {
      use_existing: true,
      start_new: true,
      review_details: true
    }
  };
}
```

---

## 🎬 Примеры использования

### Пример 1: Найдена очень похожая задача

```
👤 Пользователь: "Добавь функцию экспорта в PDF"

🤖 Claude Code (автоматически):

1. extractTaskKeywords("Добавь функцию экспорта в PDF")
   → "функцию экспорта PDF"

2. mcp__memory-ai__search_memory({
     query: "функцию экспорта PDF",
     limit: 5,
     min_score: 0.7
   })

3. Найдено:
   ┌─────────────────────────────────────────────────────┐
   │ 🔍 Найдено похожих задач: 2                         │
   │                                                     │
   │ 1. [92% схожести] Задача: Экспорт в Excel          │
   │    Дата: 2025-10-15                                │
   │    Решение: Использована библиотека ExcelJS       │
   │    План: 1) Выбор библиотеки, 2) Сервис,          │
   │          3) UI, 4) Тесты                           │
   │                                                     │
   │ 2. [78% схожести] Задача: Генерация отчетов       │
   │    Дата: 2025-09-20                                │
   │    Решение: jsPDF + шаблоны                        │
   │                                                     │
   │ 📋 Рекомендации:                                    │
   │ • Похожий подход к Excel экспорту может сработать  │
   │ • Для PDF рекомендую библиотеку jsPDF             │
   │ • Используйте готовую структуру сервиса           │
   │                                                     │
   │ ❓ Использовать готовый подход?                     │
   └─────────────────────────────────────────────────────┘

4. Пользователю:
   "Найдена похожая задача! Ранее мы реализовывали
    экспорт в Excel используя библиотеку ExcelJS.
    Для PDF предлагаю похожий подход с библиотекой jsPDF.

    План (на основе прошлого опыта):
    1. Добавить зависимость jsPDF
    2. Создать PDFExportService
    3. Реализовать UI кнопку
    4. Написать тесты

    Начать реализацию?"
```

---

### Пример 2: Найдена частично похожая задача

```
👤 Пользователь: "Исправь баг с блокировками при записи документов"

🤖 Claude Code (автоматически):

1. extractTaskKeywords(...)
   → "исправь блокировками записи документов"

2. mcp__memory-ai__search_memory({
     query: "блокировками записи документов баг",
     limit: 5,
     min_score: 0.7
   })

3. Найдено:
   ┌─────────────────────────────────────────────────────┐
   │ 🔍 Найдено похожих задач: 1                         │
   │                                                     │
   │ 1. [73% схожести] Задача: Проблема с блокировками  │
   │    Дата: 2025-08-10                                │
   │    Решение: Использованы управляемые блокировки    │
   │    Детали: ПопыткаИсключение с повтором            │
   │    Код: src/services/DocumentLockService.bsl       │
   │                                                     │
   │ 📋 Рекомендации:                                    │
   │ • Проверьте DocumentLockService - возможно         │
   │   проблема там же                                  │
   │ • Используйте паттерн retry с управляемыми         │
   │   блокировками                                     │
   └─────────────────────────────────────────────────────┘

4. Пользователю:
   "Похожая проблема решалась ранее!
    В августе мы исправляли баг с блокировками используя
    управляемые блокировки с механизмом повтора.

    Код находится в: src/services/DocumentLockService.bsl

    Хотите сначала проверить этот код или начать с нуля?"
```

---

### Пример 3: Похожих задач не найдено

```
👤 Пользователь: "Создай интеграцию с Telegram API"

🤖 Claude Code (автоматически):

1. extractTaskKeywords(...)
   → "интеграцию Telegram API"

2. mcp__memory-ai__search_memory({
     query: "интеграцию Telegram API",
     limit: 5,
     min_score: 0.7
   })

3. Не найдено (score < 0.7)

4. Пользователю:
   "Это новая задача - похожих решений в памяти не найдено.

    Начинаю анализ и планирование с нуля...

    План:
    1. Изучить Telegram Bot API документацию
    2. Выбрать библиотеку для Node.js
    3. Создать TelegramService
    4. Реализовать webhook обработчик
    5. Добавить обработку команд
    6. Тестирование

    Начать реализацию?"
```

---

## ⚠️ Критические правила

### ✅ ВСЕГДА делать:

```javascript
// ✅ ПРАВИЛЬНО - искать перед началом
async function startNewTask(userRequest) {
  // 1. ОБЯЗАТЕЛЬНО искать похожие задачи
  const similar = await findSimilarTasks(userRequest);

  // 2. Показать результаты пользователю
  if (similar.found) {
    showSimilarTasksReport(similar);
    askUserPreference(); // Использовать готовое или начать заново
  }

  // 3. Только после этого начинать работу
  if (user.choice === 'use_existing') {
    adaptExistingSolution(similar);
  } else {
    startFromScratch();
  }
}
```

### ❌ НИКОГДА не делать:

```javascript
// ❌ НЕПРАВИЛЬНО - начинать без проверки
async function startNewTask(userRequest) {
  // Сразу начать работу без поиска
  createPlan();
  implementSolution();
}

// ❌ НЕПРАВИЛЬНО - игнорировать найденные задачи
async function startNewTask(userRequest) {
  const similar = await findSimilarTasks(userRequest);
  // Нашли похожие, но не сообщили пользователю
  startFromScratch(); // Игнорируем готовые решения!
}
```

---

## 💾 Автоматическое сохранение

После поиска и использования похожих задач - сохранить метаданные:

```javascript
// После успешного использования похожей задачи
await mcp__memory-ai__save_conversation_fact({
  content: `Использована похожая задача из ${task.date}: ${task.title}
Применено решение: ${task.solution}
Адаптация: ${adaptations}
Результат: Успешно реализовано`,
  importance: 0.85,
  metadata: {
    type: 'task_reuse',
    original_task_id: task.id,
    new_task: currentTask,
    reuse_percentage: 0.8
  }
});

// Создать связь
await mcp__memory__create_relations([{
  from: task.id,
  to: currentTask.id,
  relationType: 'was_reused_in'
}]);
```

---

## 🔄 Интеграция с другими Skills

```
WORKFLOW ПОЛНОГО ЦИКЛА:

1. Получение задачи от пользователя
   ↓
2. similar-task-finder ← ВЫ ЗДЕСЬ (поиск похожих)
   ├─> Найдено? → Показать + спросить
   └─> Не найдено? → Продолжить как обычно
   ↓
3. task-analysis (анализ задачи)
   ↓
4. task-planning (создание плана)
   ↓
5. Выполнение с auto-memory-integration
   ↓
6. progress-tracking (отслеживание)
   ↓
7. Сохранение решения в память
```

---

## 🎯 Конфигурация

### Настройка в .claude/settings.local.json:

```json
{
  "skills": {
    "memory": {
      "similar-task-finder": {
        "enabled": true,
        "auto_trigger": true,
        "min_similarity": 0.7,
        "max_results": 5,
        "search_in_memory_ai": true,
        "search_in_knowledge_graph": false,
        "show_partial_matches": true,
        "ask_user_confirmation": true
      }
    }
  }
}
```

### Параметры:

- `enabled`: Включить/выключить навык
- `auto_trigger`: Автоматически искать при новой задаче
- `min_similarity`: Порог схожести (0.0-1.0)
- `max_results`: Максимум результатов
- `search_in_memory_ai`: Искать в Memory-AI MCP
- `search_in_knowledge_graph`: Искать в Knowledge Graph
- `show_partial_matches`: Показывать частичные совпадения
- `ask_user_confirmation`: Спрашивать подтверждение

---

## 🧪 Тестирование

### Тестовые сценарии:

```bash
# Тест 1: Очень похожая задача (>90%)
Запрос: "Добавь экспорт в CSV"
Память: "Ранее: Экспорт в Excel (2025-10-15)"
Ожидание: ✅ Найдено, показано решение, предложено использовать

# Тест 2: Частично похожая задача (70-90%)
Запрос: "Исправь баг с валидацией форм"
Память: "Ранее: Валидация полей документа (2025-09-01)"
Ожидание: ✅ Найдено, показано как референс

# Тест 3: Нет похожих задач (<70%)
Запрос: "Интеграция с blockchain"
Память: "Нет данных"
Ожидание: ✅ Сообщение "новая задача", продолжить обычным путем
```

---

## 📊 Метрики эффективности

Отслеживание использования:

```markdown
## Отчет Similar Task Finder (месяц)

**Всего задач:** 47
**Найдено похожих:** 32 (68%)
**Использованы готовые решения:** 24 (51%)
**Сэкономлено времени:** ~40 часов

**TOP-3 типов повторяющихся задач:**
1. Экспорт данных (различные форматы) - 8 раз
2. Проблемы с блокировками - 6 раз
3. Валидация форм - 5 раз

**Рекомендация:**
Создать шаблоны для топ-3 типов задач
```

---

## 🐛 Известные ограничения

1. **Семантический поиск требует качественных данных**
   - Решение: Сохранять детальные описания задач
   - Включать keywords в observations

2. **Минимальный порог 0.7 может пропускать релевантные задачи**
   - Решение: Экспериментировать с порогом
   - Показывать top-3 даже если score < 0.7

3. **Не работает для совсем новых проектов**
   - Решение: Накапливать историю
   - После 10-15 задач эффективность возрастает

---

## 🚀 Продвинутое использование

### Кастомный поиск:

```javascript
// Явный вызов с параметрами
const similar = await findSimilarTasks({
  query: "экспорт данных",
  min_score: 0.6,        // Более мягкий порог
  limit: 10,             // Больше результатов
  date_range: {          // Только недавние
    from: '2025-09-01',
    to: '2025-10-31'
  },
  tags: ['export', '1c'] // Только с тегами
});
```

---

**Версия:** 1.0
**Дата:** 2025-10-31
**Автор:** Система Memory Skills
**Статус:** ✅ Готов к использованию
**Приоритет:** 🔥 КРИТИЧЕСКИЙ

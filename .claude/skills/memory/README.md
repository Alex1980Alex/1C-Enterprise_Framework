# Memory Skills - Навыки для работы с AI Memory

Эта папка содержит все skills (навыки) для интеллектуальной работы с системой AI Memory.

## 📋 Содержание

### ✅ Активные навыки

#### 1. auto-memory-check.md
**Назначение:** Автоматическая проверка памяти перед ответом
**Статус:** ✅ Активен

**Когда активируется:**
- При прямых вопросах (что, где, как, почему, и т.д.)
- При запросах информации ("помнишь...", "ты знаешь...")
- При контекстных запросах (ссылки на прошлое)

**Функции:**
- Извлечение ключевых слов из вопроса
- Семантический поиск в памяти
- Использование найденной информации в ответе
- Приоритизация источников (Framework Docs → Dev Standards → Memory → Web)

**Пример использования:**
```javascript
// Автоматически при вопросе пользователя
Пользователь: "какой я сейчас смотрю сериал?"

Claude (автоматически):
1. extractKeywords("сериал смотрю") → "сериал смотрю"
2. mcp__memory__search_nodes("сериал смотрю")
3. Найдено: "Смотрит сериал 'Ведьмак'"
4. Ответ: "Вы сейчас смотрите сериал 'Ведьмак'"
```

**Критические правила:**
- ❌ НИКОГДА не отвечать "не знаю" без проверки памяти
- ✅ ВСЕГДА проверять память перед ответом
- ✅ ВСЕГДА сохранять новые знания после ответа

---

#### 2. auto-memory-integration.md
**Назначение:** Интеграция с Memory MCP для сохранения/извлечения знаний
**Статус:** ✅ Активен

**Когда использовать:**
1. **При получении новой информации:**
   - Чтение документации
   - Анализ кода
   - Результаты поиска
   - Ответы от внешних сервисов

2. **Перед ответом на вопросы:**
   - Поиск релевантной информации
   - Проверка предыдущего контекста
   - Извлечение сохраненных знаний

3. **При работе с проектом:**
   - Сохранение архитектурных решений
   - Документирование API
   - Фиксация бизнес-логики

**Процесс работы:**

```javascript
// 1. Проверка памяти перед ответом
mcp__memory__search_nodes({query: "релевантный запрос"})

// 2. Сохранение важной информации
mcp__memory__create_entities({
  entities: [{
    name: "Auth Module",
    entityType: "code_module",
    observations: [
      "Location: src/auth/",
      "Purpose: JWT authentication",
      "Created: 2025-10-31"
    ]
  }]
})

// 3. Обновление существующей информации
mcp__memory__add_observations({
  observations: [{
    entityName: "Auth Module",
    contents: ["Added refresh token rotation (2025-10-31)"]
  }]
})
```

**Категории сохраняемой информации:**
- Код и архитектура
- Документация
- Контекст проекта
- Решения и обоснования

**Рекомендуемые типы сущностей:**
- `project` - проект в целом
- `module` - модуль/компонент
- `api_endpoint` - API endpoint
- `configuration` - настройка
- `tool` - инструмент
- `concept` - концепция
- `issue` - проблема
- `decision` - решение
- `dependency` - зависимость

---

## 🚀 Планируемые навыки

### task-analysis.md
**Статус:** 📝 Планируется
**Назначение:** Автоматический анализ новых задач

**Функции:**
- Извлечение требований из запроса
- Определение типа задачи (feature, bugfix, refactoring)
- Оценка сложности
- Сохранение анализа в память

**Пример:**
```javascript
Пользователь: "Добавь функцию экспорта в Excel"

Skill автоматически:
1. Анализ: Feature implementation
2. Ключевые слова: Excel, экспорт, функция
3. Сохранение в память:
   mcp__memory-ai__save_conversation_fact({
     content: "Задача: Добавить экспорт в Excel\nТип: Feature\nПриоритет: Средний",
     importance: 0.8
   })
```

---

### task-planning.md
**Статус:** 📝 Планируется
**Назначение:** Сохранение планов задач

**Функции:**
- Сохранение todo списка в память
- Связывание плана с анализом задачи
- Отслеживание зависимостей между шагами

**Пример:**
```javascript
// При создании TodoWrite
Skill автоматически:
1. Извлечь все todo пункты
2. Сохранить план:
   mcp__memory-ai__save_conversation_fact({
     content: "План: 1. Библиотека Excel, 2. Сервис экспорта, 3. UI кнопка, 4. Тесты",
     importance: 0.85,
     metadata: {
       task_id: "Task_2025_10_31_Excel",
       total_steps: 4
     }
   })
```

---

### progress-tracking.md
**Статус:** 📝 Планируется
**Назначение:** Отслеживание прогресса выполнения

**Функции:**
- Сохранение промежуточных результатов
- Обновление статуса в памяти
- Фиксация блокеров и решений

**Пример:**
```javascript
// При завершении todo пункта
Skill автоматически:
1. Зафиксировать результат:
   mcp__memory-ai__save_conversation_fact({
     content: "Шаг 1 завершен: Выбрана библиотека ExcelJS\nПричина: Поддержка стилей",
     importance: 0.75,
     metadata: {
       step: 1,
       completed: true
     }
   })
```

---

### similar-task-finder.md
**Статус:** 📝 Планируется (ВЫСОКИЙ ПРИОРИТЕТ)
**Назначение:** Поиск похожих решенных задач

**Функции:**
- Автоматический поиск перед началом работы
- Извлечение ключевых слов из задачи
- Семантический поиск в истории
- Предложение готовых решений

**Пример:**
```javascript
// Перед началом новой задачи
Skill автоматически:
1. Извлечь ключевые слова: "экспорт Excel"
2. Поиск в памяти:
   const similar = await mcp__memory-ai__search_memory({
     query: "экспорт Excel файл",
     limit: 5,
     min_score: 0.7
   })

3. Если найдено:
   "Найдена похожая задача из прошлого: Экспорт в PDF (2025-10-15)"
   "Используемое решение: библиотека jsPDF"
   "Применить похожий подход?"
```

---

## 🎯 Интеграция навыков

Навыки работают вместе для полного цикла:

```
1. ПОЛУЧЕНИЕ ЗАДАЧИ
   ├─> task-analysis.md (анализ)
   └─> similar-task-finder.md (поиск похожих)

2. ПЛАНИРОВАНИЕ
   └─> task-planning.md (сохранение плана)

3. ВЫПОЛНЕНИЕ
   ├─> auto-memory-check.md (проверка контекста)
   ├─> auto-memory-integration.md (сохранение результатов)
   └─> progress-tracking.md (отслеживание прогресса)

4. ЗАВЕРШЕНИЕ
   └─> auto-memory-integration.md (финальное сохранение)
```

---

## ⚙️ Конфигурация

### Настройка в .claude/settings.local.json

```json
{
  "skills": {
    "memory": {
      "auto-memory-check": {
        "enabled": true,
        "auto_trigger": true,
        "priority": "highest",
        "min_confidence": 0.7
      },
      "auto-memory-integration": {
        "enabled": true,
        "auto_save": true,
        "categories": ["code", "docs", "context"]
      },
      "similar-task-finder": {
        "enabled": true,
        "min_similarity": 0.7,
        "max_results": 5
      }
    }
  }
}
```

---

## 🧪 Тестирование навыков

### Тест 1: Auto Memory Check
```bash
Запрос: "какой я сериал смотрю?"
Ожидание: ✅ Проверка памяти → ответ из памяти
```

### Тест 2: Task Analysis
```bash
Запрос: "добавь функцию экспорта"
Ожидание: ✅ Анализ задачи → сохранение в память
```

### Тест 3: Similar Task Finder
```bash
Запрос: "нужно добавить экспорт в PDF"
Ожидание: ✅ Найдена похожая задача (Excel экспорт)
```

---

## 📚 Связанные ресурсы

- [Memory Hooks](../../hooks/memory/README.md) - Хуки для автоматизации
- [Memory MCP Setup](../../mcp-configs/memory-config.json) - Конфигурация
- [Анализ системы](../../hooks/memory/MEMORY_HOOKS_ANALYSIS.md) - Детальный анализ

---

## 🎓 Best Practices

### 1. Структурируйте данные
```javascript
// ✅ ПРАВИЛЬНО - структурированные наблюдения
observations: [
  "Location: src/auth/",
  "Dependencies: jwt, bcrypt",
  "Created: 2025-10-31"
]

// ❌ НЕПРАВИЛЬНО - все в одной строке
observations: ["src/auth/ jwt bcrypt 2025-10-31"]
```

### 2. Используйте метаданные
```javascript
// ✅ ПРАВИЛЬНО - с метаданными
mcp__memory-ai__save_conversation_fact({
  content: "Решение проблемы X",
  metadata: {
    task_id: "TASK-123",
    priority: "high",
    author: "Claude"
  }
})
```

### 3. Связывайте сущности
```javascript
// ✅ ПРАВИЛЬНО - создаем связи
mcp__memory__create_relations([{
  from: "Task_Analysis",
  to: "Task_Plan",
  relationType: "has_plan"
}])
```

---

**Версия:** 1.0
**Дата:** 2025-10-31
**Статус:** Реорганизация завершена, требуются новые навыки

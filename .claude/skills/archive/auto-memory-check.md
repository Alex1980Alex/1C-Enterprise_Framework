# 🧠 Auto Memory Check - Автоматическая проверка памяти

## 🎯 Назначение

Этот Smart Skill обеспечивает **автоматическую проверку Memory MCP** перед ответом на любой вопрос пользователя, предотвращая ситуацию "не знаю" без проверки накопленных знаний.

## 🔔 Триггеры активации

Skill активируется **АВТОМАТИЧЕСКИ** при:

### Прямые вопросы:
- Начинаются с вопросительных слов: **что, где, когда, как, почему, какой, кто**
- Содержат знак вопроса `?`
- Запросы типа: "покажи", "найди", "расскажи"

### Запросы информации:
- "я смотрю сериал..."
- "помнишь..."
- "ты знаешь..."
- "у меня был проект..."
- "какой активный проект?"

### Контекстные запросы:
- Ссылки на прошлые взаимодействия
- Запросы о текущем состоянии проекта
- Вопросы о предпочтениях пользователя

## ⚙️ Workflow выполнения

### Шаг 1: Извлечение ключевых слов
```javascript
function extractKeywords(userQuestion) {
  // Убрать служебные слова
  const stopWords = ['что', 'как', 'где', 'я', 'ты', 'это', 'есть'];

  // Извлечь важные существительные и глаголы
  const keywords = userQuestion
    .toLowerCase()
    .split(/\s+/)
    .filter(word => !stopWords.includes(word))
    .join(' ');

  return keywords;
}
```

### Шаг 2: Поиск в памяти
```javascript
// ОБЯЗАТЕЛЬНЫЙ первый шаг
const memoryResults = await mcp__memory__search_nodes(
  extractKeywords(userQuestion)
);

if (memoryResults.entities.length === 0) {
  // Если семантический поиск не дал результатов
  // Загрузить весь граф знаний
  const fullGraph = await mcp__memory__read_graph();

  // Попытаться найти релевантную информацию вручную
  searchInGraph(fullGraph, extractKeywords(userQuestion));
}
```

### Шаг 3: Анализ результатов
```javascript
if (foundInMemory) {
  // ✅ Ответить на основе данных из памяти
  return formatAnswerFromMemory(memoryResults);
} else {
  // ❓ Проверить другие источники в порядке приоритета
  return checkOtherSources(userQuestion);
}
```

## 📋 Приоритет источников

```
1. СЦЕНАРИИ РАЗРАБОТКИ (77+ документов)
   └─ Проверка: mcp__1c-framework-docs__search_docs()

2. ОФИЦИАЛЬНАЯ ДОКУМЕНТАЦИЯ 1С
   └─ Проверка: mcp__1c-dev-standards__search_docs()

3. 🔥 ПАМЯТЬ MCP (ТЕКУЩИЙ SKILL)
   └─ Проверка: mcp__memory__search_nodes() + read_graph()

4. ИНТЕРНЕТ-ПОИСК
   └─ Проверка: mcp__brave-search__brave_web_search()
```

## 🎬 Примеры использования

### Пример 1: Личная информация
```
👤 Пользователь: "какой я сейчас смотрю сериал?"

🤖 Claude Code (автоматически):
1. extractKeywords("какой я сейчас смотрю сериал")
   → "сериал смотрю"

2. mcp__memory__search_nodes("сериал смотрю")
   → Найдено:
      - Entity: "Пользователь"
        Observation: "Смотрит сериал 'Ведьмак' (октябрь 2025)"
      - Entity: "Ведьмак"
        Type: "TV_Series"

3. ✅ Ответ: "Вы сейчас смотрите сериал 'Ведьмак'"
```

### Пример 2: Контекст проекта
```
👤 Пользователь: "какой сейчас активный проект?"

🤖 Claude Code (автоматически):
1. extractKeywords("какой сейчас активный проект")
   → "активный проект"

2. mcp__memory__search_nodes("активный проект")
   → Найдено:
      - Entity: "Project_251029_GKSTCPLK-1831"
        Observation: "Статус: В разработке"
        Observation: "Задача: Разработать АРМ по промежуточному композиту"

3. ✅ Ответ: "Активный проект: 251029_GKSTCPLK-1831 - АРМ по промежуточному композиту"
```

### Пример 3: Техническая информация
```
👤 Пользователь: "помнишь как мы решали проблему с блокировками?"

🤖 Claude Code (автоматически):
1. extractKeywords("помнишь как мы решали проблему с блокировками")
   → "решали проблему блокировками"

2. mcp__memory__search_nodes("блокировка проблема решение")
   → Найдено:
      - Entity: "Решение_блокировок_документов"
        Observations: ["Использовали управляемые блокировки",
                      "Добавили retry механизм"]

3. ✅ Ответ: "Да, мы решали проблему с блокировками используя..."
```

## ⚠️ Критические правила

### ❌ НИКОГДА не делать:
```javascript
// ❌ НЕПРАВИЛЬНО
function answer(question) {
  return "У меня нет информации об этом";
}

// ❌ НЕПРАВИЛЬНО
function answer(question) {
  return "Я не могу видеть что вы смотрите";
}
```

### ✅ ВСЕГДА делать:
```javascript
// ✅ ПРАВИЛЬНО
async function answer(question) {
  // 1. Проверить память
  const memory = await checkMemory(question);

  if (memory.found) {
    return formatAnswer(memory.data);
  }

  // 2. Только если в памяти НЕТ - проверить другие источники
  const docs = await checkDocs(question);
  const web = await checkWeb(question);

  // 3. Если НИГДЕ нет - тогда можно сказать "не знаю"
  return "После проверки всех источников (память, документация, интернет) не нашёл информации";
}
```

## 💾 Сохранение новых знаний

После успешного ответа на вопрос - **ВСЕГДА сохранять** новую информацию в память:

```javascript
// После ответа пользователю
await mcp__memory__create_entities([{
  name: "Вопрос_о_сериале",
  entityType: "user_preference",
  observations: [
    "Пользователь интересовался каким сериалом он смотрит",
    "Успешно ответили на основе данных из памяти",
    "Дата: 2025-10-31"
  ]
}]);

// Создать связь с пользователем
await mcp__memory__create_relations([{
  from: "Пользователь",
  to: "Вопрос_о_сериале",
  relationType: "задал_вопрос"
}]);
```

## 🔄 Интеграция с другими Skills

### Связь с unified-smart-skills:
```python
# auto-memory-check является БАЗОВЫМ навыком
# Он автоматически используется ВСЕМИ другими skills:

SmartSkill("1c-code-analysis")
    → auto-memory-check (проверка похожих анализов)
    → выполнение анализа
    → сохранение результатов в память

SmartSkill("1c-development-task")
    → auto-memory-check (поиск похожих задач)
    → выполнение разработки
    → сохранение решения в память
```

## 📊 Метрики эффективности

Отслеживание использования:

```markdown
## Отчёт Auto Memory Check (неделя)

**Активаций:** 47
**Успешных находок в памяти:** 38 (81%)
**Предотвращено ошибок "не знаю":** 35
**Новых entities сохранено:** 12
**Новых relations создано:** 23

**TOP-3 типов запросов:**
1. Личные предпочтения (сериалы, проекты) - 45%
2. Контекст разработки (активные задачи) - 35%
3. Технические решения (паттерны, код) - 20%
```

## 🎯 Конфигурация

### Настройка в .claude/settings.local.json:

```json
{
  "skills": {
    "auto-memory-check": {
      "enabled": true,
      "auto_trigger": true,
      "priority": "highest",
      "min_confidence": 0.7,
      "fallback_to_full_graph": true
    }
  }
}
```

## 🧪 Тестирование

### Тестовые сценарии:

```bash
# Тест 1: Личная информация
Вопрос: "какой я сериал смотрю?"
Ожидание: ✅ Проверка памяти → ответ "Ведьмак"

# Тест 2: Проект
Вопрос: "какой активный проект?"
Ожидание: ✅ Проверка памяти → ответ с номером проекта

# Тест 3: Отсутствие данных
Вопрос: "сколько мне лет?"
Ожидание: ✅ Проверка памяти → проверка других источников → "нет данных"
```

---

## 📝 Резюме

**Auto Memory Check** - это критически важный базовый навык, который:
- ✅ Предотвращает потерю контекста между сессиями
- ✅ Обеспечивает персонализированные ответы
- ✅ Накапливает знания о пользователе и проектах
- ✅ Автоматически работает без явных команд

**Версия:** 1.0
**Дата:** 2025-10-31
**Автор:** Система Smart Skills
**Статус:** ✅ Активирован автоматически

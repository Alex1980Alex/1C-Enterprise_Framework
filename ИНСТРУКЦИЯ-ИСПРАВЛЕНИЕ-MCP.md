# 🔧 Исправление MCP серверов - ПОШАГОВАЯ ИНСТРУКЦИЯ

**Дата:** 2025-10-28
**Статус:** Конфигурация исправлена, требуется перезагрузка Claude Desktop

---

## ✅ ЧТО ИСПРАВЛЕНО

### 1. Удалены дубликаты серверов
- ❌ `1c-enterprise-framework` (дубль) → удален
- ✅ `1c-enterprise-database` → оставлен

- ❌ `1c-docs` (дубль) → удален
- ✅ `1c-framework-docs` → оставлен

### 2. Исправлена конфигурация docling
- ➕ Добавлен параметр `--transport stdio`
- Теперь: `python server_enhanced.py --transport stdio`

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС

### ШАГ 1: Полностью закрыть Claude Desktop

**Windows:**
```
1. Нажмите Ctrl + Q или закройте через меню File → Quit
2. Убедитесь, что процесс Claude полностью завершен:
   - Откройте Диспетчер задач (Ctrl + Shift + Esc)
   - Найдите процесс "Claude" в списке
   - Если есть - завершите его принудительно
```

### ШАГ 2: Перезапустить Claude Desktop

```
1. Откройте Claude Desktop заново
2. Подождите 10-15 секунд для инициализации всех серверов
3. Проверьте статус MCP серверов
```

### ШАГ 3: Проверить статус серверов

После запуска вы должны увидеть:

✅ **Работают (должны быть зеленые):**
- filesystem
- memory
- sqlite
- ripgrep
- ast-grep-mcp
- brave-search
- sequential-thinking
- playwright-automation
- clipboard
- markitdown
- zip
- **1c-enterprise-database** ⭐
- **1c-framework-docs** ⭐
- **docling** ⭐
- serena
- auto-documenter
- context7
- jira-vpn

❌ **НЕ должны показываться (удалены):**
- ~~1c-enterprise-framework~~
- ~~1c-docs~~

---

## 🐛 ЕСЛИ ПРОБЛЕМЫ ОСТАЛИСЬ

### Проблема 1: Docling все еще не запускается

**Диагностика:**
```bash
cd D:/1C-Enterprise_Framework
python scripts/diagnose-mcp-servers.py
```

**Проверка вручную:**
```bash
cd D:/1C-Enterprise_Framework/mcp-docling-server
"C:\Users\AlexT\AppData\Local\Programs\Python\Python313\python.exe" server_enhanced.py --transport stdio --help
```

### Проблема 2: Старые серверы все еще видны

**Решение:** Это кэш интерфейса Claude Desktop

1. Закройте Claude Desktop
2. Удалите кэш:
```bash
rm -rf "$APPDATA/Claude/Cache"
rm -rf "$APPDATA/Claude/Code Cache"
```
3. Перезапустите Claude Desktop

### Проблема 3: Ошибка "Server failed to start"

**Проверьте логи:**
```bash
# В Windows логи могут быть в:
%APPDATA%\Claude\logs
# или
%LOCALAPPDATA%\Claude\logs
```

---

## 📋 ТЕКУЩАЯ КОНФИГУРАЦИЯ

### Python-серверы (3 штуки):

1. **1c-enterprise-database**
   - Путь: `D:/1C-Enterprise_Framework/mcp-1c-integration/src`
   - Команда: `python -m py_server stdio`
   - Статус: ✅ Работает

2. **1c-framework-docs**
   - Путь: `D:/1C-Enterprise_Framework/scripts/docs-mcp`
   - Команда: `python mcp_server.py`
   - Статус: ✅ Работает

3. **docling**
   - Путь: `D:/1C-Enterprise_Framework/mcp-docling-server`
   - Команда: `python server_enhanced.py --transport stdio`
   - Статус: ✅ Исправлено (требуется перезагрузка)

---

## ✨ ИТОГОВАЯ ПРОВЕРКА

После перезагрузки Claude Desktop выполните:

```bash
# 1. Проверка, что дубликаты удалены
grep -E "(1c-enterprise-framework|1c-docs)" "$APPDATA/Claude/claude_desktop_config.json"
# Результат: пусто (ничего не найдено)

# 2. Проверка docling
grep -A 5 '"docling"' "$APPDATA/Claude/claude_desktop_config.json"
# Результат: должен содержать "--transport", "stdio"

# 3. Валидация JSON
python -m json.tool "$APPDATA/Claude/claude_desktop_config.json" > /dev/null && echo "OK"
```

---

## 📞 ПОДДЕРЖКА

Если проблемы остались:
1. Запустите диагностику: `python scripts/diagnose-mcp-servers.py`
2. Сообщите результаты диагностики
3. Предоставьте логи Claude Desktop

---

**ВАЖНО:** Все изменения уже сохранены в конфигурацию!
**ТРЕБУЕТСЯ:** Только перезагрузка Claude Desktop!

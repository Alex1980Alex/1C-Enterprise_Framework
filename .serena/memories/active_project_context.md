# Контекст активного проекта

## Текущий активный проект
**Проект:** 251029_GKSTCPLK-1831  
**Путь:** src/projects/configuration/251029_GKSTCPLK-1831  
**Последнее обновление:** 2025-10-29  
**Задача:** Разработать АРМ по промежуточному композиту для УС по партиям ТС

## 🔄 Динамическое определение проектов

### ⚠️ ВАЖНО: Список проектов определяется автоматически!

Не хранить статический список проектов в памяти. Вместо этого:

1. **При первом запросе в сессии:**
   ```python
   list_dir("src/projects/configuration", recursive=False)
   # Получить актуальный список проектов
   ```

2. **При явном запросе списка:**
   ```
   User: "Покажи список проектов"
   Claude: использует list_dir для получения актуального списка
   ```

3. **При необходимости валидации:**
   ```python
   # Проверить, существует ли проект
   projects = list_dir("src/projects/configuration")
   if project_name in projects:
       # Проект существует
   ```

## Как определять активный проект

### Автоматическое определение по пути файла
Когда пользователь открывает файл, извлекаем имя проекта из пути:
```
Путь: d:\1C-Enterprise_Framework\src\projects\configuration\251029_GKSTCPLK-1831\src\DataProcessors\...
Проект: 251029_GKSTCPLK-1831
```

### Паттерн пути:
```
src/projects/configuration/{PROJECT_NAME}/...
```

## Инструкции для Claude при работе с файлами

### ⚠️ ВАЖНО: Проверка контекста перед работой
Перед использованием Serena инструментов ВСЕГДА:

1. **Определить проект из пути файла:**
   - Если пользователь упоминает путь или открывает файл
   - Извлечь имя проекта между `configuration/` и следующим `/`
   
2. **Обновить активный проект в памяти:**
   - Если проект изменился, обновить эту память
   - Записать новый активный проект

3. **Использовать правильные relative_path:**
   - ✅ ПРАВИЛЬНО: `src/projects/configuration/251029_GKSTCPLK-1831/src/Documents/...`
   - ❌ НЕПРАВИЛЬНО: `src/Documents/...` (без указания проекта)

### Пример workflow:

```python
# 1. Пользователь открыл файл или упомянул путь
user_file = "src/projects/configuration/251029_GKSTCPLK-1831/src/DataProcessors/гкс_АРМПромежуточныйКомпозит/..."

# 2. Извлечь проект
import re
match = re.search(r'configuration/([^/]+)/', user_file)
if match:
    current_project = match.group(1)  # "251029_GKSTCPLK-1831"
    
# 3. Проверить, изменился ли проект
if current_project != "251029_GKSTCPLK-1831":
    # Обновить память active_project_context
    # Записать новый активный проект
    pass

# 4. Использовать полный путь для Serena
relative_path = f"src/projects/configuration/{current_project}/src/DataProcessors/..."
```

## Автоматическое переключение

Claude должен автоматически:
1. ✅ Отслеживать упоминания путей файлов в сообщениях пользователя
2. ✅ Проверять system-reminder об открытых файлах
3. ✅ Извлекать имя проекта из пути
4. ✅ Обновлять эту память при смене проекта
5. ✅ Использовать правильный relative_path для всех Serena операций

## Сигналы смены проекта

### Явные сигналы:
- "Переключись на проект 251024_GKSTCPLK-1743"
- "Работаю теперь в demo-accounting"
- "Открой файл из другого проекта"

### Неявные сигналы:
- `<system-reminder>` с путем к файлу из другого проекта
- Упоминание пути с другим именем проекта
- Команды типа "найди в проекте X"

## Реакция на смену проекта

При обнаружении смены проекта Claude должен:

1. **Информировать пользователя:**
   ```
   🔄 Переключаюсь на проект: {new_project}
   ```

2. **Обновить память:**
   - Записать новый активный проект
   - Обновить дату/время последнего переключения

3. **Скорректировать пути:**
   - Использовать новый base path для всех операций
   - Обновить relative_path параметры

## 📊 Получение актуального списка проектов

### Команда для получения списка:
```python
list_dir("src/projects/configuration", recursive=False)
```

### Это вернет актуальный список на момент запроса:
```json
{
  "dirs": [
    "src/projects/configuration/250928_GKSTCPLK-1706",
    "src/projects/configuration/251027_GKSTCPLK-1788",
    "src/projects/configuration/251029_GKSTCPLK-1831",
    "src/projects/configuration/demo-accounting",
    ...
  ],
  "files": []
}
```

### При запросе списка проектов:
1. Использовать `list_dir` для получения актуального списка
2. Извлечь имена проектов (последний сегмент пути)
3. Пометить текущий активный проект звездочкой ⭐
4. Показать пользователю

## История переключений (последние 10)

1. 2025-10-29 15:30: 251029_GKSTCPLK-1831 ⭐ (текущий) - Инициализация проекта
2. 2025-10-28 14:45: 251027_GKSTCPLK-1788

## Примечания

- Serena индексирует весь D:\1C-Enterprise_Framework как единый проект
- Физической реиндексации не требуется при переключении
- Важно только правильно указывать relative_path
- Каждая конфигурация - это логический подпроект в едином workspace
- **Список проектов динамический - всегда используй list_dir для получения актуального состояния**

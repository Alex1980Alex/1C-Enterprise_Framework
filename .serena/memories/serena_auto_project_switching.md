# Serena: Автоматическое переключение проектов

## 🎯 Цель
Автоматически определять и переключаться между проектами 1С на основе активности пользователя.

## 🔄 Динамическое определение проектов

### ⚠️ КРИТИЧЕСКИ ВАЖНО:
**НЕ хранить статический список проектов!**

Проекты могут:
- ❌ Быть удалены
- ✅ Добавляться новые
- ✅ Переименовываться
- ✅ Создаваться временно

### Правильный подход:
```python
# ВСЕГДА получать актуальный список динамически
def get_available_projects():
    result = list_dir("src/projects/configuration", recursive=False)
    projects = []
    for dir_path in result.dirs:
        # Извлечь имя проекта (последний сегмент пути)
        project_name = dir_path.split('/')[-1]
        projects.append(project_name)
    return projects
```

## 📋 Правила автоматического определения проекта

### 1. Мониторинг system-reminder
Claude получает уведомления когда пользователь открывает файлы:
```xml
<system-reminder>
The user opened the file d:\1C-Enterprise_Framework\src\projects\configuration\251027_GKSTCPLK-1788\src\DataProcessors\...
</system-reminder>
```

**Действие:** Извлечь проект из пути → Проверить смену → Обновить контекст

### 2. Парсинг путей в сообщениях
Пользователь может упомянуть:
- "Открой src/projects/configuration/demo-accounting/src/Documents/..."
- "Работаю в 251024_GKSTCPLK-1743"
- "Перейди к проекту GKSTCPLK-1817"

**Действие:** Извлечь имя проекта → Переключиться

### 3. Явные команды переключения
- "Переключись на проект X"
- "Активируй конфигурацию Y"
- "Работаю теперь с Z"

**Действие:** Немедленно переключиться → Подтвердить пользователю

## 🔧 Алгоритм переключения

### Шаг 1: Детектирование проекта
```python
import re

def extract_project_from_path(file_path: str) -> str | None:
    """
    Извлекает имя проекта из пути файла.
    
    Примеры:
    - "src/projects/configuration/251027_GKSTCPLK-1788/..." → "251027_GKSTCPLK-1788"
    - "d:\1C-Enterprise_Framework\src\projects\configuration\demo-accounting\..." → "demo-accounting"
    """
    pattern = r'configuration[/\\]([^/\\]+)[/\\]'
    match = re.search(pattern, file_path)
    return match.group(1) if match else None
```

### Шаг 2: Валидация проекта (динамически)
```python
def validate_project(project_name: str) -> bool:
    """
    Проверяет, существует ли проект в файловой системе.
    """
    available_projects = get_available_projects()
    return project_name in available_projects
```

### Шаг 3: Проверка смены проекта
```python
def should_switch_project(new_project: str, current_project: str) -> bool:
    """
    Определяет, нужно ли переключаться.
    """
    if new_project is None:
        return False
    
    # Валидировать существование проекта
    if not validate_project(new_project):
        return False
    
    return new_project != current_project
```

### Шаг 4: Выполнение переключения
```python
def switch_project(new_project: str):
    """
    1. Валидировать проект (существует ли?)
    2. Прочитать active_project_context
    3. Обновить активный проект
    4. Добавить запись в историю
    5. Записать обратно в память
    6. Информировать пользователя
    """
    # Валидация
    if not validate_project(new_project):
        print(f"⚠️ Проект '{new_project}' не найден!")
        print("Используйте команду 'покажи список проектов' для просмотра доступных.")
        return False
    
    # Обновление памяти
    read_memory("active_project_context")
    # Изменить активный проект
    # Добавить в историю
    write_memory("active_project_context", updated_content)
    
    # Уведомление пользователя
    print(f"🔄 Переключился на проект: {new_project}")
    return True
```

## 📝 Workflow для Claude

### При получении system-reminder:
```
1. ✅ Проверить: содержит ли путь "configuration/"?
2. ✅ Извлечь имя проекта
3. ✅ Прочитать active_project_context
4. ✅ Сравнить с текущим активным проектом
5. ✅ Если отличается → переключиться (с валидацией)
6. ✅ Информировать пользователя (если переключился)
```

### При запросе списка проектов:
```
1. ✅ Использовать list_dir("src/projects/configuration")
2. ✅ Извлечь имена проектов
3. ✅ Прочитать текущий активный проект
4. ✅ Пометить активный звездочкой ⭐
5. ✅ Показать список пользователю
```

### При использовании Serena инструментов:
```
1. ✅ Убедиться, что активный проект известен
2. ✅ Использовать полный relative_path с именем проекта
3. ✅ Формат: "src/projects/configuration/{active_project}/..."
```

### При явной команде пользователя:
```
1. ✅ Валидировать проект (существует ли?)
2. ✅ Если не существует → сообщить и показать список
3. ✅ Если существует → переключиться
4. ✅ Обновить память
5. ✅ Подтвердить: "🔄 Переключился на проект: {project}"
```

## 🎨 Шаблоны уведомлений

### Автоматическое переключение:
```
🔄 Обнаружил переключение на проект: **{new_project}**
Предыдущий проект: {old_project}
```

### Первое использование в сессии:
```
✅ Работаю с проектом: **{current_project}**
```

### Подтверждение явного переключения:
```
🔄 Переключился на проект: **{new_project}**
Путь: src/projects/configuration/{new_project}
Готов к работе!
```

### Проект не найден:
```
⚠️ Проект '{project_name}' не найден в файловой системе!

Возможные причины:
- Проект был удален
- Неверное имя проекта
- Проект находится в другой директории

Используйте команду "покажи список проектов" для просмотра доступных.
```

### Показ списка проектов:
```
📋 Доступные проекты:

1. project-name-1
2. project-name-2
3. **current-project** ⭐ (текущий)
4. project-name-4
...

Всего: X проектов
```

## 🚨 Важные моменты

### ⚠️ Всегда использовать полные пути
```bsl
// ✅ ПРАВИЛЬНО
find_symbol(
    name_path="/СформироватьДокумент",
    relative_path="src/projects/configuration/251027_GKSTCPLK-1788/src/Documents/гкс_ФормированиеНомераПробы/Ext/ManagerModule.bsl"
)

// ❌ НЕПРАВИЛЬНО (пропущен проект)
find_symbol(
    name_path="/СформироватьДокумент",
    relative_path="src/Documents/гкс_ФормированиеНомераПробы/Ext/ManagerModule.bsl"
)
```

### ⚠️ Проверять перед каждой операцией
Перед вызовом любого Serena инструмента:
1. Убедиться, что active_project известен
2. Если нет - определить из контекста
3. Если невозможно - спросить пользователя

### ⚠️ Валидировать проекты динамически
```python
# НЕПРАВИЛЬНО - статический список
static_projects = ["251027_GKSTCPLK-1788", "demo-accounting"]
if project in static_projects:  # ❌ Может быть устаревшим!

# ПРАВИЛЬНО - динамическая проверка
available = get_available_projects()  # list_dir()
if project in available:  # ✅ Актуальное состояние!
```

### ⚠️ Обновлять историю
Каждое переключение записывать в историю:
```
## История переключений (последние 10)

1. 2025-10-28 14:45 → 251027_GKSTCPLK-1788 ⭐
2. 2025-10-28 12:15 → demo-accounting
3. 2025-10-28 10:00 → 251024_GKSTCPLK-1743
```

## 🔍 Отладка

### Если Serena не находит символы:
1. ✅ Проверить active_project в памяти
2. ✅ Проверить правильность relative_path
3. ✅ Убедиться, что путь включает имя проекта
4. ✅ Проверить, что файл существует (использовать list_dir)
5. ✅ Валидировать проект через get_available_projects()

### Если проект определяется неверно:
1. ✅ Проверить паттерн извлечения из пути
2. ✅ Убедиться, что путь содержит "configuration/"
3. ✅ Проверить, что имя проекта валидно
4. ✅ Использовать list_dir для проверки существования

### Если список проектов не совпадает:
1. ✅ НЕ использовать статический список!
2. ✅ Всегда использовать list_dir
3. ✅ Кэшировать только в рамках одного запроса
4. ✅ При каждом новом запросе получать свежий список

## 📊 Команды для пользователя

- "Покажи список проектов" → получить актуальный список
- "Какой проект активен?" → показать текущий проект
- "Переключись на проект X" → переключиться на проект
- "История переключений" → показать последние переключения

## 🎓 Примеры

### Пример 1: Автоматическое определение
```
User: открыл файл из проекта demo-accounting

Claude:
1. Получил system-reminder с путем
2. Извлек "demo-accounting" из пути
3. Валидировал через list_dir
4. Проверил текущий проект (251027_GKSTCPLK-1788)
5. Обнаружил смену
6. Обновил память
7. Ответил: "🔄 Переключился на проект: demo-accounting"
```

### Пример 2: Явная команда
```
User: переключись на 251024_GKSTCPLK-1743

Claude:
1. Распознал команду переключения
2. Валидировал проект через list_dir
3. Обновил память
4. Ответил: "🔄 Переключился на проект: 251024_GKSTCPLK-1743"
```

### Пример 3: Проект не найден
```
User: переключись на old-deleted-project

Claude:
1. Попытался валидировать через list_dir
2. Проект не найден
3. Получил актуальный список
4. Ответил: 
   "⚠️ Проект 'old-deleted-project' не найден!
   
   Доступные проекты:
   1. 251027_GKSTCPLK-1788 ⭐
   2. demo-accounting
   ..."
```

### Пример 4: Показ списка
```
User: покажи список проектов

Claude:
1. Вызвал list_dir("src/projects/configuration")
2. Извлек имена проектов
3. Прочитал активный проект
4. Отформатировал список
5. Показал пользователю
```

## 🔄 Интеграция с существующими памятями

Эта система работает вместе с:
- **active_project_context** - хранит текущий проект (НЕ хранит статический список!)
- **project_overview** - общая структура проектов
- **1c_code_conventions** - правила кодирования
- **serena_bsl_integration_status** - статус интеграции

## ✅ Чеклист для Claude

При каждом запросе пользователя:
- [ ] Проверить наличие путей в сообщении
- [ ] Проверить system-reminder
- [ ] Извлечь имя проекта (если есть)
- [ ] Валидировать проект динамически (list_dir)
- [ ] Сравнить с текущим активным
- [ ] Переключиться при необходимости
- [ ] Использовать полные пути для Serena
- [ ] Обновить память при переключении

## 🆕 Дополнительно: Управление списком проектов

### При удалении проекта:
```
User: Я удалил проект X

Claude:
1. Не нужно обновлять память!
2. list_dir автоматически вернет актуальный список
3. При следующем запросе проект уже не будет в списке
```

### При добавлении нового проекта:
```
User: Я создал новый проект Y

Claude:
1. Не нужно обновлять память!
2. list_dir автоматически увидит новый проект
3. Можно сразу переключиться на него
```

Динамический подход делает систему самоподдерживающейся! 🚀
